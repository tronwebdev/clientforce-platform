# SURFACE_SPECS — Console v3, end to end
Behavior/state/data contract per surface. COMPOSITION & COPY: prototypes/Clientforce Console.dc.html WINS — click through it; this doc tells you what must work, the proto shows how it looks. ✦ = AI-composed, always with provenance line.

## 0 · Shell
Anatomy: wash page → left floating campaign RAIL (262px, collapsible) → center CANVAS (max 1120px) → right DOCK strip (Console tile + 9 menu tiles + receptionist tile above, separated).
- Rail: logo row + premium focus capsule (collapses rail; manual only — NO auto-collapse); workspace switcher popover (WORKSPACES list, ⚙ Workspace settings, Account home); Campaigns panel: count pill, campaign rows (name, ✦ suggested tag, ✓ GOAL MET pill, status dot, goal line, mint selected state + chat-bubble connector arrow), View all campaigns (cyan); SUGGESTED section (dismissable ✦ cards); collapsed state = short pill w/ icon, not full-height.
- Focus choreography (LOCKED — SHARP): user-clicked only; snap easing cubic-bezier(.32,.72,0,1); rail opacity .22s / transform .3s (+ filter .3s), slim rail .18s delay .12s, dock max-height .38s / opacity .26s, canvas cfRecede .34s / cfApproach .4s. Collapsed rail EXPERIMENT: ghost-fade into background (opacity .16, blur 3px, saturate .55, scale .96, z-index 0) — confirm vs full hide at port time. Dock active tile carries a white chat-tail pointer on its left edge.
- Dock: Style A tiles (tokens doc), tooltip labels, Inbox badge = needs count (amber ping), active ink ring; Console tile = ✦ + gradient underline, breathing; dock stack collapses into Console tile.
- Ada BAR (bottom, both campaign + dock pages — TWO instances in proto): glow border, per-surface placeholder + chips (adaChips/adaBarPh keyed by surface), routeAsk() routes typed asks (create-intents open guided build, else Ada panel); ? help fab at right end of BOTH bars + floating ? on barless pages (Business/Create/All-campaigns).

## 1 · Campaign console (tab strip: Overview · Activity(hidden, via View all) · Inbox · Pipeline · Plan · Stats · Settings)
Overview: richer summary card (agent read, next up), metric tiles w/ sub-lines + deltas (hero Booked tile: green glow, "8 of 12 slots"), ACTIVITY-as-hero card ("Ada is working this campaign", pulsing dot, avatar faces via UFACE, first-row gradient highlight + edge), NEEDS YOU cards (approve/later, dismiss persists), suggested automations ✦ section.
Agent activity page (via View all only): filter pills (All/Sends/Replies/Bookings/Decisions ✦/Voice/Automations/Forms/Proposals), TODAY/YESTERDAY groups, rows = time + avatar face (fallback icon tile) + text + kind pill + mono meta, expandable receipt detail (K/V rows + links to inbox/steps/contacts/settings) + "✦ receipted · event id". Event types incl. send, booking, reply-classified, guardrail decision, voice call, automation rule, enrich, form submission, proposal engagement, WhatsApp knowledge answer.
Inbox (campaign): category chips w/ counts, thread list (unread weight, channel chip, cat pill), thread pane: header = avatar+name (click → profile dropdown w/ contact fields + Open full contact → Contacts detail + ＋ Add to list), cat pill, ⋯ move menu, ＋☰ quick-list chip, ✓ Done; message stream = email cards (subject header) / SMS bubbles / call rows; Ada DRAFT card (✦, edit/approve); reply box w/ channel select; send updates thread.
Pipeline: list + board views; board columns by stage w/ dots+counts, cards = avatar face, name/company, ＋☰ quick-list, stage action chip (NUDGE ›/OPEN › → ✓ QUEUED). More contacts per column than v1.
Plan: guided/manual seq toggle; timeline w/ left line (console-X pattern); step cards (channel chip, title, ✦ brief, credits) → step detail modal (subject checks, length, edit brief); delay chips clickable (editable); timezone row clickable; Add a step menu = Email/SMS/WhatsApp(disabled)/Voice/Wait + "Booking link — your closer ✦" (inserts wait+closer step w/ vertical-aware brief). BRANCHES & RULES (simplified): reply branch (intent categorization list), no-reply → re-engage quietly; sub-campaign rows expandable w/ mini sequences; housekeeping rules + intents = collapsed rows; suggested automations ✦.
Stats: numbers-first tiles + credits attached (per-channel costs), Ada narration lines, filters (time/channel/campaign/team).
Settings: strategy/selling arc/agent language/guardrails from repo vocabulary (strategy.ts, language.ts, guardrails.ts) — prominent labeled tab.

## 2 · Contacts
Header toolbar DYNAMIC (contact context when inside a detail). Lists rail (All contacts + lists + custom lists + ＋ new), relationship toggle (Customers/Prospects/Mixed) on All too (aggregates across lists); table: avatar photo/initials circle, name+email, mono phone, stage pill, lists, campaign, last + ＋☰ quick-list per row (opens detail w/ list menu). Detail: avatar hero row (photo, name, badges), fields grid, campaigns, activity timeline w/ icons, notes (placeholder tells what to add), tags (labeled add), move dropdown (not cut off), ＋ Add to list button + menu (list rows w/ counts + create-new inline), call/pause actions. Add contacts: manual popup + CSV (guided gb flow: source→mapping toggles→relationship+list name→hygiene receipt: dedupe+suppression).

## 3 · Lead finder (workspace-generic, NOT agency-only)
Opens CALM: Ada comment card (pre-run ICP matches line + scout mono line ⟳ + chat POINTER/tail into results) → YOUR MATCHES list: scored rows (fit % + reason receipt "looks like <best customer>", warm ◔ public signals for all, 🔥 surge pills only when BuyerPing connected), ✓-in-contacts dedupe marks, select → footer: enroll → campaign picker (existing or new), Save to list secondary, credits honest on button. "⌕ Run a new search" reveals param panel (Direct mode: industry/radius/size filters; Ada mode: describe outcome). Save any search as SCOUT (weekly, lands in Needs you). BuyerPing: 2-tier intent explainer + own 3-step connect flow (key → watch topics → surge alerts), before/after states.

## 4 · Forms & pages
List: compact cards (like chatbot grid) w/ live/draft pill, URL, responses count + NEW badge notification; ＋ create row (Ada-guided default, popup pattern). Detail: prominent tabs Form/Responses/Settings; Form = field rows (label input, type menu text/email/phone/date/select/long text, select options add/del, required toggle, mono mapping), add field; Responses = rows → detail K/V, Export CSV, unseen-badge clears; Settings = live toggle, hosted link copy, embed snippet, redirect URL + afterLine, success heading+text, double opt-in, SUBMIT BUTTON text, ACCENT swatches (Forest/Ocean/Amber/Ink), routing (campaign+list chips). gb flow: purpose→fields multi (choice w/ options)→required rules→looks (accent+CTA)→after-submit (succH/T + redirect)→routing (+ double opt-in) → creates REAL form, opens detail.

## 5 · Chatbot
List: bot cards (live pill, site, stats). Detail: 156px tab rail (Setup&Preview/Conversations/at-a-glance stats + live-visitor glow card) + LIVE PREVIEW always beside setup (host-page mock, launcher, WIDGET PANEL MATCHES Agent Widget v3 Mock: #FBFDFB header + gradient rounded-square mark + Schibsted name + online dot, #F2F6F3 agent bubbles, Powered-by footer w/ gradient square, ✦ disclosure) — preview is INTERACTIVE (quick replies answer). Setup col: name/greeting/quick replies, accent, theme light/dark, radius, position L/R, LEAD CAPTURE field toggles w/ mapping, BEHAVIOR (unread badge, open-after-4s, exit intent), FEATURES (AI callback + call-hours inputs + guardrail note, voice-in-browser, proposal generation), embed copy, publish toggle w/ honest line. gb flow: primary goal MULTI → name/site → greeting → looks → behavior → extra powers (callback/voice/proposal) → lead capture → review → creates bot.
Widget parity: packages/widget port follows Agent Widget v3 - Mock exactly (shadow-DOM, accent = one brandable token, shadow allowed as the brand exception).

## 6 · Proposals
Default view = YOUR PROPOSALS (not templates); templates tab w/ filter toggles + cover-image cards. Detail = ONE colored container: stats moved LEFT rail (views/opens/value/status timeline), 700px document canvas top-aligned (256px cover hero, 31px editable title, summary, scope list, pricing table, total, CTA block book/pay/both), sticky section bar, block library bar (add Video/Timeline/Stats/etc → block appears, editable inline), setup rail (recipient, price+cadence, tracking, meta). Live-editing: title/price/CTA edit in place. gb flow: recipient (or type) → template → price+cadence → extras toggles (video/timeline/proof) → CTA (book/pay/both + labels + calendar link) → review → CREATES proposal (no error), opens detail.

## 7 · Automations
List + ＋ New automation button → popup chooser: ⚙ Build it yourself | ✦ With Ada. Self builder = FULL campaign-rules.ts vocabulary VERBATIM: WHEN (13 triggers grouped, reply_classified w/ intent multi-select, call_knowledge_gap facets, engagement, meeting incl. before_meeting hours, payment_received, opted_out, sequence_quiet days) → ONLY IF (keyword_contains, reply-only, typed refusal note) → THEN (13 actions incl. terminals first-wins, set stage, tags, notify, carry-link flags, send booking link closer ✦, send proposal/quote ✦, webhook, HubSpot deal ops, run_automation chain, suppress) w/ per-action params + family hues; name; save → lands in list. Ada flow: guided per-template (speed-to-lead etc) in chat anchor.

## 8 · Integrations
Provider grid (7 shipped providers, real logos/branded icons TODO next pass), status pills connected/unhealthy/revoked, NEW badge; detail = accurate per-provider connect steps (from proto+registry: Stripe OAuth+permissions toggles, GCal account+calendar pick+invites, Slack workspace+channel+alert kinds, Webhooks endpoint+signing secret+events, HubSpot portal+direction+dedupe, Calendly event types, Zapier key+triggers), delivery ledger honesty, disconnect pauses dependents w/ why. Ada wiring flow mirrors card steps exactly; BuyerPing hands into its own connect.

### 8b · Ads Closed Loop add-on (Addendum 3 — full spec there)
Meta + Google share ONE bordered ads group (apart from the 7 providers), each row `ADD-ON · $49/mo covers both`. Greyed teaser at page bottom → **Watch** → 90-sec film sheet (8 chapters, play/pause/seek). Unentitled click → FULL-PAGE showcase in logo blues/greens (no hero stat strip; seven value cards; moat section; compliance line; one $49 footer) with the **Ada bar suppressed**. Setup = in-page portable container (~640px), 5 steps ending in consent + hashing ack. Connected = 8 tabs (Closed loop · Ad leads · Value ledger · Audiences · Per-ad truth · Objections · ✦ Suggestions · Health), each with live data. Second provider shows the "already have it, connects free" mint banner. NEW product → wave W11, flag `adsLoop`.

## 9 · Analytics
Numbers-first: stat tiles w/ context lines, Ada narration column (what happened, why, where budget went), filters: time/channel/campaign/team member; advanced tab.

## 10 · Business page (profile + settings BORN TOGETHER)
Opens with a CENTRED header block (Addendum 3 §G): 48px mint gear tile + green glow → "Settings and Business core" 34px/900 −.04em gradient ink → one-line description of what the section governs → status pills (core complete / knowledge gaps); back arrow floats left. Business name appears ONCE, in the identity strip below.
Identity hero (avatar, name, facts, live health chips) → left rail 3 groups: SENDERS (Channels & senders — FIRST) · WHAT THE AGENTS KNOW (Profile & story, Knowledge, Sales brain, Ideal customer, Train agent) · HOW THE WORKSPACE RUNS (Workspace, Team & roles, Custom fields, Notifications & data) · divider · ⛨ Guardrails.
Senders: email/number lists w/ health, warm-up, auth chips; ＋ Add email = 3-step wizard (provider → connect/OAuth or SMTP → DNS verify SPF/DKIM/DMARC posture; "SMS senders have no DNS posture"); ＋ Add number = Buy (area code → picks → mono price line "voice, SMS & WhatsApp · $1.15/mo plus usage" → Buy CTA; A2P pending card, texting locked until approval) | Port (number+carrier+account+PIN → authorized name + LOA upload → submit; 7–10 days, stays live; port-pending row).
Sales brain: SELLING ARC row (derived, 4-step chips) + CLOSER row (booking link, intent conditions, vertical-aware note) + TONE + LANGUAGE + strategy rows; Train agent (corrections that stick); Team & roles: RBAC enum verbatim, invite w/ role select; guardrails: caps, windows, suppression (locked rows marked).

## 11 · Workspace inbox (dock)
Workspace-wide toggle + per-campaign dropdown; same thread pane contract as campaign inbox (incl. ＋☰, profile, move); CLIENT PORTAL messages live SEPARATE (not mixed in campaign inbox); Ada summary w/ motion.

## 12 · Agency suite (Account → Agency)
Agency home (setup Q: existing agency w/ URL/socials vs DFY new: template=color scheme/font/style since it IS an agency site, lead-magnet tickbox, big preview, steps, custom domain) · hero carries ALL stats · prominent tabs Workspaces/Websites/… · clean workspace cards · sub-accounts (= client logins, 20 default, # shown; workspaces vs sub-accounts explainer; create = multi-step w/ billing: Stripe or manual notice; Stripe prominent on Earnings) · selling tools (DFY scripts/objections/SOPs w/ preview, no double-render) · client portal (owner sees client comments; portal link = client view BY DESIGN) · reports (cadence, send-update semantics, past reports+trends).

## 13 · Receptionist add-on
Separate dock tile above menu (own branding, grayscale until added); first click → add-on plan upsell → short onboarding (numbers, rules, actions-permission toggle incl. integration actions); drawer: interactions, numbers, rules; incoming-call pop state; all-calls view + per-call detail w/ rule edit.

## 14 · Guided build (gb) engine
One overlay engine, rises from Ada chat anchor (NOT centered popup): one question per step, widgets (pills/multi/in/tg), progress dots, review K/V, create() writes REAL entities then navigates. DEFS per kind: form/bot/prop/csv/int(+per-provider)/auto/leadfind. Launch points: entity-header ✦ buttons, ＋ create rows, routeAsk() intents, dock chips.

## 15 · Tour · checklist · help
90-sec spotlight tour (cutout dim, ring, clamped cards, scroll-into-view w/ 90px headroom, per-stop copy incl. rail/tabs/needs/dock/ada/biz); onboarding checklist (done states, go-links); per-page ? help panel (surface-keyed content) reachable on EVERY page (bar fab or floating fab).