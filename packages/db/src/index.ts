/**
 * @clientforce/db — Prisma schema, migrations, and the tenant-scoped client.
 *
 * T0 stub. The Prisma schema (DATA_MODEL.md §1–§7), pgvector, RLS policies, and
 * the tenant-scoped client that sets `app.workspace_id` / `app.agency_id` land in
 * T1.
 */
export const DB_PACKAGE = "@clientforce/db";
