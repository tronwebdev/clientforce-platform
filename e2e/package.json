{
  "name": "@clientforce/e2e",
  "version": "0.0.0",
  "private": true,
  "description": "End-to-end browser smoke (Playwright) — runs against deployed staging.",
  "scripts": {
    "e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.1",
    "@types/node": "^22.10.5"
  }
}
