# PROGRESS — Phase 1

> Protocol: `PHASE1_HANDOFF.md §E`. Updated **in the same PR** as the work it
> describes. BLOCKING questions stop that thread; NON-BLOCKING proceed with the
> stated default and get logged here. Prototype-vs-spec conflicts are always a
> log entry, never a silent choice (atoms: token doc wins; composition/behavior:
> prototype wins — `UI_PORTING_RULES.md`).

## Status

| Unit                                                              | Status                                                                                                                                                                                     |
| ----------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| PR 0 · repo/doc corrections (handoff §D)                          | ✅ merged (PR #22)                                                                                                                                                                         |
| C1 · shared UI components (`packages/ui`)                         | ✅ merged (PR #24)                                                                                                                                                                         |
| P1.1 · LLM gateway (`packages/ai`)                                | ✅ merged (PR #23)                                                                                                                                                                         |
| P1.2 · Knowledge ingestion + RAG (`packages/knowledge`)           | ✅ merged (PR #25); **live clientforce.io proof passed** (run 28674265474 — see Live verification log); `STORAGE-CONNECTION-STRING` in Key Vault — next deploy auto-wires DOCUMENT uploads |
| P1.3 · Business Context distiller                                 | ✅ merged (PR #26 + fix PR #27); **live clientforce.io distill proof passed** (run 28679138167, 9 cited fields — see Live verification log); citations-snapshot follow-up rides PR #28     |
| P1.4 · Planner → CampaignGraph                                    | ✅ merged (PR #28); **live plan proof passed** (run 28715807651, 8 traceable fields — see Live verification log)                                                                           |
| P1.5 · Email adapter + `SenderConnection` (+A6/A7/A8 enforcement) | ✅ merged (PR #29); **live sandbox-send proof passed** (run 28717709066, real SendGrid provider id — see Live verification log); sandbox stays on until P1.8                               |
| P1.6 · CampaignWorkflow (Temporal)                                | ✅ merged (PR #30); **live durable-timeline proof passed** (run 28719899162 — see Live verification log); run 1's header-gate failure exposed the P1.5 threading conflation, fixed in PR #31 (DEC-033); staging Temporal endpoint = owner checklist §4 (non-blocking until P1.8) |
| P1.7 · Inbound → classify → signal (+`Message` inbound, A9 names) | ✅ merged (PR #32); **live closed-loop proof passed first run** (run 28732467278 — see Live verification log); staging deploy green with Temporal Cloud connected; owner MX + Inbound Parse + webhook key configured 2026-07-05 |
| C2.1 · Shell landing (`/`→`/agents`), Dashboard stub, inert nav   | ✅ merged (PR #33, folded with C2.2 — DEC-036); §1 sidebar built to the 248px prototype literal after the owner's spec-bug fix; **punch-list:** Tools-row padding still pokes out vs the prototype — fix in the next sidebar-touching PR (owner 2026-07-05)                     |
| C2.2 · Agents List (checkpoints §2)                               | ✅ merged (PR #33); §8 set + owner fidelity-review round passed (gradients/profile-flyout/rhythm/avatar-pool/column-width fixes in `7f177a9`) — `docs/fidelity/c2.2/`                     |
| C2.3 · Create Agent wizard (§3)                                   | ✅ merged (PR #34, head `798021b` — includes the owner-review round: editor drawer + deliverability card + personalization chips, step-1 frame literals, singular readiness copy, checkpoints §3 amendment, clean prototypes + `__cf_email__`/`cdn-cgi` grep, 3-shot recapture — DEC-038/DEC-038a); §8 set `docs/fidelity/c2.3/`; **punch-list:** step-3 manual drawer anatomy (below) |
| C2.4 · Agent view — 5 wired tabs (§4)                             | ✅ merged (PR #35) — §8 set `docs/fidelity/c2.4/` (41 shots); DEC-040 conflicts resolved by owner post-merge: §4 tab-bar amendment approved (in PR #36), A8 open/link-tracking extension approved (in the wizard v2 PR) |
| C2.5 · Contacts (§5)                                              | ✅ merged (PR #36) — §8 set `docs/fidelity/c2.5/`; two owner-review rounds (DEC-044 + round-2 duplicate-skip import semantics); A10 segment chips as QUERIES; DEC-034 unsub home; A4 5s polling |
| Wizard v2 round (§3 re-amended) — DEC-039a drawer + v2 port + step-1 live bugs + A8 tracking | ✅ merged (PR #37) — §8 set `docs/fidelity/c2.3v2/` (29 shots incl. the review-round re-shoots); DEC-042/043; A8 `tracking` persists; DEC-024 zero-source gap seeding fixed + tested; CSV ingest (Q-009) |
| C2.6 · Settings → Channels + Suppression (§6)                     | ✅ merged (PR #38) — §8 set `docs/fidelity/c2.6/` (49 shots incl. the XLSX round); DEC-045/046; Q-009 closed (XLSX live), Q-010 queued (Brand-kit persistence) |
| Wizard bug round (owner live-testing B1–B7)                        | 🔨 PR (branch `claude/wizard-bugfix`) — root-cause + honesty round: worker heartbeat + /system/health + degraded banner; per-stage ingest timings; FAILED retry; planner failure+retry state (DEC-038 amendment); B2 URL-scope dropdown; B3 row IngestStatus; B4 blocked-Generate warnings; B5 no-silent-errors + implicit draft; **B6 draft resume (owner GO)**: `Agent.draftState` + wizard autosave/hydrate + Agents-list "Continue setup"; decisions DEC-047/DEC-048 |
| C2.7 · Contact custom fields (`docs/PLAN_CUSTOM_FIELDS.md`)        | 🔨 PR (branch `claude/c27-custom-fields`) — ContactFieldDef + `Contact.custom`; admin-only creation (drawer inline + CSV map `__create__`); detail-drawer inline value edit; `{{custom.<key>}}` tokens w/ mandatory fallback (never blank); 30-def cap; DEC-049 |
| Wizard bug round #2 (owner live-testing B8–B10, post-outage)       | ✅ merged (PR #48) — B8 knowledge-state honesty; B9 add-sender flow; B10 timezone picker; hardening trio (bounded storage writes · health storage probe · auto stranded-source sweep); DEC-053/054 |
| C2.8 · Contact lists (`docs/PLAN_CONTACT_LISTS.md`)                | ✅ merged (PR #49) — §8 set `docs/fidelity/c2.8/` (38 shots incl. the review-round re-shots); DEC-055/056/057; e2e sentinel-disjointness fix rode PR #50 |
| CSV import execution round (owner live-testing IMP-1/2/3)          | ✅ merged (PR #51) — server-side bulk `POST /contacts/import` (transactional dedupe/suppression/list-attach/custom-values); snapshot-at-review tiles; importing progress + real-count done modal + error summary w/ retry-failed-only; DEC-058 |
| C2.9 · Dynamic goal-completion state (`docs/PLAN_GOAL_STATE.md`)   | 🔨 PR (branch `claude/c29-goal-state`) — GOAL_META terminal labels (9-row table verbatim); inbox chip / Contacts tabs+pills+filters / Leads pills / wizard explainer; aggregation rule; event payload `{goalKey,label}`; DEC-059 |
| P2.1 · Twilio SMS adapter (Phase 2 opens)                          | ✅ **CLOSED** (owner call 2026-07-11, "close with A2P item") — full email-pattern port merged (PR #54); live proof green (carrier-confirmed delivery + allow-list & STOP-suppression refusals); STOP round-trip: rail 2 + ledger stance fully evidenced via the live webhook with the standing US test handset (DEC-067, run 29131940801); rail-1 auto-reply + A2P brand/campaign registration are BLOCKING go-live runbook items (account currently has zero A2P registrations); DEC-061/062/063/064/067 |
| M1a · Playbooks + Strategy settings (brain wave 1/5)              | ✅ merged (PR #63) — planner prompt v3 selling craft (step ROLES, banned openers, one CTA, decreasing length); internal goal×category → arc + tone map (full map on the PR plan comment — owner-approved 2026-07-10); optional `strategy` block riding guardrails Json (`strategyNotes` ≤500 / `neverSay` ≤10 — no migration); agent Settings → Strategy section (arc display + notes + never-say chips, future-regens copy); neverSay enforced in prompt AND deterministically in validateAll (bounded repair → typed failure); DEC-065 |
| M1b · Branch taxonomy + classifier v2 (brain wave 2/5)             | 🔨 PR #70 (branch `claude/branch-taxonomy-classifier-v2-on4v88`) — six-intent reply taxonomy (`interested · objection_price · objection_timing · wrong_person · info_request · not_interested`), ADDITIVE `IntentSchema` extension (legacy labels kept everywhere they're pinned); classifier prompt v2 in the versioned registry + one pinned fixture per intent; planner prompt v4 REPLY PLAYBOOK (per-intent branch strategies: price→value-reframe · timing→ack+delayed follow-up · wrong_person→referral ask · info_request→answer+CTA · not_interested→graceful close, stage `lost`, NO suppression); all 3 validation layers + bounded repair updated (six cases + default required at generation); workflow routes by intent value, legacy 1-branch graphs keep executing (back-compat test); Inbox intent chips + wizard/Steps branch labels + verbatim unknown-intent fallback chip; live proof green (run 29132157048 — see Live verification log); CI green; §0 defaults owner-accepted 2026-07-11; DEC-068 |
| P3.0 · Voice spike (Phase 3 de-risk, hard-capped)                 | 🔨 PR #62 (branch `claude/voice-phase-3-spike-wrqf82`) — Twilio Media Streams ⇄ `apps/voice` bridge ⇄ Deepgram Nova STT → Claude Haiku (new additive `voice` gateway route) → Deepgram Aura TTS; barge-in proven (Twilio `clear` + LLM/TTS abort, unit-tested + 4/22 synthetic turns); `Message(channel:"voice")` persistence taste-test (no migration); `voice-live-spike` workflow (preflight/synthetic/demo); ADR `docs/adr/voice-spike.md` = the deliverable. **Verdict: loop is a GO; latency table is a projection until the live call.** Owner-gated: `DEEPGRAM-API-KEY` + `VOICE-FROM-NUMBER` in Key Vault (both added 2026-07-10), then one demo call. DEC-066 (renumbered from 064 — 064/065 landed on main first) |
| G1 · Guided mode, SMS (brain wave 4/5)                            | ✅ merged (PR #81) — step nodes gain `mode: "scripted" \| "guided"` (absent = scripted, legacy graphs byte-identical); guided **sms** steps carry a BRIEF (objective + talkingPoints 3–6 + mustSay/neverSay) instead of body; `composer.sms@v1` renders the real message per lead at send time on the `copy` route (prompt-cached business context); deterministic post-compose checks (ban lists · mustSay · length · token syntax · ungrounded URLs) → ONE bounded retry → typed refusal that PAUSES that lead + Logs row + `sms.compose_refused.v1`; send-sms rails byte-untouched (Message.meta gains `{mode, briefVersion, composerVersion}`); per-agent `composeMode` rides guardrails Json (default scripted, Settings toggle, no wizard field); guided OUTBOUND auto-sends post-checks — guided REPLIES belong to the reply-draft wave (not on main yet; documented default); DEC-070, Q-020 |
| G2 · Guided mode, email (mode+brief → email steps)                | ✅ merged (PR #82) — G1's brief contract ported to email: email steps accept `mode:"guided"` + brief (+ email-only `subjectHint`, all 3 layers + repair extended); `composer.email@v1` (subject + body, **arc-role aware** — main-sequence position → the M1a ladder, opener/value/breakup rules ride the prompt) on the `copy` route with the same cached-context/no-invention rules; email checks on the G1 deterministic set — SUBJECT_RULE (≤60 · no bangs/ALL-CAPS/faux-Re: · playbook banned patterns) + COMPOSED_FOOTER (**the footer stays the boundary's job, forever** — a composed unsubscribe line refuses; footer-exactly-once pinned in tests); sendStep gains the composed pass-through meta (scripted email byte-identical, regression-pinned); `email.compose_refused.v1` catalog twin + channel-aware refusal recording; planner **prompt v7** (both-channel guided briefs, derived from the v5 literal; v6 stays registered; scripted v5 byte-identical) and guided gating drops the sms-sender requirement; compose-preview routes by channel (email subject+body, 2 credits display-only Q-020); UI email brief card/drawer variant (+ subject-hint field) + per-channel credits + Logs/Leads amber rows; mixed-mode fixture (scripted step 1 + guided follow-ups) at DB AND Temporal level; `guided-email-live-proof` workflow (G1's twin: variety · grounding · footer-once cage · mixed-mode · trap refusal); §8 set `docs/fidelity/g2/` (8 shots); DEC-071 |
| L1 · Agent output language (multilingual agents; app UI stays English) | ✅ merged (PR #83) — per-agent `language`/`languageSource` riders on guardrails Json (goalLabel/strategy/composeMode precedent, NO migration; absent = English, legacy rows byte-identical); curated launch list EN·ES·FR·DE·IT·PT·NL·PL (**ratified** — reviewer verdict 2026-07-11, approved as proposed); NO wizard field — the agent-layer distill detects the dominant language of its merged evidence pack (site + docs + typed answers, source-agnostic) with a DETERMINISTIC confidence-gated detector (mixed/ambiguous → English; owner-set sticky; detector-set converges as sources land); Settings tab Language row (select + provenance + in-language compliance preview + pinned-graph copy); generation in-language via versioned prompt bumps (planner v8/v9 — v9 re-derived from G2's v7 both-channel literal per the reviewer rebase deltas — distill v2, composer.sms@v2 AND composer.email@v2; English agents render v5/v7/v1s byte-identical, regression-pinned) + a deterministic language rail in validateAll (confident mismatch → repair → typed failure); understanding proven with pinned DE/FR reply fixtures through classify→branch→stage (NO classifier change) + the live-proof multilingual matrix; compliance strings deterministic from the pre-translated `COMPLIANCE_STRINGS` map at both send boundaries (en byte-equal legacy literals; STOP keyword kept in every translation; completeness vitest-pinned); §8 set `docs/fidelity/l1/` (7 shots); DEC-072 (renumbered from 071 — G2 claimed it on main first, parallel-track rule), Q-021 |
| Clerk integration (A3 — own unit, due before any external demo)   | ✅ merged + **ACCEPTED** (owner Google click-through on staging 2026-07-10: skinned sign-in → Google → /agents with first-run workspace + no-membership isolation verified; post-#58 the switcher listed all 3 workspaces with data populating and re-scoping correctly) (PR #53) — dual-mode Clerk (web provider/middleware/sign-in/up + §7 appearance skin; api lazy User upsert + typed NO_WORKSPACE + first-run POST /workspaces) + live-send flip (`SENDGRID_SANDBOX`, domain-auth-gated live-send-proof workflow); DEC-060/060a. Deploy-fix follow-up PR (DEC-060b): dev rail survives the Clerk flip (middleware + `bearerToken` dual dispatch) + publishable key build-arg'd into the web image; owner-gated: Google acceptance click-through                                                                                                                                                                                         |
| F1 · Outcome feedback v1 (brain wave 3/5)                         | 🔨 PR (branch `claude/outcome-feedback-metrics-p6woab`) — per-step rollup endpoint (`GET /agents/:id/outcomes`, tenant-client RLS, Message send-ledger + Event engagement join) with min-n signal gates (none <20 · low 20–49 · ok ≥50, constants+tested); step-card outcome badges (wizard step-2 + Steps tab) at none/low/ok; outcome-aware regen — planner prompt v5 (M1b's v4 playbook + outcomes section) injected ONLY for low+ steps, confidence-labeled; attribution constants (reply → LAST-SENT step via existing threading · goal → SEQUENCE only); additive Event index; DEC-069; §8 set `docs/fidelity/f1/` (badges both surfaces + endpoint payload + regen diff); unit/integration/e2e green on the local stack |
| W3 · Wizard fidelity round — steps 3/4/6 (owner punch-list)       | ✅ merged (PR #85) — commit 0 extracts the 2,953-line Wizard.tsx into per-step components (pure move — W3 owns the extraction, R1's UI wave rebases onto it); W3-1 the REAL C2.5 import inline over step 3 (ONE shared component, ContactsView's inline flow extracted — the kickoff's packages/ui premise corrected; wizard runs land in a referenceable list, audience by reference via `DraftState.csvListId`); W3-7 audience preview card (real counts, honest empty); W3-8 drawer adds land in the preview; W3-9 step-4 prototype anatomy w/ honesty rails (asset pickers honest-empty → nav-stub create paths, AP panel drops mock found-leads, keyword suggestions derive from own context); W3-10 goal-fit constants (`lib/goal-fit.ts` off the C2.9 registry) — FOR-THIS-GOAL tags, AP not-typical default + note, static suggested-automations strip; W3-3 step-6 anatomy w/ COMPUTED estimate (new display-only core `CREDIT_PRICES` mirroring the db seed + Q-020 guided figures; AP/enrichment as per-lead rates; readiness line rides the strip — no ledger exists); §8 set `docs/fidelity/w3/` (35 shots); DEC-073 |
| R1 · Per-agent automation rules (ARCHITECTURE §151) — wave W1/4: engine | ✅ merged (PR #86) — the SHARED When→If→Then execution core (`packages/automations`, mounted on the T2 no-op automations consumer — consumer #2 is now REAL) restoring the "what the campaign does" layer the M1b Branches tab dropped (owner verdict 2026-07-11); typed trigger/condition/action unions in core (7 triggers mapped to EXISTING bus events — engagement events verified live producers; `sequence_quiet` = the worker sweep, fire-once per (rule, enrollment)); `CampaignRule`/`CampaignRuleRun` additive models + RLS (`Automation`/`AutomationRun` byte-untouched for Phase 6); deterministic precedence rails → rules (row order) → graph continuation via the memoized-evaluation gate on the temporal-signal consumer (terminal action ⇒ signal skipped + durable run cancelled); first-terminal-wins conflicts (`skipped_conflict` rows); causation-depth guard (> 2 ⇒ typed refusal rows, incl. self-referencing automations); redelivery idempotency on unique (ruleId, eventId); honest-absence on missing/disabled automations; PAUSED/UNSUBSCRIBED inertness; `run_automation` typed end-to-end (AutomationRun rows at depth+1); move-to-node = additive `startNodeId` workflow input + `moveEnrollmentToNode` (deterministic restart id, reply signals resolve the STORED workflowId); ONE new emission `automation.rule.run.v1`; fixtures green vs real PG (23 engine + gate + move + walker); W2 seeds/wiring · W3 UI (gated on the wizard extraction) · W4 live proof = follow-up waves; DEC-074 |
| G3 · Guided-mode visibility (wizard entry + Steps read-only briefs + inbox marking) | ✅ merged (PR #87) — three waves, one commit each (one-PR-per-wave deviation flagged: the session pins one branch): W1 step-2 `Scripted \| ✦ Guided` control writing the SAME `guardrails.composeMode` rider Settings owns + "Regenerate to apply" mismatch affordance + the DEC-072 anti-clobber extended to composeMode + draft-endpoint hydration; W2 Steps-tab guided cards open the brief READ-ONLY (wizard-drawer anatomy, honest count-only sends line, status-honest edit affordance — DRAFT resumes setup, launched notes W3-4); W3 inbox marks composed messages from the REAL send-boundary meta ("✦ Composed" + mode line; absent provenance unmarked — no reply-draft surface exists on main, documented default); prototype canon updated to the 2026-07-11 `Create Agent.dc.html` (repo-wins) + artifact scrub; §8 set `docs/fidelity/g3/` (12 shots); planner literals verified at kickoff (en: scripted v5 / guided v7 — v8/v9 are the L1 non-English variants); DEC-075 |
| W3-4 · Agent-view sequence editor (editable graph + per-step mode + briefs) | ✅ merged (PR #88) — five waves, one commit each (the G3 one-PR-per-wave deviation: the session pins one branch): **W0** core graph walk + deterministic mutation layer (`graph/walk.ts`/`graph/mutate.ts` — multi-step chains in a branch first-class, stable node-id policy, `repairGraph`) + `validateEditedGraph` edit gate (relative-to-stored policy; copy rails stay generation-only per the M1a stance) wired into `PUT /planner/graph` + `Enrollment.meta.graphVersion` audit at create/move; **W1** Steps-tab editing per Campaign View canon reusing the SHARED wizard drawer (extracted to `components/sequence/`, wizard renders unchanged) — add-step popover (SMS gated on sender, WhatsApp/Voice honest), inline delay pill, reorder/delete (designed additions), ✦ Regenerate + Settings deep-link (W3-12 closed), DEC-076 live banner; **W2** per-step Scripted\|✦ Guided + dashboard brief editing + deterministic seed (`deriveBriefSeed`) + staged one-step compose (compose-preview gains optional `brief`) + ✦ AI-picked provenance + guided→scripted compose-a-draft; **W3** chain-true reply strategies (edit/reorder/delete/add within a case) + Add-a-sub-campaign honest absence (R1-gated, owner lock); **W4** versioning + gate proof vs real Postgres+RLS (`sequence-editor.e2e.spec.ts` 8/8; api suite 84/84) + §8 set `docs/fidelity/w3-4/` (22 shots); **review round**: 13 adversarially-verified findings hardened w/ regression pins (shared-chain refusal · gap-delay scoping · per-branch playbook check · fresh-read meta merges · epoch-fenced composes · ambiguous forks reject loudly again; the 14th — raw-API cyclic case chains — logged as a known limit, the PUT gate already rejects their output); **deferred proof → PROVEN live (2026-07-14, #91/W0, closes #89):** the Temporal live receipt — the staging edit→versioning walk through a REAL durable run (`sequence-editor-live-proof` run [29369417962](https://github.com/tronwebdev/clientforce-platform/actions/runs/29369417962), 19/19 gates): the mid-sequence lead finished on its ENROLLED v1 (its step-2 sent 6033ms AFTER v2 persisted, v1 scripted copy, Temporal-history input pin receipt, added step never fired for it) while the edit landed MANUAL v2 through the real gate mid-run (policy-layer refusal walk included); a NEW enrollment walked the edited graph — flipped-to-guided step composed (real Sonnet-class, `[ai] model=claude-sonnet-5` receipt) → checks → unchanged boundary → CAN-SPAM footer EXACTLY once, provenance meta `{mode:"guided", briefVersion:2, composer.email@v1}`; DEC-076 |
| SC · Branch creation — "Add a sub-campaign" on R1's trigger vocabulary (#90) | 🔨 PR #96 (branch `claude/sub-campaign-branch-creation-8sog4z`) — the #90 unit, one PR per wave (DEC-077 claimed at dispatch, collision-free vs main `2a8b847`): **W0** (#91, merged) DEC-076 deferred proof PROVEN live (run 29369417962, 19/19 gates) + board ride-alongs; **W1** (#92, merged) deterministic branch-creation mutation — SubcampaignNode-headed chain container + R1 `campaignRuleTriggerSchema` entry trigger via `CampaignRule` move_to_node, through all three gate layers at the reply-branch-count extension point, legacy/#88 graphs byte-identical; **W2** (#94, merged) the creator UI in BOTH hosts through ONE shared `SubcampaignCreator` (StepEditorDrawer precedent — host deltas ride props): 3-step canon modal (trigger → build method → review → done) over R1's kinds via the NEW `lib/triggers.ts` display map (mirrors `lib/intents.ts` — labels only, never a parallel union; prototype's "Reply received"/"Reply contains keyword" NOT R1-expressible → omitted, Q-022); honest AI — "Let AI draft it" derives 2 briefs DETERMINISTICALLY (trigger+name+goal, the seed.ts philosophy) and composes them through the REAL sandbox `compose-preview` (staged briefs), review renders them ✦-marked ("✦ AI-drafted", SeedChip convention), failure/refusal falls back honestly ("AI draft unavailable — starting from scratch", empty seed — never canned copy as AI); creation = the W1 `POST planner/subcampaign` with 422 `detail` verbatim in-modal (CfError carries it; message string unchanged for existing sinks); Steps tab: absence card goes LIVE (canon 2-col container grid + divider + dashed add card; chips from the NEW read-only `GET planner/subcampaign-rules` — R1 rows for DISPLAY, rules CRUD stays out of scope; "Rule pending" honest fallback; NO dashboard ✦ AI chip — provenance unpersisted, Q-023), inline chain expansion edits via the SHARED drawer + per-container "+ Add step" (subcampaign container mutation → the same gate); wizard Branches view: same shared grid (in-session rule state, ✦ AI chip = known this-session provenance) + "✦ Suggest more branches" (2 deterministic goal-seeded suggestions, honest copy, Add opens the creator PREFILLED at the build step); launched agents render LIVE_GRAPH_NOTICE in-modal (`subnew-live-notice`); tests: 20 new web tests (trigger map drift-guarded against the schema, availability exhaustive, brief determinism, compose orchestration vs mocked cf, POST body parsed by the real W1 schema, static canon anatomy) + subcampaign-rules e2e (14/14 vs real PG); **W3** (this PR) staging live-proof PROVEN — run 29401312663 (10/10 gates: R1 routes a durable enrollment into the created branch, interlock live; guided compose in-branch → boundary → footer-once; mid-sequence contact untouched; refusals loud) + DEC-077 close-out |
| B1 W1 · Platform backoffice — shell + staff auth + audit + tenant mgmt (FR-ADMIN-01) | ✅ merged (PR #95, docs/PLAN_B1_W1.md) — internal operator surface: separate staff-auth rail (`cf_staff_session` + audience `clientforce-backoffice` + owner-managed `PlatformStaff` allow-list) & RLS-exempt `clientforce_backoffice` role (`BYPASSRLS`; tenant paths untouched, regression pinned); agencies/workspaces list-search · suspend/reactivate (typed, reversible) · append-only credit-grant ledger; send boundary refuses `TENANT_SUSPENDED`; every action → `BackofficeAuditLog`. Verified vs real Postgres+Redis (API 107 tests incl. new backoffice e2e; web rail smoke-tested). §8 build set `docs/fidelity/b1-w1/`. DEC-079 claimed at dispatch (collision-free vs main `2a8b847`; DEC-078 held by P3.1 #93, DEC-077 by SC #92). W2–W4 follow as their own PRs. |
| B1 W2 · Platform backoffice — usage · reconciliation · credit-price editor (FR-ADMIN-02/03, FR-BILL-02/04) | ✅ merged (PR #97, docs/PLAN_B1_W2.md) — per-tenant consumption (sends by channel, voice minutes, credit burn; AI spend honest-absence "not yet metered"), provider-invoice reconciliation vs a seeded `ProviderInvoice` fixture (the FR-BILL-04 prerequisite), effective-dated credit-price editor (platform defaults + per-agency overrides + history) on the existing `CreditPrice`. Additive `ProviderInvoice` model; on-demand rollups; every price change audited. DEC-080 (collision-free vs main `668cb06`). Builds on W1/#95. |
| B1 W3 · Platform backoffice — product telemetry + adoption dashboards (FR-TELEM-01..04) | ✅ merged (PR #98, docs/PLAN_B1_W3.md) — versioned PII-free telemetry catalog (`packages/telemetry`) behind a swappable `TelemetrySink` (self-hosted PostHog, owner-confirmed; Noop in CI); privacy rail is a schema-pinned test (ids + event names only, never bodies/PII/knowledge); a telemetry consumer on the event bus + a local `TelemetryEvent` store powering activation-funnel · DAU/WAU · feature-adoption · cohort-retention dashboards with statistical-honesty floors. Additive; internal-only, excluded from tenant Analytics. DEC-081 (collision-free vs main `e9b0a0e`). Builds on W1/#95 + W2/#97. |

| B1 W4 · Platform backoffice — fleet health · abuse · kill switch · impersonation · flags (FR-ADMIN-04/05/06) | 🔨 in review (branch `claude/session-oxcd68`, docs/PLAN_B1_W4.md) — the FINAL B1 wave: per-agency/per-channel kill switch (extends the `SendBlockReason` enum with `CHANNEL_KILLED`, the W1 `TENANT_SUSPENDED` pattern — no fork; typed refusal, reversible, audited); fleet sender-health CONSUMES P5-W1's health-score endpoint (contract + pinned interlock — no second computation; honest "pending" when unwired) + backoffice bounce/spam/refusal outlier surfacing (thresholds + human review); audited read-only impersonation (banner-marked, no tenant-content write path) + delivery diagnostics; per-tenant feature flags + read-only model/prompt version-pin visibility. Additive `KillSwitch` + `FeatureFlag` (REVOKEd from `clientforce_app`). DEC-082 (collision-free vs main `1adbad8`). Closes the B1 unit. |

## Decision log

| ID      | Date       | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Why                                                                                                                                   | Reversibility |
| ------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | ------------- |
| DEC-082 | 2026-07-15 | **B1 W4 — Platform backoffice: fleet health · abuse · kill switch · impersonation · flags (FR-ADMIN-04/05/06). The FINAL B1 wave — operational safety + control.** (0) **Scope — wave W4 of B1; closes the unit.** (1) **Kill switch EXTENDS the boundary refusal enum, never a fork:** a new `SendBlockReason` value `CHANNEL_KILLED` + a shared `assertChannelLive` gate beside `assertTenantActive` at the email/SMS boundaries (the W1 `TENANT_SUSPENDED` pattern); per-agency/per-channel `KillSwitch` rows; typed, reversible, audited; disposition PAUSED. (2) **Impersonation is READ-ONLY:** banner-marked, one `BackofficeAuditLog` row per session (`impersonate.start`), read-only thread/log/delivery-diagnostic lookups — NO tenant-content write path. (3) **Fleet health CONSUMES P5-W1's health-score endpoint** (health computed once, by P5-W1): P5-W1 not on main yet → the backoffice codes against its contract (`SenderHealthClient` + DTO) and PINS THE INTERLOCK; honest "pending P5-W1" when unwired, never a second computation. Backoffice adds bounce/spam/refusal outlier + suppression-anomaly surfacing (thresholds + human review, no ML). (4) **Feature flags per tenant (audited) + read-only model/prompt version-pin visibility** (`KillSwitch`/`FeatureFlag` additive, REVOKEd from `clientforce_app`). | The operator needs to stop abuse fast, look without touching, see fleet health without duplicating P5's math, and flag/inspect tenants. Additive-only; no send-path fork (reason enum extended); no tenant-content writes; health not recomputed. | moderate (additive tables + backoffice endpoints/UI + one boundary gate; the kill-switch gate and flags revert cleanly; the P5-W1 interlock is a contract until P5-W1 lands) |
| DEC-081 | 2026-07-15 | **B1 W3 — Platform backoffice: product telemetry + adoption dashboards (FR-TELEM-01..04). Instrument operator/product actions as versioned, PII-free events behind a swappable adapter; make adoption evidence-based.** (0) **Scope — wave W3 of B1** (W4 health/abuse/flags follows as its own PR). (1) **Provider: self-hosted PostHog behind a `TelemetrySink` adapter (owner-confirmed 2026-07-15);** `PostHogSink` is SDK-free (a `fetch` to `/capture/`), the ONLY place the provider is named; `NoopSink` is the CI/test default (vendors mocked). (2) **Privacy rail = a pinned test, not a convention:** every telemetry payload schema admits ONLY id-shaped fields + enums/counts; a schema-pinned test asserts zero PII/body keys (email, body, subject, content, phone, name…). PII can't be represented → can't leak. Internal-only, excluded from tenant Analytics. (3) **Instrument additively from the event catalog:** a 4th event-bus consumer (the DEC-035 injected-deps pattern) maps domain events → PII-stripped telemetry → `{ sink, local TelemetryEvent store }`, plus thin id-only emit points for non-domain actions (wizard step complete/abandon, takeover, regen, settings edit); no planner/prompt/send-path changes. (4) **Dashboards compute from the local `TelemetryEvent` store** (additive, REVOKEd from `clientforce_app`) so they are self-contained + testable with the vendor mocked; below the sample floor → "low data" (the F1 statistical-honesty pattern), never an invented metric. (5) **`ai.spend.v1` metering rides here,** closing W2's AI-spend honest-absence. | Product decisions had no adoption data; the operator needs activation/retention/feature-usage evidence. Additive-only (`TelemetryEvent` + the telemetry catalog), versioned like all events, no tenant-facing exposure, no planner/send changes. | moderate (additive model + new package + a bus consumer + dashboards; the sink defaults to Noop so CI needs no vendor; revert is additive-only) |
| DEC-080 | 2026-07-15 | **B1 W2 — Platform backoffice: usage rollups · provider-invoice reconciliation · credit-price editor (FR-ADMIN-02/03, FR-BILL-02/04). Builds on the W1 rail (DEC-079).** (0) **Scope — wave W2 of B1** (W3 telemetry · W4 health/abuse/flags follow as their own PRs). (1) **Honest absence, no invented metrics:** AI spend is not persisted (the AI gateway logs cost only) → shown "not yet metered"; per-send credit consumption is not wired (the `CreditLedger` carries manual grants today) → that wiring is FR-BILL-04, gated on the reconciliation view this wave builds. (2) **Usage = on-demand aggregation** over `Message`/`Event`/`CreditLedger` (sends by channel, voice minutes from `call.completed.v1.durationSec`, credit burn); `MetricDaily` stays the future rollup home if query cost demands it (flagged, not built). (3) **Reconciliation:** a new platform-global `ProviderInvoice` model (additive, REVOKEd from `clientforce_app`) holds provider invoices; the view compares our metered usage to the invoice per provider per month (variance %). (4) **Credit-price editor reuses `CreditPrice`** — appends effective-dated rows (never updates in place), so the row sequence per `(agencyId, action)` IS the change history; resolution (`resolveCreditPrice`, `packages/core`) = newest `effectiveFrom ≤ now`, agency override beats platform default; every change audited. | The operator needs the numbers to run the business, and the "enforce credits after one reconciled month" decision had no reconciliation view to reconcile against; FR-BILL-02's price editor had no home. Additive-only (`ProviderInvoice`); no send-path/enforcement change (that is FR-BILL-04, deferred). | moderate (additive model + backoffice endpoints/UI; on-demand rollups revert cleanly; no destructive migration, no enforcement wired) |
| DEC-079 | 2026-07-15 | **B1 W1 — Platform backoffice: shell + staff auth + audit + tenant management (FR-ADMIN-01). The internal operator surface is a SEPARATE trust boundary — it does NOT ride tenant RBAC/RLS; cross-tenant read is its deliberate, scoped, audited purpose.** (0) **Scope — wave W1 of the B1 unit** (W2 usage/reconciliation/price-editor · W3 telemetry · W4 health/abuse/flags follow as their own PRs; one PR per wave). (1) **Separate auth rail:** platform staff are an owner-managed allow-list (`PlatformStaff`), distinct from tenant `User`s, on their own cookie (`cf_staff_session`) + token audience (`clientforce-backoffice`); a tenant dev-JWT (audience `clientforce`) fails the backoffice guard outright (proven), and login mints a token only for an ACTIVE allow-list row. (2) **RLS-exempt DB role, backoffice-only:** a dedicated `clientforce_backoffice` role with `BYPASSRLS` (new factory `createBackofficePrismaClient`; `BACKOFFICE_DATABASE_URL` → falls back to `DATABASE_URL` so CI/local need no wiring); the RLS-subject `clientforce_app` role is REVOKEd from the backoffice tables, and tenant paths stay on `withTenant` untouched (regression pinned). (3) **Every mutation is audited:** one append-only `BackofficeAuditLog` row (operator, action, target, reason) in the same tx. (4) **Suspend/reactivate = typed `TenantStatus` flip;** a SUSPENDED workspace/agency refuses every send at the existing boundary via a new `TENANT_SUSPENDED` `SendBlockReason` (email + SMS), disposition PAUSED (reversible) — reason enum extended, send path NOT forked. (5) **Manual credit grant = one append-only `CreditLedger` row + moved `Workspace.creditBalance`, atomically** — the ledger write path, previously absent, is defined here. | Two real gaps: the platform operator had no console (every prior admin surface is tenant-side) and credit-price/reconciliation had no home; and the access model needed a genuinely separate, audited trust boundary rather than an overloaded tenant role. Additive-only schema (2 tables + 1 role), no planner/prompt changes, no send-path fork. | moderate (additive migration + new API module + new `/backoffice` web surface; the send-boundary gate and RLS-exempt role revert cleanly; no destructive migration, no prompt/planner change) |
| DEC-077 | 2026-07-15 | **SC — Branch creation: "Add a sub-campaign" on R1's trigger vocabulary (#90; PRs #91 W0 · #92 W1 · #94 W2 · #96 W3). One graph, one authority — branch creation is a graph MUTATION through the three-layer gate at the reply-branch-count extension point; the ENTRY TRIGGER is R1's, living on the rule row, never in the graph.** (1) **Shape:** a sub-campaign = a `SubcampaignNode`-headed chain (`ref` carries the owner's branch name — the `enter_subcampaign` Logs receipt) terminated by its OWN end node, disjoint from the main path (entered only by its rule: never the graph entry, no inbound edge, never a case goto), plus ONE `CampaignRule` row whose terminal `move_to_node` targets the container — created ATOMICALLY (one tenant tx; a 422/409 persists neither row; endpoint-through poisoned-tx pin). (2) **Gate extension (the deliberate one):** the reply-branch-count rule is unchanged; `EditContext` gains `subcampaigns: "preserve"|"admit-new"` (only the creator admits — a raw edit can't smuggle a triggerless container) and `ruleTargetNodeIds` (no edit removes a node an enabled rule moves contacts to — the orphaned-trigger guard); container well-formedness holds on EVERY write RELATIVE to the stored version (regressions refuse loudly; guarantees the stored shape already lacked never brick unrelated edits; the unreadable-row previous=null tolerance is preserved); stored containers survive every edit — DELETION is a later unit's decision. (3) **Regenerate preserves branches:** `graftSubcampaigns` carries stored containers onto freshly planned graphs (container ids verbatim — they are rule targets; COLLIDING fresh ids rename, entry/edges/gotos following); an impossible graft fails the plan loudly, never silently orphans; a rule orphaned by any other means walks the typed no-op (TARGET_NODE_MISSING pinned at the engine seam AND the evaluator — error row, nothing moved, graph continuation stays open). (4) **Vocabulary verbatim:** `campaignRuleTriggerSchema` consumed at every layer (7 kinds; `apps/web/lib/triggers.ts` is display-only, the `lib/intents.ts` pattern); duplicate = an EQUAL trigger on an enabled move-to-sub-campaign rule (intents compare as sets; condition-refined rows count — the conservative default); overlapping triggers coexist (row order arbitrates); **Q-022** (the canon's "Reply received"/"Reply contains keyword" aren't creator-expressible — keyword is R1's CONDITION refinement) and **Q-023** (the dashboard "✦ AI" chip awaits persisted provenance) recorded, not built. (5) **UI (W2):** ONE shared `SubcampaignCreator` in both hosts (host deltas ride props — the StepEditorDrawer rule); honest absence for kinds whose event source isn't connected; "Let AI draft it" = deterministic trigger+goal briefs through the REAL sandbox composer, every drafted value ✦-marked until edited/confirmed, honest fallback on refusal/failure; "✦ Suggest more branches" = deterministic goal-seeds labeled as such, accepted suggestions ride the same gate; the DEC-076 notice renders in the creator on launched agents; §8 set `docs/fidelity/sc-w2/` (24 shots). (6) **Proof:** W0 baseline run 29369417962 (DEC-076's deferred Temporal receipt PROVEN live, 19/19 gates); W1/W2 suites (core 178 · planner 81 · web 36 · api e2e 14/14 vs real Postgres+RLS incl. the endpoint-through atomicity and orphan-guard walks; 39-agent adversarial review round, every confirmed finding fixed w/ pins); **W3 run 29401312663** — the REAL R1 engine routed a durable enrollment INTO the created branch (terminal move: cancel + restart at the container; the in-graph reply signal SUPPRESSED — interlock live), the GUIDED step inside the branch composed (real Sonnet-class receipt) → deterministic checks → the unchanged boundary → CAN-SPAM footer EXACTLY once → sent (sandbox); the mid-sequence contact finished on its enrolled v1; refusals loud (dup · count-rule · shared-chain) with the repair path reported, zero rows persisted. **Deferred:** deletion/merge UX; dup-check placement moves to the rules layer with R1's CRUD unit; `rebuildContainer` head-ref generalization; the ✦-AI-draft staging visuals (the honest fallback shot stands in until then); the Agent-cascade data note. Reversibility: moderate (additive gate modes + deterministic grafting; shared components; no migrations, no prompt changes, no new events). |
| DEC-076 | 2026-07-14 | **W3-4 — Agent-view sequence editor (PR #88): the sequence becomes authorable in the agent view; guided mode fully operable end-to-end. One graph, one authority — the editor is a graph-MUTATION surface, never a second planner; no planner prompt changes (v5/v7/v8/v9 + composer v1s untouched, UI hardcodes no version).** (1) **Graph versioning (owner-locked 2026-07-14):** in-flight enrollments finish on the version they enrolled under — guaranteed mechanically by Temporal input pinning (the graph rides `CampaignWorkflowInput`; the workflow never re-fetches) — and edits/regenerate apply to NEW enrollments and rule moves; `Enrollment.meta.graphVersion` stamps the enrolled version at create AND on R1 moves (49-3 meta precedent, no migration); every launched-agent edit surface renders the honest sentence ("Changes apply to new contacts and upcoming steps — contacts mid-sequence finish on the current version"); draft agents edit free of ceremony. (2) **Edit gate:** every editor write passes the same three-layer discipline as generation — shape zod → `validateGraph` → `validateEditedGraph` (planner pkg), the policy layer applied RELATIVE to the stored version so guarantees never regress (playbook case coverage + stage pins + routes-to-step, reply-branch count, delay presence; NEW case intents bounded by `IntentSchema`; NEW channels bounded by sender capability with stored channels grandfathered; scripted email/sms steps need body copy w/ stored-empty tolerance) — the generation-only copy rails (merge tokens · neverSay · language) deliberately stay OFF owner-typed words (the documented M1a stance in `validateAll`, restated not reversed); the deterministic `repairGraph` pass (edge dedupe/dangling/extra-out drops + brief chip trims, each repair REPORTED in the PUT response) is the manual analogue of the bounded LLM repair, which stays generation-only — owner content is never silently rewritten. `PUT /planner/graph` upgraded from `validateGraph`-only; the wizard's writes ride the same gate via the shared core mutations (and gained the missing error handling — no more stuck busy state on a 422). (3) **Mutation layer** (`core/graph/mutate.ts` + `walk.ts`): add/remove/move/content/brief/mode/delay helpers preserve `validateGraph` invariants by construction; node ids are STABLE across edits (send idempotency `(enrollmentId, stepNodeId)` · `CampaignRule.targetNodeId` refs · per-step stats) with first-free `step-added-N` fresh ids; `branchChains`/`strategyChains` make multi-step chains within a branch first-class (stop at end/branch/main-path/cycle; shared tails surfaced); a removed step takes its gap delay (never the graph's last one); removing a strategy chain's only step is refused (the playbook contract); legacy graphs round-trip byte-identical (pinned). (4) **Per-step mode (W2, owner-locked provenance 2026-07-14):** the drawer's Scripted \| ✦ Guided control flips ONE node — the agent-level composeMode rider stays the PLANNING default (G3 semantics untouched; mixed-mode executes, G2 proved it); scripted→guided seeds the brief DETERMINISTICALLY from the step's own copy + its M1a arc role (`deriveBriefSeed` — mechanical and labeled, never fake AI; thin bodies seed INCOMPLETE and the min-3 floor holds), then the ONE-STEP COMPOSE runs the staged seed through the real sandbox composer (compose-preview accepts an optional staged `brief` — samples now compose what the owner SEES); every AI-seeded/picked value renders ✦-marked until edited or confirmed (marks derive by comparison to the seed snapshot); guided→scripted requires body copy — one sandbox compose drafts it ✦-marked or the owner authors it, save blocks honestly; an un-flip restores saved values (undo, never re-derivation). (5) **W3:** the reply-strategy group is chain-true (edit/reorder/delete/add scoped to the case container; strategy steps stay scripted per DEC-070(7) — no mode control off the main path); NEW-branch authoring stays GATED on R1 (owner lock: later wave) — R1's ENGINE is on main (PR #86, newer than the kickoff's slot check) but its trigger-vocabulary UI waves are not; the canon card renders the honest disclosure, no parallel vocabulary, `lib/intents.ts` labels verbatim. (6) **Canon gaps flagged as designed additions:** reorder (↑↓), delete (drawer-footer red, sub-campaign-drawer anatomy), the per-step mode control (G3 segmented anatomy), ✦ Regenerate on the Steps tab (Create Agent extension) + the Settings deep-link (closes W3-12); the Steps tab keeps the G3 single-list composition (the canon Branches sub-tab arrives with R1's rules UI). Add-step seeds sendable copy instead of the canon's empty body — the gate refuses unsendable scripted steps (honesty over literal). (7) **Proof:** `sequence-editor.e2e.spec.ts` (committed, DB-gated) 8/8 vs real Postgres+RLS — contact A's pinned input stays v1 (+ meta audit) while the edit lands v2, contact B starts on v2 and the executor walks the added+flipped steps, playbook-regression edits 422 with the precise reason persisting nothing, repairs reported; full api suite 84/84 vs real DB; 45 new package tests (walk/mutate/repair/seed/edit-gate incl. multi-branch + legacy byte-identity pins). Temporal's ephemeral server was unavailable in the session env (download 403) — the durable walk is the standing workflows integration coverage + G1/G2 live proofs; this unit changed ZERO send-path code. Reversibility: cheap→moderate (additive helpers + gate; shared components; no migrations, no new events, no prompt changes). |
| DEC-075 | 2026-07-12 | **G3 — Guided-mode visibility (PR #87): the headline capability becomes reachable, as thin wiring — three waves, no planner changes, no schema changes, no new events.** (0) **One-PR deviation flagged:** the dispatch says one PR per wave; this session pins ONE designated branch, so the waves land as three scoped commits on one PR (wave boundaries preserved for review/revert). (1) **Wave 1 — the entry point:** wizard step-2 toolbar gains the canon `Scripted \| ✦ Guided` segmented control (prototype literals; toolbar recomposed to the canon gap-10/mb-14 row); flipping PATCHes the full A8 payload + explicit `composeMode` onto the DRAFT immediately — the SAME rider the Settings toggle owns, one field, one semantics (mode applies at the NEXT plan; steps keep their baked mode): a planned-vs-selected mismatch renders the banner line "These steps were planned as scripted — hit ✦ Regenerate to apply guided composing." and relabels the EXISTING Regenerate button "✦ Regenerate to apply" (both flip directions; the banner itself is guided-only per canon). The DEC-072 anti-clobber extends to `composeMode` (a payload that OMITS it preserves the stored rider — the step-5 rebuild + limits modal can no longer reset a mid-wizard flip; the two mode controls always SEND it explicitly, so flipping back writes `"scripted"`, never a silent key drop); `GET /agents/:id/draft` resolves `composeMode` server-side (absent/unparsable = scripted) so resume hydrates the control; the duplicated guardrails assembly in `next()`/`saveLimits()` folds into ONE `guardrailsPayload()` (its composeMode/language omission is the documented contract). Kickoff verifications logged: guided cards key off the step node's own `mode` (G1/G2 ✓); step-6 estimate already prices per node mode (W3-3 ✓); **planner literals — English agents render v5 (scripted) / v7 (guided); v8/v9 are the L1 NON-ENGLISH variants** (the dispatch's parenthetical corrected; UI hardcodes no version). **Banner copy deviation:** the canon's "WhatsApp templates and voice scripts stay as written." sentence is dropped — the build plans email + SMS only this phase (honest absence over prototype literal). (2) **Wave 2 — Steps tab:** guided cards (already canon-treated) become clickable ("View brief ›") and open the brief READ-ONLY in the wizard drawer's anatomy — display values, never inputs; the sample-compose panel calls the REAL `POST /planner/compose-preview` (free, Q-020; refusal/unconfigured stay the designed states); the sends line is COUNT-ONLY ("N messages sent from this brief", F1-rollup/perStep sourced) because **no step-scoped sent-message surface exists** (flagged per the unit; a per-step message list belongs to W3-4's step detail); the footer affordance is honest per status — DRAFT renders a REAL "✎ Edit in campaign setup" link (B6 wizard resume), launched agents get the read-only note naming the W3-4 sequence editor, never a dead button. Scripted steps unchanged; mixed sequences render mixed. (3) **Wave 3 — inbox:** the inbox serializer surfaces the send boundary's pass-through compose meta (G1/G2 `Message.meta` `{mode, composerVersion}`) as an additive `composed` key on OUTBOUND rows with `mode:"guided"`; the reading pane marks them "✦ Composed" + the mode line "composed from brief · checked against your rails"; absent provenance renders unmarked — no inference. **Documented default:** the reply-draft surface is NOT on main (DEC-070(7) — the composer is visual-only), so the marking lands on the composed MESSAGES the inbox actually shows and extends verbatim to draft cards when that wave lands. (4) **Prototype canon:** the repo's `Create Agent.dc.html` predated the guided surfaces — replaced with the owner-attached 2026-07-11 revision (repo-wins / fix-forks-by-PR) with the `__cf_email__`/`cdn-cgi` artifacts scrubbed to the literal (C2.3 rule). Carried-over G1/G2 drawer deltas noted for W3-4, not changed here: credits chip lives in the explainer note vs the canon's Objective-row placement; sample lead stays "Jane Doe · Acme Dental" (DEC-070(6)) vs the canon's "Maya Torres · Hilltop Smiles"; the built guided card renders objective-as-title + bullets vs the canon's one-line "Objective:" preview (accepted in G1/G2 §8). Acceptance: build/lint green; api e2e 76/76 vs real PG incl. the two new specs (composeMode anti-clobber walk; inbox provenance marks ONLY guided-meta rows); §8 set `docs/fidelity/g3/` (12 shots, disclosures in the README) | A merged, live-proven capability the product never surfaces is indistinguishable from an unbuilt one — the fix is pure legibility: one control writing the field that already exists, read-only views of data that already exists, and provenance the boundary already records | cheap (UI + one additive serializer key + the anti-clobber twin; no migration, no planner change, revert is three commits) |
| DEC-074 | 2026-07-11 | **R1 — Per-agent automation rules, wave W1: the engine (PR #86; renumbered from 073 — W3 (#85) claimed it on main first, parallel-track rule; documented defaults flagged on the plan comment).** Restores the campaign-BOOKKEEPING layer the M1b Branches tab dropped (owner verdict 2026-07-11: partial implementation, prototype canon) — planner owns strategy, rules own bookkeeping, always. (1) **D1 — shared core, distinct entities:** ONE typed trigger/condition/action vocabulary (`@clientforce/core` `campaign-rules.ts`) + ONE evaluator (`packages/automations`, mounted on the T2 no-op automations consumer — consumer #2 now REAL, the DEC-035 injected-deps pattern keeps it Temporal-free); Phase 6's standalone Automations consume the SAME core (the DEC-034 one-enum rule applied to rules). But `CampaignRule`/`CampaignRuleRun` are DISTINCT additive models (row order, goal-seed provenance, wizard-draft lifecycle, graph interlock), NOT `Automation` rows with a nullable campaignId — `Automation`/`AutomationRun` byte-untouched. (2) **Triggers map to EXISTING bus events only** (kickoff-verified): `reply_classified` ⇒ `*.replied.v1` payload intent (there is NO reply.classified kind; intents validated against `IntentSchema` at the API boundary — core keeps them opaque strings, its dependency-light precedent, never a second enum) · `meeting_booked` ⇒ `call.booked.v1` OR stage move to `booked` (A10) · `opted_out` ⇒ `lead.unsubscribed.v1`/`sms.opted_out.v1` · `email_opened`/`link_clicked` ⇒ the SendGrid webhook events (**verified live producers — engagement triggers in scope** per the unit's kickoff clause) · `lead_captured` ⇒ form/widget/linkedin captures · `sequence_quiet` ⇒ the worker sweep (below). Keyword-contains is a REFINEMENT condition on reply triggers only, never the primary. Excluded (Phase 6): whatsapp button, bounce/spam, proposal/payment/list/schedule triggers. (3) **Actions** (all idempotent): terminal = `move_to_node · end_enrollment · pause_enrollment · suppress_contact` (each also CANCELS the durable run — skipping the signal alone is insufficient, the branch default would continue after its 72h timeout; pause writes the `meta.blocked` amber row; suppress = create-if-absent MANUAL rows `source campaign-rule:<id>` on email+sms + optOut + UNSUBSCRIBED, publishing `lead.unsubscribed.v1` ONLY on created rows — the cascade terminator); non-terminal = `set_stage` (publishes `lead.stage_changed.v1` only on an ACTUAL change — the set_stage("booked")→meeting_booked loop terminator) · `notify_team` (documented default: the run row + Logs event ARE the Phase-1 surface; no notification transport exists) · `add_tag` (set-union) · `run_automation` (resolved LIVE per B6 — missing/DISABLED = typed `error` run row, never silent; executes the automation's actions through the SAME union at depth+1 with `AutomationRun` rows). (4) **Deterministic precedence:** rails (upstream, untouched) → rules in ROW ORDER → graph continuation; bus fan-out is parallel, so the order is achieved by ONE memoized evaluation per event id + an additive `gate` param on `createTemporalSignalConsumer` — terminal fired ⇒ signal skipped; gate failure fails OPEN (a rules bug degrades to pre-R1 behavior, loudly logged, never breaks reply handling); a fresh process re-answers from the persisted rows. First terminal wins rule-vs-rule conflicts; later terminals no-op with `skipped_conflict` rows while their non-terminal actions still run. (5) **Depth guard:** causation depth on every run; > 2 ⇒ typed `refused_depth` rows (rule AND AutomationRun level — a self-referencing automation terminates in 3 rows, fixture-pinned; the G2 bounded→typed-refusal pattern). (6) **Idempotency — documented default:** runs unique on **(ruleId, eventId)**, deliberately stronger than the unit prompt's (ruleId, enrollmentId, eventId) — the envelope carries at most one enrollment and a nullable column inside a Postgres composite unique would let null-enrollment events double-fire; executors converge on redelivery, the row dedupes the record. (7) **Inertness:** rules fire only for ACTIVE/DONE enrollments — PAUSED per the unit prompt, UNSUBSCRIBED/BOUNCED as documented defaults; DONE stays fireable (late replies + the sweep's re-engagement path). (8) **Timer trigger = the deterministic sweep**, NOT Temporal surgery: hourly worker poll (the stranded-source-sweep pattern; owner-client discovery, tenant-client writes) with fire-once semantics IN THE RUN KEY (`quiet:<enrollmentId>`) — a (rule, enrollment) pair fires at most once, ever; cadence only bounds latency. (9) **Move mechanics:** additive optional `startNodeId` on `CampaignWorkflowInput` (in-flight runs replay unchanged) + `moveEnrollmentToNode` (live-graph re-validation + target check — `TARGET_NODE_MISSING` typed; cancel stored run; deterministic restart id `enroll-<id>-m<eventId>` so redelivery dedupes on AlreadyStarted; enrollment re-points ACTIVE at the target; already-sent steps safe via the (enrollmentId, stepNodeId) send idempotency); reply signals now resolve the STORED `Enrollment.workflowId` (owner-client lookup, the P1.7 thread-resolution precedent). (10) **ONE new emission `automation.rule.run.v1`** `{ruleId, runId, status, trigger, detail?}` — named once HERE (A9 ossification), the CampaignRuleRun row's Logs twin, emitted per run row, publish failure never blocks the row (the compose_refused precedent). (11) **Phase-6 reconciliation noted now so it can't fork:** the Automations prototype's "Reply sentiment" condition renders as an INTENT SET over the one classifier — one classifier, never two; planner prompts untouched (W2 seeds are a CODE-SIDE constant table applied post-plan — prompt v10 explicitly NOT minted); classifier/boundary/guardrails packages consumers-only. Fixtures green vs real PG: precedence/gate, conflicts, depth, idempotency, honest-absence, inertness, condition, suppress-cascade, stage-loop, sweep×2, disabled rules, no-campaign events + gate unit tests + move suite + `startNodeId` walker fixture (time-skipping) | The prototype's Branches tab promises campaign bookkeeping the build silently dropped — restoring it as a typed, bus-native shared core makes every rule outcome a checkable row (never a silent skip, never an unbounded loop) and hands Phase 6 its engine for free | moderate (additive migration + append-only catalog entry; the consumer swap and signal gate revert to the no-op stub cheaply) |
| DEC-073 | 2026-07-11 | **W3 — wizard fidelity round, steps 3/4/6 (PR #85; canon = `Create Agent.dc.html` 2026-07-11; companion R1 is a separate unit).** (0) **Commit-0 extraction (W3 owns it):** `Wizard.tsx` (2,953 lines) → orchestrator + per-step components (`steps/Step1Setup…Step6Review`, `BuildingScreen`, `shared.tsx`) — pure move, all state/actions stay in the orchestrator, step-owned overlays move with their steps (each only opens from inside its step; fixed-position renders identically); R1's UI wave rebases onto this commit, never two open PRs restructuring the wizard. (1) **W3-1 — kickoff premise corrected, flagged:** the C2.5 import flow lived INLINE in `ContactsView.tsx` (~500 lines), not `packages/ui`; reuse-wholesale = extracted VERBATIM to `apps/web/components/ContactImportFlow.tsx` (it is `cf`-coupled — chunked transactional POSTs — so the shared home is app-level; `packages/ui` stays presentational) and BOTH surfaces mount the one component (Contacts pixel-identical; DEC-058 close-mid-run preserved by keeping the component mounted while closed). Wizard-mount extras (props — absent on the Contacts mount): with no list picked the run lands in a fresh list named after the file (created at import start via the existing `POST /lists`, numbered on 409 collisions; the review select's default reads `＋ New list "<file>"` and "no list" isn't offered — the audience must be referenceable); `onImported` hands back the listId and the wizard re-resolves `{name, count}` from `GET /lists`. **Audience by reference (B6):** new additive `DraftState.csvListId` resumes exactly like `pickedListId`; launch resolves the list's members NOW through the same origins path (kind `csv` + listId/listName provenance) — members added to the list after the import still enroll. Kept over the wizard proto where reuse conflicts (both logged): the C2.5 540px modal (proto draws 560) and the C2.5 server-count done modal (the proto's "They're enrolled in this campaign" is false before launch). Kickoff flag resolved: NO new endpoint was needed. (2) **W3-7 — audience preview:** header + "N contacts ready" pill + up to 4 member rows + "+ N more" footer per the proto; rows are 34px INITIALS avatars over the proto's four tints (the proto's 🦷 glyphs are sample-data emoji; W3-7 itself specifies avatar-initial rows, matching the drawer + Contacts); honest empty body before any source (designed — the proto has no empty state); the pill count is the SAME arithmetic launch enrolls (per-source real counts summed — adds + referenced-list counts; cross-source overlap dedupes at enroll time, never in display math); samples fetch via the existing `contacts/view?listId=` (first rows, display-only). `added[]` entries gain lastName/company (additive) so manual rows render name · email · company. Step-3 source cards now wear the proto's OWN literals (they wore step-1 goal-card values): radius 14 · padding 18 · 40px/19px icon · grid mb 20 · 0 2px 8px shadow · 820px wrapper. (3) **W3-9/W3-10 — step 4 rebuilt to the proto anatomy** (the invented "Website chat widget / Form capture" pair deleted): master card, AP card (keyword chips + suggestions, the proto's literal param option lists, signal pills), the three inbound assets, dark preview panel, verbatim footnote. Honesty rails, each a designed state: asset pickers render "No saved … yet" + a ＋ Create-new path to the `/forms` · `/widget` nav stubs (no Forms/Widget subsystem exists — never fake assets; with none selectable the proto's widget/embed/form preview panels stay unreachable and are NOT built as dead code); the AP dark panel drops the proto's mock found-leads rows + "~12 new matches / day" line for a matches-appear-after-launch body and reads "· on" not "· live" (nothing is live before launch); keyword suggestions DERIVE from the agent's own distilled/typed context fields (icp/services/offer — real data) plus a flagged type-your-own row (the proto's chips are pre-seeded sample state with no input affordance); keywords start EMPTY; master-off dims + disables everything below (the proto draws only on). **Goal-fit (owner-approved direction):** `apps/web/lib/goal-fit.ts` is the ONE constants module (goal→fit + AP notes + automation templates), keyed off the C2.9 `GOAL_KEYS` — R1's goal-seeded rules key off the same registry, never a parallel enum. Existing-audience goals (`reactivate_leads · upsell_clients · collect_reviews`): step-3 Upload CSV + Choose a list get the green "FOR THIS GOAL" tag + soft `#9FD8AC` border (tag yields to the picked treatment); step-4 AP defaults OFF (`capture.ap: null` = no explicit choice → fit default; the user's toggle always overrides and persists), badge flips to "Not typical for this goal" (designed neutral tint), in-card note = goal icon + title + one line; config collapses whenever AP is off (any goal). **Suggested automations** strip below Inbound capture (designed): two dashed cards from the same constants (existing-audience → "Sync lapsed patients from your CRM" + "Re-enroll a list on a schedule"; prospecting → "New form fill → enroll here" + "Missed call → instant follow-up" — kickoff copy verbatim), CTAs deep-link to `/integrations` · `/automations`; STATIC templates, zero connector calls, no fake connected states. `DraftState.capture` extends additively (enabled/embed/ap/apKeywords/apParams/apSignals, all optional — pre-W3 `{widget, form}` drafts parse unchanged). (4) **W3-3 — step 6 at the proto's anatomy** (4-up tiles at the 12.5/600 + 16/700 scale · Lead capture card · Guardrails card · Estimated cost card · launch strip): the estimate is **COMPUTED** — per-step per-send rate × the REAL audience count; scripted steps price by channel from the NEW display-only core `CREDIT_PRICES` (mirrors the db seed's `DEFAULT_CREDIT_PRICES`; the editable `CreditPrice` table stays the future runtime source; nothing is emitted or charged — Q-020), guided steps keep their `GUIDED_*_CREDITS` display figures (the numbers their step cards already show — never a third invented price); a voice line renders only when voice steps exist (none can this phase); AP + enrichment lines render only while AP is on, as PER-LEAD rates (`signal_lead` 15 / `enrichment` 10) — prospecting volume is unknowable pre-launch, never a fake total; inbound reads "Included" (unmetered); "Total to launch" sums the computable lines. The proto's "Workspace balance: 6,200 credits" sentence is replaced by the build's readiness line riding the strip (kept per the kickoff, both states: green ready + amber unresolved w/ disabled Deploy); the strip's 🚀 Deploy is a second gradient CTA beside the rail's — the proto draws both, composition wins over the §0 one-CTA convention (flagged). Sending-window/daily-limit rows compute from the A8 state (caps sum only over channels the sequence uses). (5) **Explicitly NOT in this round:** W3-4 branch semantics · W3-11 rules (R1) · W3-2/5/6 campaign-view items; no planner changes, no DB schema changes, no event emissions | The wizard's three weakest screens now match the canon while every claim on them stays checkable — reuse over forks, references over copies, computed figures over hardcodes, and honest absences where subsystems don't exist yet | cheap–moderate (UI + additive zod keys + one display constant; the import extraction is shared-code refactor with both mounts pinned by the same component) |
| DEC-072 | 2026-07-11 | **L1 — Agent output language (PR #83): agents write and understand the customer's language; the app UI stays English.** (1) **Rider:** `language` (launch-list enum) + `languageSource` (`"detected" \| "owner"`) ride guardrails Json (goalLabel/strategy/composeMode precedent — NO migration; absent = English, legacy rows parse unchanged; PRESENT-yet-invalid throws, A8 discipline). Launch list is a CURATED constant set — **EN·ES·FR·DE·IT·PT·NL·PL** (proposed on the PR #83 plan comment; **RATIFIED by the reviewer verdict 2026-07-11, approved as proposed**); adding a language is one PR touching the list + its compliance strings, forced by the completeness test. (2) **Detection — NO wizard field:** the AGENT-layer distill detects the dominant language of its merged evidence pack (website chunks, uploaded-document chunks, typed answers — source-agnostic: a doc-only agent detects from its documents) with a DETERMINISTIC in-code detector (stopword + diacritic scoring, thresholds as named constants — no model in the loop, so fixtures pin it exactly), confidence-gated: mixed/ambiguous/thin corpus → English default. Write rules: `languageSource:"owner"` is NEVER touched; a confident detection writes `{language, languageSource:"detected"}` (the detector may overwrite its own earlier call — wizard sources arrive one at a time, so detection must converge); a NOT-confident pack CLEARS a previous detector write (German site then English doc ends English, same as uploading both at once). A legacy empty-guardrails row materializes the conservative defaults alongside a detection — behavior-identical to the parse-time fallback it replaces. The workspace/Brand-kit layer never detects (no agent to write to): a workspace-sources-only agent stays English default, the Settings row is the lever (documented default). (3) **PATCH anti-clobber:** `/agents/:id` preserves the row's language keys when the incoming guardrails OMIT them — the wizard's step-5 rebuild and any stale-read compose can't erase a mid-wizard detection; a payload that SENDS language writes it as given (the Settings row sends source `owner` — sticky). (4) **Generation in-language — versioned prompt bumps, never string-replace (numbers per the #83 reviewer rebase deltas — G2 merged first and took DEC-071 + planner v7):** `planner.campaign@v8` = the v5 literal + an OUTPUT LANGUAGE section before GUARDRAILS (ALL human-visible copy — main sequence, reply-strategy steps, briefs incl. subjectHint; machine identifiers/`intent`/`pipeline` values stay English; merge tokens stay literal; never write opt-out lines — the boundary appends them in-language itself); `@v9` = **G2's v7 literal** (both-channel guided — NOT G1's v6, so non-English guided agents keep both-channel semantics) + the same seam insert, the seam asserted against BOTH literals at registration so none of the registered variants can drift. `context.distill@v2` = v1 + the rawSummary-language rule (the wizard's About card reads it). `composer.sms@v2` = v1 + the compose-in-language constraint; the first-touch note quotes the ACTUAL in-language opt-out line the boundary appends. **`composer.email@v2`** (reviewer delta 3 — G2 introduced composer.email@v1 under this unit): the same treatment mirrored — subject AND body in the agent's language; without it a German GUIDED agent would compose English email bodies over a German footer. Both `Message.meta.composerVersion` stamps go `@v2` for non-English (honest provenance); both composers read the language from the ONE shared `strategyOf` rider read in compose-shared. Version-SELECT by agent language: **English agents render planner v5/v7, distill v1, and both composer v1s BYTE-IDENTICAL** — regression-pinned by test (fixtures re-proven against the renumbered/layered prompts, the F1-rebase precedent). (5) **Deterministic language rail** (the M1a double-rail pattern — the prompt asks, the gate PROVES): for non-English agents `validateAll` runs the same detector over the generated copy (subjects + bodies + brief objective/talkingPoints/subjectHint); a CONFIDENT mismatch fails → the existing bounded repair → typed `PlannerError`, nothing persisted. Confidence-gated on purpose (quoted English proof points inside German copy never trip it); English agents skip the check entirely. (6) **Understanding — NO classifier code change:** `MULTILINGUAL_REPLY_FIXTURES` (German→`interested`, French→`objection_price`) pinned SEPARATELY from the one-per-label M1b matrix (which is length-pinned); CI proves German/French replies classify to their pins and a GERMAN graph executes identically on Temporal (German copy sent → intent routes the branch → stage `replied`→`booked`); the multilingual matrix is appended to `inbound-live-proof` (REAL-model gate on the next dispatch). (7) **Compliance strings stay DETERMINISTIC:** `COMPLIANCE_STRINGS` in core — per launch language, the email footer's unsubscribe label + the full SMS opt-out line, pre-translated constants rendered at the send boundaries by the agent's parsed guardrails, never AI-generated at send. `en` entries are byte-equal to the pre-L1 literals (English sends wire-identical, pinned); **every translation keeps the literal keyword STOP** — Twilio's opt-out rail is English-keyword-scoped (DEC-067's lesson), so a localized line must still name the keyword that actually triggers it. Vitest pins completeness: every launch language × every compliance string, non-empty, STOP present. (8) **Settings → Language row** (designed section, no prototype anchor — DEC-065(6) waiver): launch-list select (native + English names), provenance note (✦ Detected from this agent's content / Set by you / default), the in-language compliance-lines preview (the §8 footer-preview surface), and the applies-to-future-regens/sends footnote (pinned-graph rule; compliance lines switch at send time — flipping before a regen may briefly pair old-language copy with a new-language footer, the documented cost of the owner's explicit action, called out in the copy). (9) **Documented defaults / explicitly NOT built:** UI i18n · per-CONTACT language override (**Q-021** — implies per-send generation) · intl SMS rates/credit multipliers · WhatsApp template localization · voice languages · the in-flight reply-draft wave must read the SAME rider when it lands (composer v2 + the rider plumbing are built for it to reuse) | The loop was already nearly language-agnostic — making it official costs prompt versions and a constants map, not a migration; deterministic detection + deterministic compliance strings keep every language-varying byte pinnable by tests | cheap (no migration; prompts append-only; constants map additive) |
| DEC-071 | 2026-07-11 | **G2 — Guided mode, email (PR #82; post-G1/#81; parallel-track rule applies — 071 claimed after G1's 070).** Owner brief locked: the composer learns subjects and structure; **the footer stays the boundary's job, forever**; guided email = 2 credits (display-only, Q-020 amended); NOT built: voice, approval-queue UI, HTML templates (composed email is text-first like scripted). (1) **Schema across all 3 layers (DEC-029/DEC-070 pattern):** guided becomes legal on channel "email" (email/sms-only overall); `StepBrief` gains `subjectHint ≤120` — EMAIL briefs only (an sms brief carrying one fails layer 2), planner-emitted and owner-editable, a *direction* the composer adapts, never pasted; brief × body/subject exclusivity unchanged; layer-3: the M1a neverSay scan covers `subjectHint`, and the `{{firstName}}`/`{{company}}` token rule re-scopes to scripted MAIN-PATH steps (the same entry→edges→default-case walk every surface uses) — under v7 a guided agent's main sequence is all-guided while reply-strategy steps stay scripted email, and strategy steps never carried the token contract; scripted agents validate exactly as before. (2) **`composer.email@v1`** on the existing `copy` route: same shared context renderer, prompt-cached business context (`cachedContext` block), last-10 history, lead fields, no-invention rules — plus **arc-role awareness**: the workflow passes the step's main-sequence position (`mainStepPosition`, pure walk in workflows/shared — strategy steps resolve role-free), the composer maps it onto the agent's M1a ladder (first = OPENER, last = BREAKUP, middles fold like planned copy) and the role + the playbook's role rules ride the prompt; length literals are the planner's own email rules (body hard cap 140 words, target ≤120; subject cap 60). Shared composer plumbing extracted to `compose-shared.ts`; `compose-sms` re-exports byte-identically. (3) **Deterministic checks = the G1 set on subject+body PLUS the email rails** (pure string ops, send path AND preview): **SUBJECT_RULE** — empty · >60 chars · exclamation · ALL-CAPS · faux "Re:"/"Fwd:" (real threading is the boundary's, owner rule 3) · any `BANNED_SUBJECT_PATTERNS` hit ("quick question" + the BANNED_OPENERS list, case-insensitive); **COMPOSED_FOOTER** — unsubscribe/opt-out/"stop receiving"/"remove me from" language anywhere in composed text (the boundary appends company_address + the one unsubscribe line itself; a composed footer would double it). Same ONE bounded retry → typed refusal; **footer-exactly-once** asserted in tests (composed part carries zero footer language, the persisted body exactly one `Unsubscribe:` line). (4) **Runtime:** guided email composes AFTER the idempotency check, BEFORE the byte-untouched `sendStep` rails, via the `composeEmail` seam (worker wires `createEmailStepComposer` off ONE shared gateway when `ANTHROPIC_API_KEY` is set; absent → typed COMPOSER_UNCONFIGURED); `sendStep` gains the same pass-through `composed` meta as sms — `Message.meta` `{mode, briefVersion, composerVersion}`, scripted meta byte-identical; a refusal pauses THAT lead + **`email.compose_refused.v1`** (append-only catalog twin, deliberately no messageId — DEC-064 honesty); `recordComposeRefused` + the worker publish hook become channel-aware (absent channel = "sms" for in-flight pre-G2 replay compat); `content.threaded` on a guided follow-up keeps the boundary's real-threading semantics (subject inheritance included). (5) **Planner:** prompt **v7** registered append-only, derived from the v5 literal exactly like v6 (which stays registered untouched) — EVERY main-sequence step a guided brief on both channels, email briefs + subjectHint, sms briefs without, reply-strategy steps stay fully scripted email (guided replies remain the reply-draft wave's, DEC-070(7)); the guided SYSTEM addendum widens to both channels (prefix-stable); **gating drops the sms-sender requirement** — `guided = composeMode === "guided"` (email needs no extra sender; the ACTIVE-Twilio check still gates only whether sms steps are plannable, DEC-061); scripted agents render v5 byte-identical (pinned). (6) **API/UI:** compose-preview routes by the step's channel (email → `composeSampleEmail`, subject+body, arc role from the saved graph via the same walk; refusal stays a 200 display state); `GUIDED_EMAIL_CREDITS = 2` beside the sms 3 (display-only); wizard + Steps email brief cards (channel-true chips, per-channel credits, subject-hint line), the 560px drawer's email variant (subject-hint field + email-aware note), preview renders subject+body, Settings compose copy widens to "messages", Logs/Leads render the email amber row (DEC-057 no-raw-slugs). (7) **Acceptance evidence:** CI composer mocked (prompt-driven fakes) — compose-email unit walk, guided-email DB integration (variety/grounding/footer-once/meta/idempotent-replay/subject+footer refusal walks/scripted-regression/**mixed-mode** scripted-step-1 + guided-step-2 on one enrollment), Temporal mixed-mode fixture (position + channel-aware refusal routing; time-skipping env — runs in CI, skips sandboxed); `guided-email-live-proof` workflow_dispatch = the staging e2e (real Sonnet ×2 leads through the full activity path, SendGrid SANDBOX, gates: variety · personalization · grounding · footer-once cage · mixed-mode w/ scripted-meta regression + real threading · trap-brief refusal → pause + Event row through the real bus); §8 set `docs/fidelity/g2/` (8 shots + provenance README — the two sample-preview cards are labeled UI-state renders: the capture env carries no model key; the live proof is the real-compose evidence) | A brief that also carries the subject keeps the WHOLE composed message checkable — subjects get their own deterministic rails, and the compliance cage stays upstream-agnostic because the footer is appended after composition, never written by it | moderate (no migration; new event + prompt + composer versions are append-only; the token-rule rescope affects generation-time validation only) |
| DEC-070 | 2026-07-11 | **G1 — Guided mode, SMS (brain wave 4/5, PR #81; post-#54 + post-M1a; parallel-track rule — renumbered from 068: M1b and F1 claimed 068/069 and merged first; prompt version likewise moved v4→v6 under their v4/v5).** Owner decisions locked: SMS first · agents default Scripted · guided OUTBOUND auto-sends after checks · replies ride the AI-reply-draft surface. (1) **Schema across all 3 validation layers (DEC-029):** `StepNode` gains `mode: "scripted"\|"guided"` (absent = scripted — legacy graphs parse byte-identical) + `brief { objective ≤200, talkingPoints 3–6×200, mustSay ≤5×120, neverSay ≤10×80 }`; layer-2 semantics: guided ⇒ sms-only this unit (email = G2), brief required, brief and body copy mutually exclusive; layer-3 slice gate: guided steps legal only for guided agents (a scripted agent's model emitting them fails → repair), the `{{firstName}}`/`{{company}}` token rule scopes to SCRIPTED copy (an all-guided graph carries none at plan time — the composer personalizes from real lead fields), and the M1a neverSay scan covers brief text too. (2) **Composer `composer.sms@v1`** on the existing `copy` route (Sonnet-class): inputs = brief + the ONE shared context-text renderer (extracted to `@clientforce/context` so planner and composer can never fork) + last-10 conversation history + lead fields (incl. C2.7 custom values) + channel constraints (no subject; target 160, hard cap 300 — the planner's sms literal); same no-invention rules as the planner. **Prompt-cached business context** via an additive gateway seam: `CompleteRequest.cachedContext` → a second system block with `cache_control: ephemeral` (static prompt first, per-agent-stable context second — a lead fan-out reuses the cached prefix); fakes unaffected, no other gateway change. (3) **Deterministic post-compose checks** (pure string ops, send path AND preview): ban lists (agent strategy ∪ brief) · mustSay present · length · merge-token integrity (composed text carries ZERO `{{…}}` syntax — finished copy, so `renderTokens` at the boundary is a no-op) · URL grounding (every URL verbatim in context/brief). Fail → ONE bounded retry carrying the named violations → typed `ComposeRefusedError` (reasons: COMPOSER_UNCONFIGURED · NEVER_SAY_VIOLATION · MUST_SAY_MISSING · TOO_LONG · TOKEN_SYNTAX · UNGROUNDED_URL). (4) **Runtime:** the send activity composes guided sms steps AFTER the idempotency check (replays never re-spend a model call) and BEFORE the byte-untouched `sendSmsStep` rails; a refusal pauses THAT lead (PAUSED + `meta.blocked`, other leads unaffected) + publishes **`sms.compose_refused.v1`** (new catalog entry — deliberately no messageId, nothing was sent; DEC-064 catalog honesty) → the Logs tab's amber row; never a silent skip, never an unchecked send. `Message.meta` gains `{mode, briefVersion, composerVersion}` via a pass-through param — scripted meta byte-identical; `CampaignWorkflowInput` gains optional `graphVersion`. Worker wires `createSmsStepComposer` only when `ANTHROPIC_API_KEY` is set (absent → typed COMPOSER_UNCONFIGURED refusal — the smsTransport honest-absence pattern). (5) **`composeMode` rides guardrails Json** (goalLabel/strategy precedent — no migration, no wizard field; absent = scripted): the planner emits briefs only for guided agents WITH an active Twilio sender (honest absence, DEC-061 pattern) via **prompt v6** (registered append-only beside v2–v5, derived from the v5 literal at registration so the layers can never drift; scripted agents render v5 byte-identical — pinned by test) + a guided SYSTEM addendum (prefix-stable). Settings gains the Message-composing toggle (designed section, no prototype anchor — DEC-065(6) waiver) with applies-to-future-sends copy: each step's mode is baked at plan time, so flipping steers future generations; the tab's compose-over guardrails write preserves the mode. (6) **UI:** guided step card = objective + talking-point bullets + "✦ Composed at send" + "3 credits / send" (display-only, Q-020) on wizard step-2 + Steps tab; the 560px drawer becomes a BRIEF editor for guided steps (objective + bullet rows + mustSay/neverSay chips — bullets, never copy; saves via the existing PUT /planner/graph); **sample preview** = `POST /planner/compose-preview` composing the saved brief against the fixed sample lead (Jane Doe · Acme Dental) through the REAL checks, api-side (the api already carries the key, DEC-027; missing key → designed 503, DEC-047 ladder; a refusal returns 200 `{refused}` — a legitimate outcome to display, not an error). (7) **Documented defaults:** guided REPLIES deferred — the reply-draft surface is not on main yet (in-flight parallel wave); G1 wires outbound only and the composer takes history precisely so that wave reuses it. Carry-alongs: sms.\* rows joined the Logs feed (raw slugs violated DEC-057) and the §8 walk caught + fixed a preview-card flex-collapse | A brief is a contract the platform can CHECK — talking points steer the model while deterministic rails prove compliance per message; the cage stays upstream-agnostic because composition finishes before the boundary | moderate (no migration; new event + prompt versions are append-only) |
| DEC-069 | 2026-07-11 | **F1 outcome feedback v1 (parallel track; PR #78; approved 2026-07-11 with rebase deltas).** Rollup endpoint `GET /agents/:id/outcomes` computed live from the ledger under the tenant client — **sent comes from OUTBOUND `Message` rows** (`email.sent.v1` is catalog-only, nothing emits it; A6 makes Message the send ledger) joined with `Event` engagement rows; **no new event emissions** (A9 names ossify) and no warehouse (owner: pre-launch, BRAIN_V2_PLAN §2 — `MetricDaily` stays unwritten). **Statistical honesty lives in the PAYLOAD**: `SIGNAL_MIN_SENDS = {low: 20, ok: 50}` (boundary-tested), below the floor every rate field is `null` and signal is `none` — no UI can render an unsupported rate. **Attribution constants** (`ATTRIBUTION_RULES`, edge-tested): a reply attributes to the **LAST-SENT step** — the thread pointer the existing threading resolved (`inReplyToId`), falling back to the latest outbound in the same enrollment (else contact) sent at-or-before the reply; goal completion attributes to the **SEQUENCE only**, deduped per enrollment, never a step row. Counting vocabulary (documented defaults flagged on the PR #78 plan — **owner-ACCEPTED 2026-07-11 with the approval**): `POSITIVE_INTENTS = [interested, booked]` · unsubscribe-intent replies are excluded from reply counts (DEC-034 side-effect label; they surface as opt-outs) · rate denominator = sent · replies/opt-outs count **distinct leads** per step and delivered counts **distinct messages** (provider webhooks are at-least-once — a retry can never report delivered > sent) · `delivered: null` for channels without delivery telemetry (sms) — never a fake 0. Additive `Event(workspaceId, campaignId, type)` index (RLS untouched). **Regen is outcome-aware**: `planner.campaign@v5` — M1b's v4 REPLY PLAYBOOK text verbatim + the OBSERVED OUTCOMES section (v2/v3/v4 stay registered) — renders the block ONLY for low+ steps, confidence-labeled, built from the SAME loader the endpoint uses (`loadCampaignOutcomes`) so prompt and payload cite one set of numbers; zero qualifying steps → no section (young campaigns plan exactly as the v4 playbook did); regen stays owner-triggered — **no credit changes** (no credit ledger exists in code; the "10 credits" story is untouched). Step-card badges (wizard step-2 + Steps tab): none → no chip (honest absence) · low → muted "low data" · ok → full green — §0 convention addition (no prototype stat chips; "low data" is designed copy). The Steps tab's stats span rebinds to the rollup — **the pre-F1 `Message` groupBy never attributed replies** (INBOUND rows carry no `stepNodeId`; live replies-per-step was always 0). Explicitly NOT built: automatic regen, threshold automations (Phase 6), dashboards (Phase 10), cross-campaign/vertical learning. Parallel-track collision resolved at rebase (M1b merged first and took DEC-068 + prompt v4): this DEC renumbered 068→069, the outcomes prompt rebased v4→v5 LAYERED on the playbook ({{outcomes}} composes with the v4 text, never replaces it), and the regen fixture re-proven against the layered prompt | The ledger already pays for itself pre-launch: visible per-step truth + outcome-grounded regen with zero ML, and honesty gates in the payload keep thin data from cosplaying as signal | cheap (additive index · append-only prompt registry · presentational badges) |
| DEC-066 | 2026-07-10 | **P3.0 voice spike (Track-C claim; parallel-track rule — renumbered from 064: P2.1's STOP DEC and M1a's playbooks DEC claimed 064/065 and merged first).** Real-time loop proven: Twilio Programmable Voice (number already Voice-capable per the P2.1 inbound probe) → Media Streams WebSocket → sandboxed `apps/voice` bridge → Deepgram Nova-2 streaming STT → Claude via a **new additive `voice` route** in the `packages/ai` gateway (`streamVoice` + `streamText` provider seam + Haiku-class default `AI_MODEL_VOICE` + price row — **no other gateway changes**) → **Deepgram Aura-2 TTS** (chosen over ElevenLabs: native mulaw/8k = zero transcode, one vendor with STT, ~$0.03/1k chars, comparable TTFB). **Barge-in = GO** (caller speech → Twilio `clear` + abort of in-flight LLM+TTS; unit-tested + exercised 4/22 synthetic turns, 0 dropped audio). Persistence taste-test: transcript → `Message(channel:"voice")` INBOUND/OUTBOUND via `withTenant`, **no migration** (DATA_MODEL A6 absorbs voice). **Latency/cost tables in the ADR are synthetic-mechanics + a projected live budget — NOT yet a real call:** the live path was owner-gated on `DEEPGRAM-API-KEY` + a `VOICE-FROM-NUMBER` secret (**both added to Key Vault 2026-07-10**) + answering one demo call; the `voice-live-spike` `workflow_dispatch` becomes runnable once this lands on the default branch (GitHub limitation), then preflight → synthetic (now live-brain) → demo-call. **Scope recommendation for July: "designed + waitlist" with one gated live demo, NOT live calls at GA** — the loop is derisked; the unmeasured live TTFA and the newly-onboarded Deepgram vendor are the remaining risk. Explicitly NOT built (per the spike charter): call tools, Calls-tab UI, graph voice nodes, recording retention, production error handling. Deliverable = `docs/adr/voice-spike.md` | July date lives or dies on voice — the only Phase-3 piece reusing almost nothing from Phase 1; a measured go/no-go is worth as much as a demo | throwaway (spike dir); the `voice` gateway route is additive and durable |
| DEC-067 | 2026-07-10 | **P2.1 rail-1 closure via a platform-owned US test handset (owner decision on #54).** The binding probe proved every keyword rode the platform service + pool number, and four silent STOPs plus a silent START — the FIRST predating any toggle, when Twilio's mandatory default opt-out would have replied if in scope — established that **Twilio's opt-out keyword handling is US/CA-scoped and never engages for the NG (+234) test handset** (DEC-064's rail-1 note is corrected accordingly: not a console mis-toggle). Owner-directed remedy: (1) buy ONE US local number on the platform Twilio account as the STANDING US TEST HANDSET — FriendlyName `clientforce-us-test` (discovery tag: never re-purchased), stored in Key Vault as `SMS-US-TEST-NUMBER`, attached to the platform messaging service so it sends under the approved A2P campaign (pool becomes 2 numbers; proof sends may originate from either). (2) `sms-us-rail1-proof` workflow drives STOP → START → STOP from it via API and captures Twilio's `outbound-reply` confirmations with delivery status (rail-1 closing evidence + Twilio-side idempotency). (3) **Ledger stance (DEC-062/064 wording confirmed): our Suppression is one-way — STOP creates it (create-if-absent, so N STOPs = 1 row) and NOTHING clears it automatically; START resubscribes only at Twilio level. Re-consent is an explicit future product action, never an inbound keyword side-effect.** The proof asserts this against the staging API (NG row present and singular; US handset row absent — no matching contact, per the DEC-064 fail-safe scope). (4) **DEC-063 scope extension:** the allow-list becomes the Key-Vault-backed `SMS-ALLOWLIST` secret (`CHANNELS_SMS_ALLOWLIST` env via secretRef; numbers never live in the public repo) valued NG test number + US handset. **Outcome (2026-07-11, owner call: "close with A2P item"):** rail 2 + the ledger stance are FULLY evidenced (proof run 29131940801: STOP → suppression row via the live staging webhook; START does NOT clear it; repeat STOP stays a single row; the NG row exists too). Rail-1's auto-reply is NOT provable in the account's current state — the status probe (run 29131879047) found the account Full/active but with ZERO A2P registrations (no brand, no campaign; the kickoff premise "A2P approved" did not hold), and all test traffic is Twilio-to-Twilio on-net. A2P brand+campaign registration + rail-1 re-verification move to the go-live runbook as BLOCKING items; the KV writes for `SMS-US-TEST-NUMBER`/`SMS-ALLOWLIST` are owner-manual (pipeline identity is vault-read-only) | Rail 1 is vendor compliance machinery scoped to US/CA handsets — evidence must come from an in-scope device the platform controls; consent ledgers only ever widen without explicit human re-consent | moderate (recurring ~$1/mo number) |
| DEC-001 | 2026-07-02 | `apps/web` stays Next.js 15 + React 19; Vue only in the Phase-4 Chrome extension (tokens, not components)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Phase 0 shipped it; no Phase-1+ argument covers a rewrite tax (handoff A1)                                                            | expensive     |
| DEC-002 | 2026-07-02 | Domain data = NestJS REST + zod DTOs in `packages/core`, one typed client, TanStack Query; Next API routes = auth/session only; no tRPC/GraphQL                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Three docs disagreed; mixed protocols would ossify per screen (A2, re-audit #4)                                                       | moderate      |
| DEC-003 | 2026-07-02 | Dev-JWT auth accepted for Phase 1; provider = Clerk as its own later unit, before any external demo                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Unblocks the slice; onboarding UI waits for the provider (A3)                                                                         | moderate      |
| DEC-004 | 2026-07-02 | Realtime = TanStack Query polling (5s on Inbox/Logs/open lead drawer); no WebSockets this phase                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Cheap, testable, nothing thrown away (A4)                                                                                             | cheap         |
| DEC-005 | 2026-07-02 | 1 agent = 1 goal = 1 auto-created primary campaign ("Primary sequence") + graph v1 (AI); routes `/agents/[agentId]/[tab]`; sub-campaigns deferred                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Owner-confirmed UI mapping; decides routes/queries/wizard writes (A5)                                                                 | expensive     |
| DEC-006 | 2026-07-02 | Add `Message` model; outbound persisted **as rendered** at send (P1.5), inbound + intent persisted (P1.7); Inbox/timeline read `Message`, events carry `messageId` refs only                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Inbox needs a message store; events are a fan-out contract, not storage (A6, re-audit #1)                                             | expensive     |
| DEC-007 | 2026-07-02 | Add `Suppression` model; adapter checks Suppression **and** `Contact.optOut` before every send; unsubscribe writes both                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Compliance-grade opt-out needs a provable ledger (A7, re-audit #2)                                                                    | expensive     |
| DEC-008 | 2026-07-02 | Guardrails = typed zod schema in `packages/core` (sending window w/ days+timezone, per-channel dailyCap, consent attestation, `unsubscribeFooter: true`, `suppressionCheck: true` — literal, not disableable)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Read by planner + adapter + workflow; a bare Json invites drift (A8, re-audit #6)                                                     | moderate      |
| DEC-009 | 2026-07-02 | Event names: version suffix mandatory; canonical P1 set = `email.sent/delivered/opened/clicked/bounced/spam/replied.v1`, `lead.enrolled/stage_changed/unsubscribed.v1`; **`lead.replied` removed** (replies are channel-specific; consumers filter `*.replied.v1`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Names ossify — Phase 6/7 subscribe to them (A9, re-audit #8)                                                                          | expensive     |
| DEC-010 | 2026-07-02 | Contacts segments are **queries**, not stages: All=everything · New=stage `new` · Replied=any `email.replied.v1` · Qualified=stage∈{`interested`} · Booked=stage `booked` · Unsub=`optOut.email` OR `Suppression` row OR enrollment `UNSUBSCRIBED`; keep the "Qualified" chip label                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Prototype chips aren't pipeline stages (A10, re-audit #9)                                                                             | cheap         |
| DEC-011 | 2026-07-02 | Icons = `lucide-react`; prototype glyphs are placeholders; map once in §Icon map below and reuse verbatim                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | No icon system existed for the greenfield app (A11)                                                                                   | cheap         |
| DEC-012 | 2026-07-02 | Fidelity standard = the prototype's literal values; CONSISTENCY_AUDIT "recommended ramps" deferred to a dedicated token pass — no silent normalization                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Makes fidelity checks verifiable (A12, re-audit #19)                                                                                  | cheap         |
| DEC-013 | 2026-07-02 | Sending domain `clientforce.io`, DNS at SiteGround **by the owner**; demo From = `agent@send.clientforce.io`; inbound parse MX on `reply.clientforce.io` only; Reply-To = the `reply.` address; **root-domain mail DNS (MX/SPF on `@`) is production — never add/edit/remove, never instruct to**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Root domain carries active company mailboxes (owner-confirmed, §G)                                                                    | expensive     |
| DEC-014 | 2026-07-02 | Test inbox + send allow-list for this phase = `tronwebng@gmail.com`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Owner-confirmed (§G)                                                                                                                  | cheap         |
| DEC-015 | 2026-07-02 | Ingestion target = `https://clientforce.io`; **ingestion-proof rule:** planner copy must contain ≥2 concrete facts traceable to ingested pages, asserted against stored BusinessContext in P1.2/P1.4 tests                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Proves the sequence came from ingestion, not model priors (§G)                                                                        | cheap         |
| DEC-016 | 2026-07-02 | Auth provider = Clerk (Google + Microsoft social desirable, non-essential); Phase 1 ships on dev-auth                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Owner-confirmed (§G / Q-001)                                                                                                          | moderate      |
| DEC-017 | 2026-07-02 | PR 0 also fixes every `agent_platform_build/` / `docs/…` wrong-path reference in live docs (`PHASE1_ISSUES.md` preamble, `BUILD_PLAN.md` first-tasks list, `EXECUTION_PHASE0.md §C`) — beyond the literal §D list, flagged in the PR                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Canonical-docs rule: a wrong path misdirects future sessions                                                                          | cheap         |
| DEC-018 | 2026-07-02 | `packages/events` still ships `lead.replied.v1` (catalog + sample publisher + tests). Removing it is **product code** → owed by **P1.7** (per handoff §B amendments), not PR 0. All doc references updated/annotated now (incl. T2's acceptance example in `EXECUTION_PHASE0.md`, kept as a historical record with an A9 footnote); code + catalog aligned in P1.7's PR                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | PR 0 is docs-only by definition (§D)                                                                                                  | cheap         |
| DEC-019 | 2026-07-03 | **Warmup trigger rule** (owner-set): ramp/re-warm a sender when any of — new/idle domain · cold audience · >2–3× volume jump · health-signal breach (bounce >3%, complaints >0.1%). **Dedicated-IP tier** gated behind a sustained-volume floor (~100k emails/mo). Enforced when the warmup/dedicated-IP tiers activate (`SenderConnection.warmupState`/`dedicatedIp` reserved fields, P1.5)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Deliverability guardrail; pairs with Q-008 cold-email path                                                                            | moderate      |
| DEC-020 | 2026-07-03 | `packages/ui` vendors the lucide icon **geometry** (ISC) as inline SVG components instead of depending on `lucide-react` at runtime: the published package declares `"type": "module"` while shipping a `.js` "CJS" build, so Node parses its require entry as ESM and Next SSR through our tsc-CJS dist gets empty exports (reproduced; crashed the `/design` prerender). A11's glyph→lucide mapping is unchanged — same icons, same names; app code outside `packages/ui` uses the `lucide-react` package directly (bundlers resolve its ESM entry fine). Revisit if/when `packages/ui` emits ESM                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Unblocks SSR without giving up the A11 icon system                                                                                    | cheap         |
| DEC-021 | 2026-07-03 | **Filtered-empty states carry secondary actions only** — the gradient primary CTA belongs to TRUE-empty exclusively (one gradient CTA per view). Owner design-review decision; note it **overrides the Contacts prototype literal** (its filtered-empty ships a gradient "⚲ Find leads", `Contacts.dc.html:182`) — logged here per the conflict rule, not silently chosen                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Owner review of C1 (2026-07-03), fix #3                                                                                               | cheap         |
| DEC-022 | 2026-07-03 | Empty-state icons are **lucide in the 90px radius-24 gradient tile** (Agents List `allEmpty` anatomy) — never OS emoji: `sparkles` for true-empty, `search` for filtered-empty; `StatusPill` ships the Contacts status color vocabulary in `packages/ui` (never re-derived per screen)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Owner review of C1 (2026-07-03), fixes #2/#4 + A11                                                                                    | cheap         |
| DEC-023 | 2026-07-03 | **P1.2 source-kind scope (owner amendment):** three LIVE kinds — WEBSITE (fetch+extract), DOCUMENT (real upload PDF/DOCX/TXT/MD → blob storage, `uri` = blob path → extract → chunk → embed), TEXT (pasted); CONNECTOR designed-but-inert. Sources exist agent-scoped AND workspace-scoped (`agentId` null); a knowledge base = any mix. Every source drives `IngestStatus` PENDING→INGESTING→READY/FAILED live. Acceptance += PDF fixture → READY chunks → retrievable                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Owner directive 2026-07-03; amends `PHASE1_ISSUES.md` P1.2                                                                            | moderate      |
| DEC-024 | 2026-07-03 | **P1.3 distiller + gap checker (owner amendment):** evidence-cited fills only — every `BusinessContext` field cites supporting chunk(s) from READY sources; no citation → no fill → gap (never filled from model priors). Required fields are goal-conditional (table owner-reviewed before build). Gap actions per the wizard prototype: "Type it" (persists, re-distills) · "✦ Let AI" (`ai_decides`, audited — planner's choice surfaces in step-6 preview + About-your-business card; Undo reverts). `gapResolved/gapTotal` drives the counter; step-6 launch gated on all gaps typed-or-delegated. Re-distill on source added/removed/re-ingested or typed answer changed; newly covered gaps auto-resolve. **Table APPROVED 2026-07-03 with four owner edits** — binding registry below (`## BusinessContext field registry`); canonical code home = `packages/core` once P1.3 lands                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Owner directive 2026-07-03; amends P1.3 + checkpoints §3; approval + edits 2026-07-03 (PR #25 thread)                                 | moderate      |
| DEC-025 | 2026-07-03 | **Two-layer context (resolves Q-003):** workspace layer = `BusinessContext` with canonical home Settings → Brand kit (`Settings.dc.html` ~140–310: dark Agent-summary card w/ per-section edit + ↻ Regenerate, Company-docs upload = workspace ingestion, description/offer fields, writing rules tagged Always/Never/Tone — structured planner input, separate from A8 sending guardrails). Agent layer = wizard gap answers + agent-scoped sources as per-agent overlay. Planner reads workspace+agent merged, agent wins; gap checker evaluates BOTH layers — workspace-covered items show "✓ Found in your docs", never re-asked. Phase-1 Settings scope: Brand kit WIRED; connectors + brand identity inert                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Owner directive 2026-07-03; amends checkpoints §6                                                                                     | expensive     |
| DEC-026 | 2026-07-03 | **P1.2 runtime placement:** the BullMQ ingest worker runs inside `apps/worker` alongside the Temporal stub (one long-running container, REDIS_URL already wired there); the api enqueues only. Embeddings pinned 1536-dim + hnsw cosine index (migration `20260703170000`, resolves T1's TODO). Both api + worker get `OPENAI_API_KEY` (query/chunk embedding); `STORAGE-CONNECTION-STRING` is **conditional** in Bicep (`storageSecretAvailable` param probed from Key Vault by the pipeline) so deploys don't fail while the owner step is pending — its absence only disables DOCUMENT uploads. api/worker gateways are embeddings-only until P1.3/P1.6 wire completions                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Referencing a missing KV secret fails the whole Container App deploy; separation keeps ingestion off the request path                 | cheap         |
| DEC-036 | 2026-07-05 | **C2.1 folded into the C2.2 PR (#33):** the shell-landing unit is one redirect (`/`→`/agents`), a Dashboard empty-state stub, and inert-but-real nav stub pages — not enough surface for its own review round. Also folded: the **§1 sidebar corrections** (owner-diagnosed 2026-07-05 — the deployed T6 shell predated the checkpoints): width 264→**248px** (the checkpoints' own 256 was a spec bug the owner fixed to the prototype literal), padding 22/16, nav items 11px 14px/15px/radius 12, wordmark 19px, mark radius 9, ws-badge #7FE8A0 radius 7, NEW Help flyout (240px: Help center / What's new / Contact support), single-open across ws/tools/help. Known §1 deviation kept: the profile block keeps the T6 sign-out button instead of the prototype's chevron menu (no profile-menu contents exist yet — revisit with account settings) | One redirect + stubs can't justify an owner merge round; the sidebar fixes belong to the shell unit | cheap |
| DEC-037 | 2026-07-05 | **Agents-List live-data mapping:** UI `Running/Paused/Draft` = `ACTIVE/PAUSED/DRAFT` (ARCHIVED hidden). **Health is the one derived column** (no model source): `Warn` when the agent has no runnable graph or the workspace has no ACTIVE sender, else `Good`. **Payments** renders — (real zeros) until billing exists; column stays toggleable per the prototype. Duplicate/Move-to-folder are present-but-inert with a toast (A5 one-agent-per-goal makes duplicate a later design question). Agent avatars: deterministic goal→emoji+tint map (icon map) — the prototype's per-row emojis are mock data. Agents screen renders with NO white TopBar band — the prototype's in-canvas header is the page header (composition → prototype wins; first capture round caught the drift) | Every column must be honest live data or explicitly derived/logged — never invented | cheap |
| DEC-038 | 2026-07-05 | **C2.3 build decisions:** (1) `/agents/new` lives OUTSIDE the `(shell)` route group — the prototype is a standalone full-screen wizard (own top bar `‹ Dashboard`/`✕`→`/dashboard`, 332px rail, no app sidebar). (2) Rail defs, BSTEPS build list, goal cards, build methods, contact-source cards, step titles/subtitles all verbatim from the prototype's defs. (3) Building interstitial runs on the prototype's timings but the final step **holds until the real planner graph lands**; "What we found" shows LIVE values only (sources/ready counts, context fields, real cadence) — the prototype's invented metrics (deliverability 91/100 etc.) are not faked: the Deliverability row states the real suppression/unsubscribe enforcement instead. (4) Gap checker consumes the P1.3 `GapReport` verbatim (`open/typed/ai_decides/covered`, `resolved/total`, `launchReady` gates step-6 launch); covered chips + Grounded-in chips reveal DEC-028 citation snapshots — chunk ids never render. (5) Step 2 = tabs per prototype; Branches view renders the REAL branch node's cases; ✎ edits, delay edits and `+ Add step` (delay+step inserted before the branch) persist as the next `source: MANUAL` version via new `PUT /planner/graph`; ✦ Regenerate re-runs the planner → next AI version. (6) Step 5 is email-only: phone/WhatsApp channel cards omitted (A1 email slice — they return with those channels); **A8 wins over the prototype's suppress/DNC toggles** — rendered as locked `🔒 Required` rows (suppressionCheck/unsubscribeFooter are literal `true`); suppression-count copy dropped (no live count endpoint). Category picker, capture toggles, quiet-hours/ramp toggles are visual-only local state this phase (logged, like §3's capture note). (7) `GET /senders` now returns live `sentToday` per sender (outbound Message count via `meta.senderId`). (8) Launch = PATCH `ACTIVE` + `POST /enrollments` per added contact (idempotent, P1.6); successes counted — partial failure surfaces an amber warning on the success overlay instead of silently claiming reach | Prototype literals bind composition; values must be live or explicitly visual-only; A8 guardrail literals can never become toggles | moderate |
| DEC-038a | 2026-07-05 | **PR #34 owner-review round:** (1) Step editor rebuilt as the prototype's **560px right drawer** (STEP header + channel chip, `✦ AI deliverability check`, PERSONALIZATION chips) — checkpoints §3 was stale vs the updated prototype and is **amended in this PR** (owner-authorized). Deliverability rows are **deterministically computed** (subject length, Flesch–Kincaid reading level, read time, link count, "free" count); the AI-only /100 score, verdict line and spam-risk row are omitted, never faked. Personalization chips = the REAL P1.5 `renderTokens` set (`firstName/lastName/company/senderName`) — the prototype's `{{calendarLink}}` is omitted until a booking-link token exists; chip click appends the token to the body. `✦ Rewrite with AI` renders per prototype, inert-with-title (full re-plan lives on ✦ Regenerate). (2) Step-5 sender mini-column: prototype's HEALTH (no live metric) → **AUTH n/3** from live `domainAuthStatus` (SPF/DKIM/DMARC). (3) Readiness banner singular copy: "Email channel ready to send" / "Email channel not ready" (the prototype's `All N channels` formula reads broken at N=1; email-only phase). (4) Display font: swapped static Bricolage 700 for the **variable cut** (wght+opsz), aliased to the same family — the prototypes load the variable font and the static instance rendered visibly heavier at display sizes. (5) Frame literals enforced with explicit border-box: rail divider at x=396 @1440 (64+332), top-bar border at y=66, step-1/2 containers max-width 760, rail sticky so Back/Next stay visible at 1440×900 | Stale checkpoints get amended (repo = source of truth), not silently built against; derived UI values must be computable, never invented | cheap |
| DEC-039 | 2026-07-05 | **UI punch list (owner, post-#34-merge — logged with the first C2.4 commit as instructed).** Outstanding: (a) **step-3 manual drawer anatomy** (deferred from PR #34): prototype = header "Add contacts manually" (Bricolage 17) + 32px ✕; white form card (radius 14) with 2-col rows First/Last + Email + Company/**Phone**, 11px/800 micro-caps labels, in-card tinted `+ Add contact` **multi-add**; "ADDED THIS SESSION · N" list with initials avatars + Remove; footer "N contacts ready to add" + gradient `Add to campaign`; scrim rgba(12,20,15,.4), shadow -24px 0 70px rgba(0,0,0,.28) — build is a simpler single-column drawer → fix in the next wizard-touching PR and re-shoot the pair. (b) **sidebar Tools-row padding** pokes out vs the prototype (carried from PR #33) → fix in C2.4 (first sidebar-rendering PR). Already landed in the merged head `798021b`, so NOT re-opened: step-2 editor drawer + deliverability card + personalization chips; step-1 frame literals; "All 1 channels" singular copy; checkpoints §3 amendment; the 4 clean prototypes + repo-wide `__cf_email__`/`cdn-cgi` grep (clean); the 3 duplicate evidence shots (ingesting / step-5 behavior vs compliance) | The owner's post-merge list crossed with the review-round push — reconciled here so nothing is double-built or silently dropped | cheap |
| DEC-040 | 2026-07-05 | **C2.4 build decisions** (plan approved w/ two owner amendments). (1) Inbox chips = prototype `inboxCatDefs` (DEC-034 set); checkpoints §4 chip list amended in this PR (owner-approved). **Unsubscribed threads leave the Inbox** (owner amendment — server-side filter; Contacts→Unsub + lead timeline are their home). (2) A4 5s polling on Inbox, Logs and the open lead-drawer timeline. (3) New read endpoints: `GET /agents/:id/view` (header+steps data), `GET /agents/:id/inbox` (threads grouped per contact: unread/intent/done/enrollment), `GET /agents/:id/events` (Logs + drawer timeline, optional contactId); writes: `PATCH /messages/:id/done` (thread mark-done on the latest inbound's meta) and `PATCH /enrollments/:id` (manual stage move — persists + writes the `lead.stage_changed.v1` Event row directly, `manual: true`; no bus in the API process). (4) Tab bar active state = **brand gradient** per the prototype's `tabs` map — checkpoints §4 "ink fill" wording is stale (flagged; amend with owner sign-off like the chip list). (5) Steps tab: prototype sequence cards carry NO per-step stat chips — the checkpoints-required sent/reply counts render as a right-aligned stats line per card (§0 no-anchor convention, flagged). (6) Sender drawer ports the Campaign View anatomy (identity + health + warm-up + footer); the SPF/DKIM/DMARC + Re-check-DNS + OAuth/SMTP blocks live in `Settings.dc.html`'s drawer → C2.6. Ring value = live AUTH n/3 (DEC-038a precedent — no invented reputation score). (7) Tracking toggles: open/link tracking have NO A8 Guardrails field — present-but-inert visual toggles with title notes (extending A8 is an owner decision); unsubscribeFooter/suppressionCheck render locked 🔒 Required. (8) Email-only omissions as in DEC-038: phone/WhatsApp cards, non-email channel rows live but inert w/ the prototype's Calls-tab footnote. (9) Step column = `currentNode` index / graph step count (live workflow position). Danger zone wired: Archive = PATCH `ARCHIVED`, Delete = existing DELETE | Prototype binds composition; stale spec lines get amended with owner sign-off; UI values stay live or explicitly inert | moderate |
| DEC-041 | 2026-07-07 | **Owner approvals batch (post-#35).** (1) C2.5 go — port `Contacts.dc.html` as-is (byte-identical through the corruption sweep); plan reflects DEC-034 (unsubscribed threads' home = Contacts → Unsub + lead timeline) and A4 5s polling on live surfaces. (2) Checkpoints §4 tab-bar wording amended: active = **brand gradient** (was stale "ink fill") — this PR. (3) **A8 Guardrails schema extension approved**: `tracking.openTracking` + `tracking.linkTracking` booleans so the C2.4 Settings toggles persist — lands with the wizard v2 PR (it owns the guardrails writers), DEC there. (4) **Wizard v2 PR scoped** (next after C2.5): v2 `Create Agent.dc.html` (`349be73`) becomes the fidelity source; live-URL step-1 fixes (gap checker must seed goal-conditional REQUIRED fields as OPEN gaps when uncited — DEC-024; DOCUMENT upload disabled-with-reason when STORAGE-CONNECTION-STRING absent — DEC-026; ingest row-state polling on staging; About Edit editable per §3); DEC-039 manual-drawer anatomy + re-shot pair; v2 UI (sticky rail buttons, stateful sources w/ remove ✕ + PDF add, gap/About empty states w/ covered strip suppressed at zero sources, build-method gating on ≥1 READY source or typed answer w/ dimmed Generate, **9 goal cards** w/ per-goal DEC-024 registry rows: offer→details/pricing/purchase-link/deadline · event→name/date/registration-link/what-attendees-get · upsell→what-to-pitch/who-qualifies/pricing/booking-link replacing referrals); checkpoints §3 amendment | Owner decisions land as DECs before the code that implements them | cheap |
| DEC-042 | 2026-07-07 | **Wizard v2 round decisions** (owner instruction 2026-07-07; approvals batch logged as DEC-041 in PR #36). (1) **v2 prototype = fidelity source**, committed at `349be73`; its 2 Cloudflare artifacts (email-decode script + obfuscated `jane@clinic.com`) are a regression of the PR #34 corruption — stripped mechanically in this PR, clean forms restored from the PR #34 precedent; never ported. (2) **A8 extension (owner-approved):** `tracking.openTracking`/`tracking.linkTracking` booleans added to the Guardrails schema with `.default({both:true})` so legacy rows parse; wizard step-5 + C2.4 Settings toggles now persist (DEC-040(7) inert treatment superseded); `unsubscribeFooter`/`suppressionCheck` stay literal `true`. (3) **Goal registry extension:** `GOAL_KEYS` +3 — `promote_offer` 🏷 / `fill_event` 🎟 / `upsell_clients` 📈 (prototype order, before `custom`); 9 new `ContextFieldKey`s carry the owner-specified per-goal required rows (offer→offer_details/pricing/purchase_link/offer_deadline · event→event_name/event_date/registration_link/event_value · upsell→upsell_pitch/upsell_audience/pricing/booking_link — `pricing`/`booking_link` reused from the existing vocabulary); recommended sets start EMPTY pending owner input (non-blocking default). **Correction: `upsell_clients` is net-new — no `referrals` goal exists in v1 or v2**, the owner's "replaces referrals" phrasing corrected in the plan. (4) **DEC-024 gap-seeding bug (staging):** goal-conditional required fields must seed as OPEN gaps when no cited coverage exists — zero sources currently renders all-clear and wrongly flips `launchReady`; fix in the gap checker + wizard. (5) **DEC-026:** DOCUMENT upload renders disabled-with-reason when the storage connection string is absent — never a dead click. (6) Step-1 ingest row states not visible on staging — polling path audited in this PR. (7) v2 UI: sticky rail footer (divider moves to content `border-left`), stateful knowledge rows w/ remove ✕ + URL/PDF add flows, zero-context empty states (gap subline, covered strip suppressed w/ EMPTY citation arrays, dashed About card), "How should we build" gated on ≥1 READY source or typed answer with dimmed no-op Generate (`hasContext` lifted to the parent for the rail). (8) DEC-039a manual drawer anatomy built + pair re-shot. Checkpoints §3 amended in this PR (frame/step-1/step-3 wording) | The repo prototype is the single fidelity source; A8 consent literals never become toggles; registry changes are core edits, never UI-local keys | moderate |
| DEC-043 | 2026-07-07 | **PR #37 owner-review round.** (1) 37-1 root cause: `overflow:hidden` on the wizard root div created a scroll/clip container that disables every descendant `position:sticky` — the v2 rail footer never stuck. Fixed with `overflowX:'clip'` (clips horizontally without creating a scroll container); scrolled pair re-shot with the rail pinned. (2) 37-2 root cause: **capture-environment error, not a P1.2 bug** — the API and the local capture worker resolved `UPLOADS_DIR` per-process cwd (`apps/api/.uploads` vs `apps/worker/.uploads`), exactly the caveat documented in `packages/knowledge/src/storage.ts`, so the worker couldn't read the uploaded file → FAILED. TXT extraction works. Re-shot with a shared `UPLOADS_DIR`: distinct added/READY shots + FAILED kept as its own labeled state. (3) **Q-009 side-finding fixed:** the dropzone advertises CSV (prototype literal) but the extractor and the API extension gate both rejected `.csv` — CSV now ingests as plain text (`extract.ts` txt path + gate + error copy). (4) Upload-disabled reason reworded user-facing: "Document storage isn't configured for this workspace yet — ask your admin to finish storage setup." (no repo/PR references in product copy). (5) C2.4 carry-along: Settings Danger zone now right-aligns Archive/Delete on the copy row per the Campaign View prototype (was stacked). (6) Ingest vocabulary confirmed: PENDING → "Queued", INGESTING → "Ingesting" (both amber), READY → "Ready", FAILED red | Review fixes land on the same PR branch; §8 pairs re-shot for every changed surface | cheap |
| DEC-044 | 2026-07-07 | **PR #36 owner-review round.** (1) 36-1: Add-a-contact rebuilt as the prototype's **484px right drawer** (white header band + ✕, 2-col First*/Last, Email* w/ invalid border `#E0A99E`, 2-col Company/Phone, Title, footer validity hint + Cancel + gated gradient Create contact; placeholders verbatim Jane/Doe/jane@clinic.com/Clinic name/+1 …/Owner). Owner-approved omissions: **STATUS picker** (writes a stored status — conflicts with A10 derived status), **LIST select** (lists inert this phase), **CUSTOM FIELDS** (no model), **LOCATION** (no Contact field; Title kept). (2) 36-2: Import CSV rebuilt as the prototype's **3-step wizard** (progress bars + STEP N OF 3 · Upload w/ dashed dropzone + real file parse + selected-file row "N rows · N columns detected" · Map your columns 1fr/22px/1.1fr grid with auto-matched selects incl. Skip this column · Review stats tiles New/Duplicates/On-suppression/Columns-mapped computed from the parsed file vs live rows · done screen "N contacts imported" with the real created count); client-side parse → existing `POST /contacts` loop; Add-to-list select omitted (lists inert). **Round-2 amendment:** import follows the prototype's semantics — detected duplicates are **skipped**, only fresh rows POST; the button reads `Import {newCount} contacts` and the done-count follows. Tile label "Duplicates skipped" replaces the prototype's "Duplicates merged" — the importer skips, it does not merge (honest label). Micro-deviations kept per owner round-2: the add-drawer footer hint "First name & a valid email required" and the valid-state "Ready to create" wording. (3) 36-3 DEC-021 regression fixed: filtered-empty "⚲ Find leads" is a white/bordered secondary. (4) 36-4 DEC-022 regression fixed: both empty states use the shared `EmptyState` (90px radius-24 gradient tile, lucide sparkles/search). (5) 36-5 shell: `.cf-sb__tools-toggle` gains `text-align:left` (button default centered the flex label); `.cf-sb` gains `line-height:normal` — the body's 1.5 made every nav row ~4px taller than the prototype (§1 ±2px tolerance). (6) Minors: detail drawer omits LOCATION (no field) and adds an "Added <date>" row (logged — substitute metadata, flagged for owner veto); Move menu shows the ✓ on the contact's current derived stage | Composition → prototype wins; approved omissions are logged, never silent | cheap |
| DEC-045 | 2026-07-07 | **C2.6 build decisions** (plan on PR #38). (1) **Wired:** Email-senders + Mailer tables (live `GET /senders`; Email tier renders live non-CF_MANAGED rows — empty this phase → designed §0 empty state; Mailer = live CF_MANAGED rows), the 500px sender-detail drawer on LIVE fields only (identity, sends-today vs dailyLimit, warm-up card iff `warmupState`, dedicated-IP iff set, domain-auth rows from `domainAuthStatus`) — the prototype's health /100 ring, ISP reputation, blacklist, token-expiry, week/all-time tiles have NO backing and are omitted per DEC-038a, itemized; **Suppression list** wired to new `GET/POST/DELETE /suppressions` (manual add upserts the SAME rows the send boundary checks — adding genuinely blocks the next send, §6 acceptance); table gains **channel + source** columns per §6 (prototype had Email/Reason/Added only); reason strings map to the `SuppressionReason` enum ("Complaint"→SPAM_COMPLAINT). (2) **Brand kit:** Company-docs + Web-pages cards wired to WORKSPACE-scoped knowledge sources (list/upload/add-URL/remove — real P1.2 ingest, "Indexed" = live READY); Core-offer persists via the workspace BusinessContext layer (`context/answers`, no agentId); description/guardrails/agent-summary render the full prototype anatomy as **designed-inert local state** with a "persists with the workspace Brand-kit unit" note (no 14-section summary backend; the free-text Guardrails card is planner guidance, NOT the A8 schema — A8 literals stay locked in wizard step-5/agent Settings). (3) **Connect flow:** all provider trees render; only the Mailer flow completes (`POST /senders`); Gmail/Outlook/Exchange/SMTP land on the coming-soon inert submit matching the API's CF_MANAGED-only gate; "Exchange" has no `SenderType` — inert, mapped conceptually to OUTLOOK_OAUTH. (4) Sending/receiving display vocabulary ("Good/Warming/Needs verification") is DERIVED from `status` + auth passes, never stored. (5) Toast follows §0 bottom-center (prototype top-right — logged deviation). (6) Phone/WhatsApp/SMS/voice sections + flows render inert-with-layout per A1. (7) Sender "Remove"/"Re-check DNS" are inert-with-note (no endpoints yet — never fake a mutation). (8) **Owner question raised on PR #38:** commit `d951403` dropped v3 `Contacts.dc.html`/`Create Agent.dc.html` at the repo ROOT (outside the canonical prototypes dir; clean; contains new drawer-edit + custom-fields-mapping work) — awaiting owner call: adopt as fidelity sources + punch-list the deltas, or remove | Live-or-omit for metrics; A8 literals never render as controls; §6 spec wins on data contracts, prototype on styling | moderate |
| DEC-046 | 2026-07-07 | **v3 prototypes adopted (owner housekeeping instruction).** The `d951403` root drop was the intentional upload for the upcoming **C2.7 · Custom fields** unit, wrong location. Moved `Contacts.dc.html` + `Create Agent.dc.html` into `design_handoff_clientforce_restyle/prototypes/` replacing the canonical copies (additive v3 supersets — all existing checkpoint states unchanged; corruption-clean, 0 hits); duplicate root `sidebar.js` deleted (byte-identical to canonical). v3 is now the §3/§5 fidelity source, but the NEW v3 states become binding checkpoints only at C2.7 kickoff — **punch-list (do not build before C2.7):** add-drawer custom fields, CSV map-step custom-fields dropdown (standard + custom + ADMIN create), contact-drawer inline field edit (✎/save), custom token chips + fallback card | The repo prototypes dir is the single fidelity source; forks get folded in by commit, never edited in two places | cheap |
| DEC-047 | 2026-07-07 | **Wizard bugfix round (owner live-testing B1–B7; PR #39).** (1) **Honesty machinery for background jobs:** worker writes a 15s Redis heartbeat (capabilities: ingest/distill/planner on?, storage mode, resolved uploads root); new `GET /system/health` compares against the API side; the wizard renders a **degraded-state banner naming the missing prerequisite** (worker offline / AI planning unconfigured / UPLOADS_DIR mismatch) — same honesty rule as DEC-026. Per-stage ingest timings (extract/chunk/embed/persist) + plan duration log from the worker; chunk persistence batched. (2) **DEC-038 amended:** the "Designing sequence" interstitial holds until the graph **or a failure** — never infinite; new `GET /planner/status` surfaces the job state; failure renders a designed error + Retry (re-enqueue). FAILED source rows gain Retry (`POST /knowledge/sources/:id/retry`). (3) **B3:** IngestStatus lives ON the source rows — Queued → Ingesting (amber + thin animated indeterminate bar; the prototype models amber text only, the bar is the owner-sanctioned "or equivalent" motion) → Ready/Failed(+Retry), immediate refresh on upload; the uploading overlay is demoted to transfer-only feedback. (4) **B2:** URL scope dropdown is real — "This page" default/working; "Entire site" designed-but-gated coming-soon (DEC-026 pattern); site crawl queued as Q-011. (5) **B4:** a blocked Generate always explains itself — reason ladder (name → goal → sources-still-ingesting → no-context) via §0 toast; the v2 silent no-op is superseded. (6) **B5:** no silent failures — knowledge actions without a name show "Name your agent first — knowledge attaches to it"; the draft agent is created implicitly once name+goal exist; full catch-block audit (every user-action failure surfaces a toast). (7) **B6 resume** proposed on the PR (Agent.draftState migration + Continue-setup) — awaiting owner scope call. (8) **Secret-name chain verified (platform-team note):** Azure Key Vault holds the HYPHENATED `ANTHROPIC-API-KEY` (underscores not allowed there); `infra/main.bicep` maps it → container secret `anthropic-api-key` → env `ANTHROPIC_API_KEY` on BOTH api and worker containers; `infra/scripts/preflight.sh` hard-requires the hyphenated name so a missing/misnamed secret fails the deploy, not the runtime — staging cannot be in the missing-secret silent mode. The remaining silent path is an INVALID key VALUE (preflight can't test it), which now surfaces via planner status + the failure panel. Code stays on the underscored env var; the banner admin detail + OWNER_CHECKLIST name both layers so the hyphen/underscore split can't be "fixed" into a breakage | Background jobs must be observable and honest; designed failure states, never infinite holds; secret NAME (vault, hyphenated) and env VAR (runtime, underscored) are two layers — docs must name both | moderate |
| DEC-050 | 2026-07-07 | **PR #40 owner-review round (C2.7).** (1) **40-1 root cause:** the CSV `__create__` path posted the RAW header as the def label ("practice_type") — the humanize transform existed only in the dropdown/review display, so the promise ("Practice type") and the stored label diverged; fixed by sharing one `humanizeHeader` and posting the humanized label (the raw slug lives only in the def key / `{{custom.practice_type}}` token, which was already correct). The value placeholder echoed the field name for non-seeded defs — placeholders are value examples or generic, never the field name; non-seeded defs now use the prototype's generic "Value". (2) **40-2:** both create paths surfaced the duplicate message for every failure; the 422 cap now gets its own copy ("This workspace has reached its 30-field limit — archive a field to add another."), 409 keeps the duplicate copy, and the CSV variant appends "The import was not started." (3) **Owner-accepted as designed, logged not changed:** the review-step created-fields note row (honest addition — prototype has no created-count tile); the gated "Insert token" affordance (prototype editor is display-only); the "New field name" placeholder (prototype-wins over the plan's example copy); and the **inline-create two-step flow** — the plan's create-def-then-focus-value semantics supersede the prototype's local name+value ad-hoc row (the prototype folded ad-hoc fields into the contact at Create time; the plan makes the def a workspace object at creation, so the drawer creates on Enter/confirm and re-renders the def input) | Display transforms belong where the DATA is written, not just where it renders; designed errors are per-cause, never one-size | cheap |
| DEC-049 | 2026-07-07 | **C2.7 kickoff — `docs/PLAN_CUSTOM_FIELDS.md` adopted verbatim (owner-approved 2026-07-07, committed with this kickoff).** Binding scope: (1) `ContactFieldDef` (key slug immutable, `@@unique([workspaceId,key])`, `FieldType` TEXT|NUMBER|DATE|SELECT with **TEXT-only creation UI**, origin manual|csv_import, archive-never-delete) + `Contact.custom Json @default("{}")` — `Contact.enrichment` untouched, reserved for machine enrichment. (2) API: GET/POST `/contact-fields` + PATCH (label/options/archived only), **POST admin-only (OWNER/ADMIN; 403 otherwise)**; contact create/update accepts `custom` validated against active defs (unknown keys rejected); CSV `__create__` target creates the def transactionally with the import; **31st active def → designed 422**. (3) UI: the four v3 prototype surfaces (add-drawer CUSTOM FIELDS block w/ admin-only inline create; CSV map-step sectioned dropdown w/ create row, picked-new teal `#1192A6`; detail-drawer inline value edit; wizard token picker `{{custom.<key>}}` chips w/ **mandatory-fallback card** — never-blank at render AND save-time validation, P1.5 precedent); non-admins see/fill inputs, never create affordances. (4) DEC-046's v3 punch-list states are now binding checkpoints (§3/§5 amended in this commit). Deferred per plan: table columns/sort/filter/segments, Settings field manager, non-TEXT creation UI, auto-enrichment | The plan doc is canonical in-repo; owner decisions land as committed text, not chat memory | moderate (1 model + 1 column) |
| DEC-048 | 2026-07-07 | **B6 draft resume (owner GO on the PR #39 proposal).** (1) Migration `20260707220000`: `Agent.draftState Json?` — the wizard's LOCAL working set only (`step`, `buildMethod`, `added` contact refs, `capture` toggles, step-5 schedule scalars), typed by core `draftStateSchema`; everything durable (name/goal/instructions/sources/gaps/context/graph/guardrails) already lives on its own rows and REFETCHES on resume — never duplicated into the JSON. (2) Wizard autosaves the set debounced 800ms onto the DRAFT agent (`PATCH /agents/:id`); save failures toast once per outage, not per tick. (3) `GET /agents/:id/draft` hydrates `/agents/new?agent=<id>`: non-DRAFT agents redirect to the agent view; resume at step ≥2 refetches the planner graph and falls back to step 1 if no plan ever landed. (4) Agents list: DRAFT row click + a "Continue setup" row-menu action (first item) open the resumed wizard — no prototype anchor exists for a draft-row treatment; composition follows the existing row menu (owner-sanctioned B6 addition). (5) Launch PATCHes `draftState: null` — a launched agent is not resumable. (6) `urlScope` was proposed for the set but is NOT persisted: it lives in the add-source panel and always resolves to "page" while "Entire site" is gated (Q-011) — persisting it would be dead surface; it joins the set when site crawl lands | Resume must restore from the server's truth, not a JSON copy of it; the working set is only what would otherwise die with the tab | cheap (one nullable column) |
| DEC-051 | 2026-07-08 | **Staging Redis/queue outage — full five-layer diagnosis and fixes (owner-directed live investigation; PRs #41 #42 #43 #44 + this PR).** The B1 degraded banner stayed up after #39 because five independent faults stacked, each invisible until the previous one cleared: **①** the platform team's network lockdown closed the path to Azure Redis (both containers `connect ETIMEDOUT`; platform-fixed, incl. the storage account in the same sweep) · **②** `redisOptionsFromUrl` dropped the `rediss://` scheme — plain host/port sent plaintext BullMQ connections to a TLS-only 6380 (PR #41; same PR un-redded `infra/scripts/smoke.sh`, which had asserted the pre-C2.1 "Welcome back" dashboard since PR #33 — five red deploys were burying the real outage; the e2e sign-in had the same stale assertion, PR #43) · **③** the #41 regression test's credential-shaped literal tripped the deploy-gating secret-scan (PR CI doesn't run the scan; deploys do) — the scanner was RIGHT, the fixture was wrong; tests now build credentialed URLs at runtime via URL setters (PR #42) · **④** the live cache is Redis CLUSTER: BullMQ's multi-key Lua scripts died with CROSSSLOT until every Queue/Worker passed the hash-tagged `BULL_PREFIX = "{cf}"` (PR #44) · **⑤** WHATWG `URL.password` returns the PERCENT-ENCODED form and Redis AUTH wants the raw secret — an access key ending `=` was sent as `%3D`, so every BullMQ connection looped WRONGPASS while the heartbeat client (ioredis's own decoding URL parser) stayed green: health said "worker alive", yet the API's enqueue hung each doc upload to the 240s ingress timeout (504 "stream timeout"). Fix (this PR): `decodeURIComponent` on username/password in `redisOptionsFromUrl`, and the three local `connectionFrom` copies in knowledge/context/planner queues (same raw-password bug, plus missing SNI) now delegate to it — ONE URL parser platform-wide, with a runtime-built `%3D`→`=` regression test. **Security:** a diagnostic probe run echoed api-container stderr (ioredis error objects include the AUTH argument) into a public-repo Actions log, exposing the encoded Redis key; the run's logs were deleted within minutes but the key is treated as COMPROMISED — platform team must rotate it and update KV `REDIS-URL` after this fix deploys (decoding makes the new key's encoding irrelevant). The probe's two container-log steps now mask token-shaped strings (24+ secret-alphabet chars → `[REDACTED]`) before anything reaches stdout; `worker-health-probe.yml` keeps the storage-target + timed-upload instrumentation as a permanent dispatchable ops tool | Health checks must exercise the SAME client path as the work — split-brain otherwise; one URL parser, not four copies; public-repo Actions logs are publication: mask before echo; stale test assertions that redden a pipeline hide real outages | moderate (spread over 5 hotfix PRs) |
| DEC-052 | 2026-07-08 | **Layer ⑥ — cluster-aware Redis clients (`REDIS_CLUSTER=true`).** The post-decode probe proved AUTH fixed (the enqueue Lua actually executed) and surfaced the next fault in ~1s instead of a 240s hang: `ReplyError: MOVED 4633 <node-ip>:8501` → HTTP 500 on upload. The staging cache is **OSS-cluster-policy**: the keyspace is sharded across nodes, a standalone ioredis client cannot follow MOVED redirects, and BullMQ only speaks cluster when handed a **`Cluster` instance** — plain connection options always construct a standalone client (verified in bullmq 5.79 `RedisConnection`). Fix: `bullConnectionFromUrl` in `@clientforce/events` returns plain options normally and a `Redis.Cluster` instance when `REDIS_CLUSTER` is set (explicit flag, not sniffed: a standalone client on a cluster fails only per-key, a cluster client on plain Redis fails outright — guessing wrong is a silent split-brain); Azure OSS-cluster TLS recipe baked in (identity `dnsLookup` — shard nodes are announced by raw IP — and SNI pinned to the cache hostname); `createRedisClient` (heartbeat) is cluster-aware under the same flag so health keeps exercising the same path as the work (DEC-051 lesson); ALL 9 Queue/Worker sites + both EventBus constructions + the channels URL-defaults route through the one factory; `infra/main.bicep` sets `REDIS_CLUSTER=true` on api AND worker (must agree, or one side's queue writes land where the other can't read). Local dev/CI without the flag is byte-identical to before | A clustered cache is a topology contract, not a connection string — the client must be told; every new connection path goes through the shared factory or it WILL re-break | cheap |
| DEC-055 | 2026-07-08 | **C2.8 kickoff — `docs/PLAN_CONTACT_LISTS.md` adopted verbatim (owner-approved 2026-07-07, attached at kickoff 2026-07-08, committed with this kickoff).** Binding scope: (1) **Lists ≠ segments** — segments stay derived status queries; lists are explicit stored membership. `ContactList` (`@@unique([workspaceId, name])`, `origin` manual|csv_import|form|widget|automation — future origins RESERVED, archive-never-delete like ContactFieldDef) + `ContactListMember` (composite PK `[listId, contactId]`, `addedAt`, `addedBy` userId|"import"|"automation"), both RLS'd. (2) **Events are the join points:** `list.member.added.v1` / `list.member.removed.v1` on the existing T2 bus (catalog + zod schema + payload-asserting tests) — Forms/Widget/Automations subscribe later, NO integration UI in v1. (3) API: `GET/POST /lists`, `PATCH /lists/:id` (name/archived), bulk `POST/DELETE /lists/:id/members`, `GET /contacts?listId=`, contact payload gains `lists: [{id,name}]`; **any member** creates/manages (owner-resolved — working data, unlike C2.7's admin-only defs); **no list cap**. (4) **Unification rule (owner):** ONE Add-to-list menu component in `packages/ui` — v4 anatomy (header "Add N to list" · list rows w/ icon+count · ✓ current · "＋ New list [from selection]" footer) — mounted in all FOUR spots: Contacts bulk bar, contact detail drawer, Leads-tab bulk bar, lead detail drawer; §5's anatomy is the fidelity source for every mount. (5) Surfaces: live lists rail (the rail IS the filter — no separate dropdown, plan-dropped), list-scoped header, New-list modal, add-drawer LIST select, CSV step-3 "Add to list" (origin csv_import), LIST column (primary; "+N" multiple), wizard step-3 "Choose a list" card + 480px picker with **snapshot-at-launch** enrollment (same path as CSV; live-sync deferred). (6) Seed: the 22 seeded contacts' prototype list display values become real rows + memberships so rail counts are honest. **Closes DEC-044's approved omissions by reference** (add-drawer LIST select + lists rail inert were "nothing backs them" waivers — now backed). Checkpoints §5/§3-step-3/§4 amended in this kickoff commit | The plan doc is canonical in-repo; join points (origins + events) ship in v1 so integrations later subscribe instead of re-modelling; one component everywhere or the four mounts drift apart | moderate (2 models) |
| DEC-057 | 2026-07-08 | **PR #49 owner-review round (C2.8).** (1) **49-1:** the contact-drawer activity feed's fallback rendered unknown event types raw ("list.member.added.v1 · just now") — event slugs never surface: both timeline maps (Contacts drawer + lead drawer) gain human rows "Added to <list>" / "Removed from <list>" from the payload's `listName`; the slug stays in logs. (2) **49-2:** the "resumed" §8 shot was byte-identical to the pre-leave shot — indistinguishable pixels prove nothing; re-shot as the B6-precedent trio: `c28-before-leave` (picked card + manual row) → `c28-agents-draft-row` (DRAFT pill + Continue setup menu) → `c28-resumed-picked` (arrived via Continue setup, card re-resolved server-side). (3) **49-3:** list-enrolled leads read ORIGINATED FROM "Manual" — the column was a hardcoded literal. Enrollment now carries provenance: `createEnrollmentSchema` gains optional `origin {kind: manual|csv|list, listId?, listName?}` persisted into the existing P1.6 run-audit `meta` (no schema migration); the wizard's launch tags every enrollment (list members from the snapshot resolve, adds carry their per-entry `src` — manual-drawer vs CSV now distinguished on the draft's `added[]`; an add wins over list membership when a contact arrives both ways); the Leads column renders "≣ List · <name>" / "⬆ CSV import" / "✎ Manual". (4) **49-4:** wizard list picker gains the prototype's "⚲ Search lists…" row (live name filter, reset on open). (5) **Owner-accepted, logged not changed:** the New-list modal helper copy ("Group contacts for targeting — a contact can be in many lists.") replaces the prototype's segment-conflating copy — accepted as better; picker rows show "N contacts" without the prototype's "Updated X ago" meta (add when a per-list `updatedAt` surfaces in the DTO); the picked-card "· snapshot" suffix becomes plain language "· as of launch day" (owner's suggested wording) | Event slugs are internal names, never UI copy; identical pixels are not evidence of a state transition — capture the journey; provenance is recorded where the action happens, at enqueue/enroll time | cheap |
| DEC-061 | 2026-07-08 | **P2.1 — Twilio SMS adapter (Track B, PR #54; Phase 2 opens).** The P1.5 email boundary pattern ports whole: `sendSmsStep` runs the same strict rails (A8 window → per-channel caps — `guardrails.dailyCap` gains OPTIONAL `sms`, additive zod, legacy rows parse unchanged, A8 literals untouched → `Contact.optOut.sms` + `Suppression(channel "sms")` → DEC-063 allow-list) and persists outbound AS RENDERED (A6) with the GSM-7/UCS-2 segment count in `Message.meta`. `TwilioSmsSender` is transport-only (SMS_SANDBOX default ON everywhere, DEC-060a discipline; deterministic sandbox ids keep idempotency testable). **Enum-only migration:** `SenderType.TWILIO_SMS`; the E.164 number rides the `fromEmail` column (documented reuse — a rename ships when SMS columns consolidate) and the messaging-service SID rides field-encrypted `credentialsEnc`. Planner: sms steps ONLY when an ACTIVE Twilio sender exists (prompt v2 parameterizes the channel line; `validateAll` takes allowed channels). ONE durable workflow routes steps by channel; the sms activity resolves the workspace's active SMS sender itself (the enrollment's senderId is the email sender). Inbound: signature-validated `POST /webhooks/twilio-inbound` → INBOUND Message → the SAME P1.7 classify queue (channel-agnostic loop; goal labels ride C2.9's event payload unchanged). Surfaces: Settings "Phone & SMS" live (§6 amendment), wizard/Steps ChannelChip sms cards + sms daily-cap stepper (§3), functional Inbox channel filter + sms chips + `sms.*` timeline rows (§4). `sms.failed.v1` joins the pre-existing versioned sms.* catalog. **Q-items:** Q-016 per-contact-timezone sending windows (TCPA — agent-level window ships this unit) · Q-017 WhatsApp (Meta verification owner-side) · Q-018 MMS · Q-019 Twilio status callbacks (delivery events → sms.delivered/failed emission) | Port the proven boundary, don't reinvent it; every SMS-specific decision is the smallest one that keeps the email pattern recognizable | moderate |
| DEC-062 | 2026-07-08 | **P2.1 — the opt-out DOUBLE RAIL (A7 for SMS).** Rail 1: Twilio **Advanced Opt-Out stays ON** at the messaging-service level (owner checklist — Twilio auto-replies and blocks at the carrier edge). Rail 2 (ours, provable): the inbound webhook detects STOP-family bodies (Twilio's default keyword set) → `Suppression(channel "sms")` + `Contact.optOut.sms` + ACTIVE/PAUSED enrollments → UNSUBSCRIBED + `sms.opted_out.v1` + `lead.unsubscribed.v1` — the same ledger email keeps, so the send boundary refuses forever after with a typed reason. The FIRST outbound SMS of every enrollment carries the literal "Reply STOP to opt out." line (the sms `unsubscribeFooter` — not disableable, boundary-enforced) | Compliance the platform can PROVE from its own rows, not provider settings alone | cheap |
| DEC-063 | 2026-07-08 | **P2.1 — the DEC-014 sms analog (recipient allow-list).** `CHANNELS_SMS_ALLOWLIST` (comma-separated E.164) is the SMS recipient filter until the owner widens it in a logged DEC; the boundary refuses non-allow-listed numbers with `RECIPIENT_NOT_ALLOWLISTED` even in live mode. The owner's test number lands here when supplied (live-proof gate); until then no environment has live SMS recipients | Same phase discipline as email: test sends only, widened deliberately | cheap |
| DEC-064 | 2026-07-10 | **P2.1 STOP round-trip round (post-#54, owner live-testing with a Nigerian test number).** The inbound probe proved the STOP DID reach Twilio (inbound received seconds after the reply; service inbound URL correct) and surfaced two defects. (1) **Twilio error 12300 on every inbound:** the webhook answered `application/json`; Twilio messaging webhooks require TwiML — the endpoint now always returns the empty `<Response/>` as `text/xml` (we never instruct a reply). (2) **Consent failed OPEN on a threadless STOP:** resolution required a contact AND a prior outbound sms `Message` row, so a STOP with no thread silently dropped (`matched: false`) — exactly the staging state, where the proof's outbound lived in the CI runner's throwaway DB. A consent rail must fail toward suppression: a STOP whose thread can't resolve now suppresses in EVERY workspace holding a contact with the sender's phone (`resolveSmsStopFallback`; digits-length guard so a malformed From can't match everything) — Suppression + `optOut.sms` + enrollments UNSUBSCRIBED per workspace, `lead.unsubscribed.v1` emitted; `sms.opted_out.v1` is deliberately NOT emitted on this path (its catalog payload requires a `messageId` and no Message row exists — the catalog is binding). Rail 1 note: no Twilio auto-confirmation was sent because Advanced Opt-Out is not enabled on the messaging service — owner console step per DEC-062, logged as still open. **Round-trip verified 2026-07-10 21:13Z (probe run 29124612813):** the re-texted STOP arrived as a REAL inbound (`SM09f188c3…`, status received, NO webhook alert post-#61 — the 12300 is gone) and the staging suppression row was produced by that real inbound rail (no signed replay was ever dispatched this round); the missing auto-confirmation is Advanced Opt-Out still DISABLED on the messaging service — no `outbound-reply` record exists at all (absent, not undelivered), so the NG→US MT constraint is not implicated (proof sends to the same handset deliver). Console-only owner toggle; re-verify = enable → text STOP → probe shows the `outbound-reply` record + its delivery status | Consent rails fail toward suppression, never toward continued sending; vendor webhooks get answered in the vendor's contract, not ours | cheap |
| DEC-068 | 2026-07-11 | **M1b — Branch taxonomy + classifier v2 (brain wave 2/5, PR #70; documented defaults owner-accepted 2026-07-11: emission set incl. `replied` fallback + retired legacy labels · `lost` as pipeline stage, no status enum · the three designed chip labels + chip order).** (1) **Intent set extension (ADDITIVE):** `IntentSchema` gains the six-intent reply-strategy taxonomy — `objection_price · objection_timing · wrong_person · info_request · not_interested` join `interested`; every pre-M1b value stays wherever intents are pinned (catalog payloads, old Message rows, old graphs, chips) — the validate tests pin the WHOLE legacy set so additive can never silently become destructive. NO migration (`Message.intent` is a string column; no DB enum touched). (2) **Classifier v2** (`inbound.classify@v2`; the classify prompt joins the P1.1 append-only registry with v1 kept verbatim): emission set = the six + `replied` (the none-fits fallback → routes the branch default) + untouched `ooo`/`unsubscribe`; legacy `booked`/`question`/`not` retired from EMISSION only ("accepts a time" classifies `interested` — the interested case already carries the goal-met move, C2.9 labels unchanged); the structured-output enum REJECTS out-of-set labels — bounded choice, no free agency. One **pinned fixture per emission label** (`packages/channels/src/classify-fixtures.ts`, append-only contract): the CI fake AND the live proof's REAL-model matrix assert the same pins. German/French fixtures NOT carried — Prompt 8 unmerged (no such fixtures exist in repo or history). (3) **Planner v4 REPLY PLAYBOOK** (`planner.campaign@v4`; v2/v3 stay registered): the reply branch cases all six + default — price→VALUE-REFRAME (a discount may be mentioned ONLY if the business context itself contains one; DEC-015 grounding already makes invented discounts unreachable) · timing→ACK + 14–45d delay + follow-up · wrong_person→REFERRAL-ASK→end · info_request→ANSWER-from-context+CTA · not_interested→GRACEFUL-CLOSE→end; price/timing/info paths REJOIN the reply branch (loop-back edge — safe by construction: sends are idempotent per `(enrollmentId, stepNodeId)`, so a repeated intent can never re-send a strategy step). REPLY CRAFT joins the system prompt (strategy steps ≤80 words, one CTA, threaded, exempt from the decreasing-length ladder, never argue with objections, never mention unsubscribing). (4) **Validation layers:** `validateGraph` rejects duplicate intent cases and second defaults (manual `PUT /planner/graph` edits get this integrity too); `validateAll` (GENERATION-only) requires exactly one reply branch casing the six + default, every case intent ∈ `IntentSchema` (violations name the intent for the existing bounded repair), the `interested→booked` / `not_interested→lost` stage pins plus `replied` pins on the conversation cases enforced deterministically, and strategy cases must route to a strategy STEP. Legacy 1-branch graphs still EXECUTE — pinned workflow tests route a NEW intent to the legacy default case; nothing re-gates persisted graphs. (5) **LOST = pipeline stage `lost`** (string, `booked` precedent): the graceful-close path ends the enrollment DONE with stage `lost`; **NO suppression, NO opt-out** — not_interested ≠ unsubscribe; suppression stays STOP/unsubscribe-only (no deviation taken; live proof asserts zero suppression rows on this path). Documented default: no `EnrollmentStatus.LOST` enum value (additive migration available on owner request). (6) **UI — ONE intent vocabulary module** (`apps/web/lib/intents.ts`, the DEC-034 "never fork" rule extended to labels): Inbox chips filter intent SETS (prototype vocabulary preserved — `info_request` under "Question", `not_interested` under "Not interested", each with its legacy twin); THREE designed labels with no prototype anchor (flagged): **"Price objection" / "Not now" / "Wrong person"**; chip order deviation flagged: the six strategy chips lead, Replied/Auto-reply trail (row scrollable per prototype); **unknown intent renders VERBATIM** in the neutral tint (the C2.9 timeline rule — never "Unclassified", never a crash; null keeps "Unclassified"); wizard step-2 branch rows use the vocabulary + goto pills resolve the target step's subject-or-body snippet (raw node ids never render); classified-intent timeline/log rows humanized everywhere (DEC-057 — the slug never surfaces); Leads gains the designed stage-lost **"Closed"** pill + filter (flagged). (7) **v4-graph honesty carry-alongs:** every "sequence" surface now walks the MAIN PATH (`apps/web/lib/graph-path.ts`) instead of filtering all step nodes — wizard sequence list/flow card/step counts/cadence + Steps tab (reply-strategy steps render in their own labeled, editable "Reply strategies" group — designed grouping, flagged); fixed latent M1a bug: `+ Add step` spliced after the LAST step node (a strategy step on v4 graphs, the breakup on M1a's own shape) producing an invalid two-edge graph — it now splices before the reply branch along the main path (the DEC-038(5) documented semantic); api planner e2e fake emits the v4 shape; live proof gains the fixture-matrix/objection-path/no-suppression gates with the unsubscribe leg moved LAST (its suppression of the shared allow-listed inbox would block every later send) | Objection handling becomes PLANNED behavior: the classifier picks from a pinned enum, the planner pre-plans one strategy path per intent, and every layer keeps runtime choice bounded — additive everywhere so nothing already shipped moves | cheap (no migration) |
| DEC-065 | 2026-07-10 | **M1a — Playbooks + Strategy settings (brain wave 1/5, PR #63).** (1) **Arc map adopted** (full map on the PR #63 plan comment, Prompt-8 pattern — **owner-approved 2026-07-10**): 5 arcs (`diagnose_prescribe` · `give_value_first` · `revive_relationship` · `momentum_deadline` · `earned_ask`), `GOAL_ARC` default per goal, `CATEGORY_TONE` per the 8 picker categories, 2 sparse goal×category overrides (promote_offer×Healthcare→give_value_first; upsell_clients×SaaS→diagnose_prescribe). **The arc is never stored** — `selectStrategy(goal, category)` is a pure function of two creation-time values, so the Settings display can't drift from planning behavior. (2) **Category persisted**: `BUSINESS_CATEGORIES` moves to core beside the map (picker vocabulary and map keys can't fork); the wizard's existing step-1 picker now writes the dormant Phase-0 `Agent.category` column (supersedes DEC-038(6) visual-only) — created with the draft, piggybacked on the autosave PATCH, hydrated on resume; **PATCH accepts category only while DRAFT** (owner rule: derive at creation). (3) **Prompt v3** (`planner.campaign@v3`; v2 stays registered — append-only registry): SELLING CRAFT system section (step ROLES; opener ≤`OPENER_WORD_CAP`=70 words, one question, ends with it, `BANNED_OPENERS` list; value/proof; objection-preempt; breakup ALWAYS last and shortest; one CTA per message; decreasing length; per-role subject rules) + template STRATEGY block (arc/tone/notes/neverSay, "(none)" defaults). (4) **Strategy rider** on guardrails Json (goalLabel precedent, NO migration): `{ strategyNotes ≤500, neverSay ≤10×80 }`; absent = defaults, legacy rows parse unchanged; PRESENT-yet-invalid throws (A8 discipline). (5) **neverSay double rail**: prompt ban AND deterministic case-insensitive scan in `validateAll` naming every hit → the existing bounded repair round-trip → typed `PlannerError`, nothing persisted. **Documented default:** manual `PUT /planner/graph` edits are NOT scanned — the owner's own typed words; the rail guards generation. (6) **Settings → Strategy section** (arc display + derivation note, notes textarea w/ counter, never-say chips w/ designed at-cap state, pinned-graph copy) — **designed state, no prototype anchor** (DEC-059-style waiver; composed from the tab's §0 card/label/chip conventions). (7) **Carry-along fixes:** Settings guardrails saves now COMPOSE over the server's parsed guardrails — the old rebuild-from-tab-state write silently clobbered `goalLabel` on every day/cap/tracking edit (latent since PR #52) and would clobber `dailyCap.sms`/`consent`/`strategy`; an invalid guardrails PATCH is now a designed 400 (was an unhandled 500) | The planner had compliance + honesty but zero method; teaching craft without a wizard field means deriving from what creation already knows and letting owners steer post-creation | cheap (no migration) |
| DEC-060 | 2026-07-08 | **A3 — Clerk integration (Track A, PR #53).** Dual-mode on BOTH sides, keyed on config alone: web wires `ClerkProvider`/`clerkMiddleware`/`/sign-in`/`/sign-up` ONLY when `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` is set (the legacy dev-login flow is byte-identical without it — CI/e2e run with zero Clerk env); the api's existing OIDC verifier (`AUTH_JWKS_URL`/`AUTH_ISSUER` → the Clerk instance JWKS) sits beside the dev verifier unchanged. First verified login LAZILY upserts the User (`authProviderId` = token sub; email claim REQUIRED — the Clerk session-token template must add it; a seeded user matched by email gets the provider id linked; a sub mismatch on a linked user is a 401, never a takeover). Membership-less principals get a TYPED `403 code:NO_WORKSPACE` → the shell renders the minimal first-run "Create workspace" modal (system anatomy, NOT Onboarding.dc.html) → first-run-only `POST /workspaces` bootstraps Agency → Workspace → OWNER membership (409 once any membership exists). Workspace switching stays the `x-workspace-id` header path (Clerk org activation client-side deliberately unused this unit). Clerk components skinned to §7 via the appearance API (`lib/clerk-appearance.ts`) — zero Clerk purple. Bicep wires all four `CLERK-*` Key Vault secrets conditionally (deploy probes; partial sets warn in preflight and stay OFF). **Q-items (Clerk gives these free; deliberately skipped):** Q-012 org invitations UI · Q-013 session-management/devices screen · Q-014 full onboarding flow (auth→verify→onboarding→first-run) · Q-015 Microsoft social (ships only if the owner's app review is done — non-essential per DEC-016) | One auth surface per mode, chosen by config — not code branches scattered per screen; JIT provisioning keeps the DB authoritative for RBAC | moderate |
| DEC-060a | 2026-07-08 | **A3 — live-send flip (Track A, PR #53).** Sandbox becomes env-controlled: `SENDGRID_SANDBOX` (legacy `CHANNELS_SANDBOX` still honored as fallback), default ON everywhere — the Bicep param stays `'true'` in every environment config and ONLY an explicit production parameter flips it. The new `live-send-proof` workflow (manual dispatch, typed `LIVE` confirmation input) HARD-GATES in-script before any real send: SendGrid domain auth for the send domain must report valid (SPF/DKIM) via the API AND a DMARC record must exist at `_dmarc.<root>` (read-only DNS lookup — root-domain DNS observed, never touched). The DEC-014 allow-list REMAINS the recipient filter until the owner widens it in a logged DEC — the same live run proves a non-allow-listed recipient still refuses (`RECIPIENT_NOT_ALLOWLISTED`) with sandbox OFF | The flip is a config decision gated on verified deliverability infrastructure, never a code default; the proof asserts the gate rather than trusting it | cheap |
| DEC-060b | 2026-07-10 | **A3 follow-up — the Clerk-enabled deploy broke the dev rail (post-#53 staging smoke red: authenticated GET /agents → 307).** Two defects, one fix PR. (1) In Clerk mode `clerkMiddleware` guarded EVERY request, so smoke's and the staging e2e suite's `cf_session` bounced to sign-in. The web now mirrors the api's dual verifier: a request carrying `cf_session` rides the legacy guard — and `bearerToken()` returns the dev token — ONLY while no Clerk browser session exists (`__client_uat*` cookies all absent/zero), so a stale dev cookie can never shadow a real Clerk sign-in into a redirect loop. (2) `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` was runtime-only env (Bicep), but Next.js inlines `NEXT_PUBLIC_*` into the client bundle at BUILD time — the browser half of the app stayed Clerk-blind. The deploy now passes it as a docker build-arg to the web image, gated on the SAME all-four-secrets probe as Bicep's `clerkSecretsAvailable` so image and infra always agree (the publishable key is public-by-design — it ships to every browser — and is masked in run logs regardless). **Second fix PR (same DEC):** the middleware fix exposed a third defect — the api's verifier factory was EITHER/OR (`AUTH_JWKS_URL` set → OIDC only), so with Clerk env live every dev-token API call 401'd and the shell layout 307'd on `fetchMe`; the factory now builds the real dual verifier when both are configured (`AlgDispatchVerifier`: protected-header `alg` HS256 → dev verifier, else JWKS — each branch verifies against its own key material only, so no downgrade: an HS256 token is accepted only if signed with `AUTH_DEV_SECRET`) | The dev rail is an A3 acceptance (smoke + staging e2e authenticate with it) and must survive the Clerk flip; build-time inlining is a Next.js constraint, not a wiring choice | cheap |
| DEC-060c | 2026-07-10 | **A3 Google acceptance enabler (owner-requested).** The seed adds the owner's real Google-account email as an OWNER member of BOTH demo workspaces (owner said OWNER or ADMIN; OWNER chosen for full demo parity) so the acceptance click-through's switcher re-scope step has somewhere to go. Both upserts are no-op on existing rows (`update: {}`): the Clerk-lazy-upserted first-run User keeps its `authProviderId`, and the owner's self-created first-run workspace is never referenced — it stays intact as first-run evidence | The acceptance step needs the account inside seeded data, but seeding must never mutate what the Clerk first-run flow created | cheap |
| DEC-059 | 2026-07-08 | **C2.9 adoption — dynamic goal-completion state (`docs/PLAN_GOAL_STATE.md`, committed verbatim, PR #52).** The owner-approved plan (all four OPEN items resolved 2026-07-07) is binding: `GOAL_META` in `packages/core` gains `{ terminalLabel, terminalPill }` per the plan's 9-row table VERBATIM; display-layer mapping only — internal stage key stays `booked`, NO enum migration, NO new columns, NO per-goal classifier detectors (book_appointments keeps today's booked-event path; other goals ship labels only — honest absence until their detectors land). Custom goal takes an optional owner-typed label from wizard step 1 (default "Goal met"). Aggregation rule: per-ROW pills use the completing campaign's label (carried on the `lead.stage_changed.v1` payload as `{ goalKey, label }` — additive OPTIONAL fields, no version bump); workspace-level labels = the shared goal's pill iff all ACTIVE agents share one goal, else "Goal met". Agents-list metric header stays generic. **Design-pass WAIVER (owner delegate, 2026-07-08, logged per plan):** no new anatomy — every surface already has its prototype form; only the text becomes goal-dynamic, so the plan's label table (not prototype shots) is the binding fidelity source and §8 evidence validates rendered labels against it. Checkpoint amendments: §3 step-1 explainer, §4 inbox chips + leads stage pills, §5 segment tabs/pills/filters. **52-review addendum (deliberate deviation from the table's "(tables/tabs)" column, accepted as built):** the Leads tab's table/drawer stage pill renders the LONG `terminalLabel` ("Purchase made") — it inherits the prototype's `leadST` map, whose booked entry was always the long form ("Meeting booked") — while the Leads filter option + "Mark as …" move and every Contacts row/tab/filter surface use the short `terminalPill` | The terminal stage's wording was hard-coded "Meeting booked" from the single-goal era; 9 goals need per-goal wording without schema churn | cheap |
| DEC-058 | 2026-07-08 | **CSV import execution round (PR #51, owner live-testing IMP-1/2/3).** (1) **Server-side bulk import:** `POST /contacts/import` (≤200 rows/call, zod contract in `packages/core/src/contact-import.ts`) replaces the per-row `POST /contacts` storm — one `withTenant` transaction per chunk: within-batch dedupe → case-insensitive workspace dedupe → suppression flagging (suppressed rows still create; A7 blocks at send) → batched custom-VALUE validation collected into `failed[]` BEFORE any write (a mid-tx create failure would poison the transaction) → creates (`source: "csv_import"`) → C2.8 list attach (archived lists never gain members) → post-commit `list.member.added.v1` events. This is also the seam the wizard CSV flow reuses (W3-1). (2) **Snapshot-at-review (IMP-2):** the Review tiles freeze when the user enters the step — the old `useMemo` recomputed against the 5s-polled `rows`, so the duplicates tile climbed as the import's own rows landed. The tiles are now labelled ESTIMATES; the done modal shows the server's authoritative counts, and the client makes NO skip decisions (every valid row goes up; the server re-dedupes transactionally), so stale poll data can no longer silently drop a legitimate row. (3) **Designed behavior change — within-batch duplicates:** two same-email rows in one file used to BOTH import (the old filter only checked existing contacts); now the first occurrence wins and later repeats count as `skippedDuplicates`. (4) **Execution states (IMP-1):** button disables on click → "Importing… X of N" + progress bar over 25-row chunks → done modal with real counts (imported/duplicates/suppressed/failed) or a designed error summary listing each failed row's email + reason with "Retry N failed" (failed rows only; a chunk is one transaction, so a network-failed chunk imported nothing and retry can never double-import). (5) **Close mid-import (owner: "pick one, log the DEC"):** closing the modal CONTINUES the import in the background and confirms via the §0 toast — sent chunks have already committed, so warn-and-abort could only strand a half-imported file; a caption under the progress bar says so. (6) Custom-field DEF creation stays the admin-gated client pre-step (C2.7 locks def-create to OWNER/ADMIN; import is any-member) — the endpoint takes custom VALUES only; flagged in the PR plan. (7) **51-review log line (owner-accepted, no code change):** the review-step estimate already compares LOWERCASED emails on both sides (snapshot builds the existing-set with `toLowerCase()` and lowercases each CSV email before the lookup), so client and server dedupe agree for ordinary case differences; the residual divergence is Postgres `mode: "insensitive"` unicode case-folding vs JS `toLowerCase()` on exotic characters (Turkish dotted-İ class) — accepted, since the done modal always renders the server's authoritative counts | Bulk writes belong behind one transactional boundary; a summary the user acts on must be a snapshot, not a live query; the server, not the client, decides what's a duplicate | moderate |
| DEC-056 | 2026-07-08 | **C2.8 build decisions (PR #49).** (1) **Phase-0 reconciliation (delta correction on the PR):** dormant `ContactList`/`ListMembership` tables already existed (T1, RLS-policied, zero code references) — migration `20260708100000` adds the plan's semantics (`origin` backfilled `manual`, `archived`, `@@unique([workspaceId,name])`) and renames the membership table to the plan's `ContactListMember` (`createdAt`→`addedAt`, new `addedBy`; policies + FKs ride the rename). Flagged additive departures from the plan's Prisma sketch: the surrogate `id` stays (composite uniqueness enforced via `@@unique` — same guarantee, no FK churn) and `workspaceId` stays on membership rows (the house RLS pattern keys on it; the sketch omitted it). (2) **Events:** every `POST/DELETE /lists/:id/members` publishes per-contact `list.member.added.v1`/`removed.v1` through `BusOrInlinePublisher` — they persist as Event rows too, so membership changes appear on the contact timeline for free; adds SKIP existing members (no duplicate events); adding to an **archived** list is a designed 422. (3) **One menu, four mounts:** `AddToListMenu` + `listGlyph` in `packages/ui` (v4 anatomy verbatim: 248px, uppercase header, 24px glyph rows, count column on bulk mounts / ✓-current column on drawer mounts, green footer); triggers stay mount-local (v4 "≣ Add to list ⌄" chip on Contacts, the dark-bar chip on Leads, the green "＋ Add to list" pill on both drawers). The New-list modal exists on BOTH screens (Leads too — a footer that says "go to Contacts" would be a dead end, B4 rule). (4) **Wizard:** live 480px picker (prototype anatomy); picked-card treatment composes the goal-card selected state (prototype shows only the resting card — flagged); `pickedListId` rides draftState with server re-resolution on resume (B6 rule — name/count are never draft copies; archived/deleted lists drop); launch resolves membership at deploy time and enrolls through the same `POST /enrollments` path as adds (SNAPSHOT, per plan); **stepValid fix found by the acceptance walk** — step 3's Next required manual/CSV adds, a picked list now satisfies it. (5) CSV "Add to list" select sits on the review step (the import wizard's step 3) with "No list (all contacts)" default; imported rows join the list in one bulk POST after creation. (6) LIST column renders the primary (first-added) list + "+N"; contact payloads carry ACTIVE lists only (archived vanish everywhere, membership preserved — verified in evidence) | Join points ship as events + reserved origins, not integration UI; one component per pattern or four mounts drift; snapshot semantics live in the launch path, not the model | moderate |
| DEC-053 | 2026-07-08 | **Wizard bug round #2 (owner live-testing B8–B10, post-outage; PR #48).** (1) **B8 root cause — three faults, not a missing recompute:** the 5s poll already recomputes gaps + About (clean local run updates 6s after READY, no reload; staging metadata showed both owner agents' context rows GOOD server-side — QForce distilled 48s after creation), but (a) the distill kick died into an **empty catch** — during the outage tail every kick 500'd invisibly, so that session could never fill (B5 violation); (b) the wizard never read `BusinessContext.status`, so from READY until the distill landed the gap checker showed the *resting* "Not found in your docs — resolve before launching" and About sat empty — a lie for the whole read window; (c) nothing marked in-flight from the kick itself. Fix: the poll reads the rows' DISTILLING status; About + gap checker render a live "Reading your documents…" treatment (B3's amber + indeterminate-bar motion; designed state, no prototype anchor); a failed kick toasts once per streak and **retries on every poll tick** (self-heals when the server recovers); the same poll response that flips READY carries the fresh fields, so covered chips/About/launch blockers update in one tick. (2) **B9:** the dead "＋ Add sender" span now opens the **C2.6 `ConnectFlowDrawer`** with senders + readiness banner refetching on close — the owner's instruction sanctioned the Settings connect flow as the destination; the prototype's `openAddEmail` models a centered **3-provider mini-modal** (Google Workspace · Microsoft 365 · Clientforce Mailer), the build reuses the Settings **5-provider chooser** (adds Exchange + Custom IMAP/SMTP) — **owner-approved on review (2026-07-08): one component everywhere** beats a second provider list that would fork every time senders evolve. (3) **B10:** the timezone control is the picker the prototype models (cursor:pointer + ▾): IANA zones stored (A8 `sendingWindow.timezone` — both PATCH sites previously hardcoded "UTC"), menu rows in the prototype's "(GMT−06:00) Central Time" format, closed control in its "America/Chicago (CT)" format, dropdown anatomy composed from the wizard's existing menus (prototype shows only the closed control — flagged); persists via `draftState.timezone` (survives resume — proven in evidence) and renders in the step-6 review chip | Background AI reads get the same honesty machinery as background jobs (DEC-047): in-flight state, designed failure, self-healing retry; a resting empty-state copy shown during work is a lie | cheap |
| DEC-054 | 2026-07-08 | **Hardening trio (owner GO with this round).** (1) **Bounded storage writes:** blob `put` aborts at 15s; timeout AND network-level failures (ENOTFOUND/ECONNREFUSED/ETIMEDOUT/…) map to `StorageUnavailableError` → the upload returns a **designed 503** naming storage, and the source row goes FAILED with the B3 Retry (evidence: 503 in exactly 15s against a black-hole endpoint; auth/HTTP errors stay loud as 500s). (2) **Storage reachability in `/system/health`:** one bounded (3s) `exists()` round-trip to the uploads container, cached 60s so the banner's 5s poll stays cheap; `api.storageReachable` (null on the local file store) joins the payload and the wizard's B1 banner ladder names storage specifically ("…check the storage account's network access"). (3) **Automatic stranded-source sweep:** the worker re-enqueues sources stuck PENDING/INGESTING >10 min on boot + every 10 min (never a live job; re-ingest idempotent per P1.2) — the in-product replacement for the one-off `drain-stranded-sources` workflow that recovered the outage's 17 rows; cross-tenant maintenance read on the OWNER client, same precedent as P1.7 inbound thread resolution (evidence: synthetic 25-min-old PENDING row → READY on worker boot) | Every failure mode the outage exposed now has a designed, self-healing in-product answer — the ops workflows remain as diagnosis tools, not crutches | cheap |
| DEC-034 | 2026-07-04 | **Intent labels = the prototype's Inbox chips (owner directive):** the canonical classifier/catalog enum is `interested · booked · replied · question · not · ooo` (from `Campaign View.dc.html` `inboxCatDefs`; `all` is a filter) **plus `unsubscribe`** (required by P1.7 acceptance; side-effect label, never a chip — the thread leaves the Inbox for Suppression). One `IntentSchema` in `packages/events`, shared by classifier, branch cases, and the P1.8 Inbox UI. **Conflict logged, not silently chosen:** `PHASE1_ISSUES.md` said `interested\|not_now\|objection\|unsubscribe\|other` and wiring-notes §B says "interested/not-now/objection" — the prototype's literal set wins (owner 2026-07-04 + A12); issue text + DATA_MODEL §5 amended. Planner-emitted graphs unaffected (they only use `interested` + `default`) | The chips ARE the labels — a forked enum would guarantee a UI/classifier mismatch at P1.8 | moderate (enum migration) |
| DEC-035 | 2026-07-04 | **P1.7 build decisions:** Inbound Parse endpoint `POST /webhooks/sendgrid-inbound` is public-with-URL-token (`INBOUND-PARSE-TOKEN` — Inbound Parse payloads are UNSIGNED; missing/wrong token rejected in production, accepted in dev). Thread resolution precedence: In-Reply-To/References ↔ `Message.meta.rfcMessageId` (PR #31 wire id) → `providerMessageId` → latest OUTBOUND to the sender address; unmatched mail is acknowledged `{matched:false}` with zero probe-able detail. Reply pipeline: INBOUND `Message` persisted (A6) → BullMQ `clientforce.inbound.classify` → Sonnet structured-output classify (prompt carries campaign goal + last 10 `Event` rows — engagement awareness) → `Message.intent` + `email.replied.v1` on the T2 bus → consumer #1 (now REAL, injected-signaler factory so `packages/events` stays Temporal-free) signals the enrollment workflow; signal failures log, never dead-letter (Event row already persisted). `unsubscribe` intent: Suppression + `Contact.optOut.email` + `lead.unsubscribed.v1` + enrollment `UNSUBSCRIBED` + workflow **cancelled**. Event-webhook enrichment: delivered/open/click/bounce/spam publish their typed events (persisted `Event` rows — Logs feed + drawer timeline); pipeline moves at branches publish `lead.stage_changed.v1` via an optional activity hook. `lead.replied.v1` REMOVED from the catalog (A9/DEC-018 aligned). Deploy: Temporal trio + inbound token as CONDITIONAL Bicep secrets (probed like storage) — owner's Key Vault step done, worker connects next deploy | Every rule the loop depends on executes in exactly one place; unsigned webhooks need explicit auth; conditional secrets keep deploys unbreakable | moderate |
| DEC-033 | 2026-07-04 | **Provider id vs RFC Message-ID split (P1.5 bug, caught by the P1.6 live-proof header gate, fix PR #31):** run 28719640378 failed its threading gate — `SendGridSender` returned SendGrid's X-Message-Id as `providerMessageId` while the wire carried a locally-minted RFC Message-ID, and the boundary threaded In-Reply-To off `providerMessageId` → follow-ups referenced an id that is NOT the first email's Message-ID header (mail clients would not thread once sandbox is off; earlier proofs missed it by checking DB-row linkage only). Fix: transports return BOTH ids (`SendResult.rfcMessageId`), the boundary persists `Message.meta.rfcMessageId` and threads on it (pre-fix rows fall back to the bracketed provider id, which doubled as the wire id in sandbox); webhook correlation keeps X-Message-Id untouched. Regression test pins the split; proof gate now asserts header-level identity | The two ids serve different protocols — webhook correlation vs RFC threading — and conflating them breaks real inbox threading | cheap |
| DEC-031 | 2026-07-04 | **P1.6 build decisions:** `@clientforce/workflows` — ONE Temporal `campaignWorkflow` per `Enrollment` (workflow id `enroll-<enrollmentId>`; start-by-id makes double-enroll/crash-retry a no-op), walking the persisted graph with T4 semantics: `step` → `sendEnrollmentStep` activity through the FULL P1.5 boundary, **idempotent by `(enrollmentId, stepNodeId)` Message lookup** — a retried/replayed activity can never double-send; `delay` → real Temporal timer, scaled by `TEST_DELAY_SCALE`/`delayScale` input so graph data never changes for tests; `branch on="reply"` → buffered `reply` signal (P1.7's classifier target; dev surface `POST /enrollments/:id/signal-reply`), default case taken after a 72h (scaled) timeout. **`SendBlockedError` = non-retryable ApplicationFailure** — the workflow records it on `Enrollment.meta.blocked` + flips status (SUPPRESSED/OPTED_OUT → UNSUBSCRIBED, else PAUSED) and ends the path; infra errors retry with backoff (5 attempts). `Enrollment.meta` (migration `20260704210000`) is the USER-VISIBLE audit trail — refusals/branch routing render as the Logs tab's amber rows per the owner's P1.8_UI_WIRING_NOTES edit (`ae4b665`), never just server logs. Worker: `apps/worker` registers workflows+activities when `TEMPORAL_ADDRESS` set (`TEMPORAL_API_KEY` ⇒ Temporal Cloud TLS). API: POST/GET `/enrollments`, signal-reply; engine behind an injectable seam (`WORKFLOW_ENGINE`) so e2e runs without Temporal. All `@temporalio/*` pinned to one exact version (1.19.0) — pnpm resolved split majors across packages and their type trees are incompatible | Durable timers + signals are exactly what Temporal buys us (locked stack); idempotency at the Message layer makes at-least-once activity delivery safe; refusals-as-data is an owner requirement | moderate |
| DEC-032 | 2026-07-04 | **Process fix (owner directive):** from PR #31 onward every PR gets a FRESH, uniquely-named head branch — never reuse a branch across PRs. Cause: merging PR #29 auto-deleted the shared head branch, which auto-closed the just-opened PR #30 (no work lost; branch re-pushed + PR reopened). PR #30 finishes on the existing branch; the new rule applies after it | A shared head branch couples unrelated PRs to GitHub's delete-branch-on-merge behavior | cheap |
| DEC-030 | 2026-07-04 | **P1.5 build decisions:** T1's `Sender` placeholder is **replaced** by `SenderConnection` (three-tier model; migration `20260704200000` also adds `Message`/`Suppression` per A6/A7 with tenant-isolation RLS). Per-tenant creds = AES-256-GCM `credentialsEnc` under Key Vault `FIELD-ENCRYPTION-KEY` (version-prefixed for rotation); CF_MANAGED shared pool carries no tenant secret. Send boundary order: A8 guardrails (window tz-aware; campaign cap + sender dailyLimit) → suppression/opt-out → owner rule 1 (no from-name → FAIL) → §G allow-list (`CHANNELS_ALLOWLIST`) → tokens (missing token fails) → owner rule 3 (threaded steps inherit subject + In-Reply-To/References from the prior `Message`; **faux-"Re:"/"Fwd:" on a fresh thread = strip + audit in `Message.meta`**, documented default — fail-hard available on request) → owner rule 2 (footer = workspace `company_address` VERBATIM; none → FAIL) → transport → `Message` persisted as rendered. SendGrid **sandbox mode default-on** (`CHANNELS_SANDBOX`, off deliberately in P1.8); provider Message-ID minted by us so threading works in sandbox. Webhook endpoint public-with-signature (unverified accepted in dev only, rejected in production); bounce/spam/unsub write `Suppression` + `Contact.optOut`. `SENDGRID-API-KEY` + `FIELD-ENCRYPTION-KEY` wired api+worker, preflight-required | The boundary is the one place every compliance rule provably executes; sandbox-by-default makes "no accidental real sends" structural | moderate      |
| DEC-029 | 2026-07-04 | **P1.4 build decisions:** planner = **Opus-class** (`planner` route) via versioned prompt `planner.campaign@v1`; graph validation is layered — shape via `campaignGraphSchema` (exported from core, enforced + auto-repaired inside `completeStructured`) → T4 semantic validator → **slice gate** `validateAll` (email-only steps, ≥1 delay, branch on `reply`, `{{firstName}}`/`{{company}}` tokens in copy); ONE bounded semantic-repair round-trip, still-invalid output throws and is **never persisted**; executor dry-run (reply→interested) runs BEFORE the persist transaction. Persistence per A5: primary campaign auto-created on first plan, `CampaignGraph` next-version `source: AI`, `Campaign.graphId` repointed atomically. Planning refuses an empty BusinessContext (DEC-015 — no context, no grounded copy). Queue `clientforce.planner.plan` runs in apps/worker beside distill; API = POST /planner/plan + GET /planner/graph (wizard step-2 polls per A4)                                                                                                                                                                                                                                                                                                                                                                                               | Grounded-or-nothing keeps DEC-015 enforceable; layered validation makes "invalid is never stored" a structural guarantee              | moderate      |
| DEC-028 | 2026-07-04 | **Citation snapshots (P1.3 follow-up, owner directive):** `BusinessContext` citations change from bare `chunkId[]` to snapshots `{ chunkId, sourceId, sourceLabel, sourceType, locator, quote }` **denormalized at distill time** — re-ingest deletes + recreates chunks, so bare ids dangle between re-distills; a snapshot stays renderable and is the honest audit record of the evidence as distilled. `retrieve()` now returns source label/kind/uri per chunk. Locator in P1: WEBSITE → page URL · DOCUMENT → file path (no page # — pdf-parse flattens pages; page-level locators need an extractor change, deferred) · TEXT → label. Quote = verbatim ≤280-char word-safe excerpt of the cited chunk, taken server-side (model-claimed quotes are never trusted). The contract landed on main via owner doc upload (`e47ed86`, 2026-07-04) while this PR was in flight — implemented snapshot shape verified to **match it field-for-field**; branch rebased onto the owner's doc versions (my interim in-PR doc patches dropped as redundant)                                                                                                                                                                                                                                                                                                                          | Owner directive 2026-07-04; UI stays in the P1.8 wave                                                                                 | moderate      |
| DEC-027 | 2026-07-03 | **P1.3 build decisions:** `BusinessContext` v2 = two-layer rows (agentId null = workspace layer; partial unique indexes in migration `20260703180000`) with `fields Json` ({value, citations, source}), `proposedAsks Json` (custom-goal asks, dismissals sticky), `status DISTILLING\|READY` for A4 polling. Distiller = **Sonnet-class** (`classify` route) via versioned prompt `context.distill@v1`; server-side grounding: fills with citations outside the retrieved evidence pack, or unrequested keys, are dropped (→ gap) — the model's citation claim is never trusted. Agent layer distills **agent-only** evidence (`retrieve` scope `includeWorkspace:false`, added in this PR) so workspace-covered facts stay attributed to the workspace layer ("Found in your docs"). Distill queue `clientforce.context.distill` runs in apps/worker; ingest completion enqueues the source's layer re-distill. `ANTHROPIC_API_KEY` wired api+worker; preflight requires `ANTHROPIC-API-KEY`                                                                                                                                                                                                                                                                                                                                                                                  | Grounding enforcement must live server-side; layer attribution drives the never-re-ask UX                                             | moderate      |

## BusinessContext field registry (DEC-024, owner-approved 2026-07-03)

Semantics: **Required** = must be filled **with citations** from READY sources
(workspace or agent layer); uncited → gap row ("Type it" / "✦ Let AI"), step-6
launch gated. **Recommended** = distilled + cited when evidence exists; never
gaps, never blocks. Field keys are stable identifiers shared by the distiller,
gap checker, planner, and Brand kit.

**Core (required for every goal):** `offer` · `usp` · `tone`.

**Workspace-level required for any email goal:** `company_address` — the postal
address CAN-SPAM requires in every unsubscribe footer. Asked **once at
workspace level**, never per agent; usually auto-distilled + cited from the
website's contact/footer pages, so it only gaps when truly absent. **P1.5's
footer consumes this field — never a placeholder.**

| Goal card            | Required (gaps when uncited) — beyond core                                                   | Recommended (cited-if-found, never gaps)                                                                                 |
| -------------------- | -------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| 📅 Book appointments | `icp` · `booking_link`                                                                       | `availability` (booking_link carries it — owner edit 1) · `pricing` · `proof_points` · `objection_handling`              |
| 🎯 Generate leads    | `icp`                                                                                        | `lead_magnet` (must not gate launch — owner edit 4) · `qualification_criteria` · `pricing` · `proof_points` · `services` |
| ♻ Reactivate leads   | `winback_offer` · `pricing` · `availability`                                                 | `relationship_context` · `proof_points`                                                                                  |
| 🚀 Drive sign-ups    | `icp` · `trial_details` · `pricing`                                                          | `proof_points` · `objection_handling`                                                                                    |
| ⭐ Collect reviews   | `review_channel` · `incentive_policy`                                                        | `services` · `tone` nuances for existing clients                                                                         |
| ✎ Custom goal        | core only, plus ≤2 distiller-proposed asks ("suggested for your goal", auditable, removable) | everything else                                                                                                          |

Owner edits applied (2026-07-03): (1) `availability` Required only on
Reactivate; (2) **`sender_identity` is NOT a context field** — the email
signature is a merge token resolved at send time from the step-5-selected
`SenderConnection`'s from-name; **P1.5 must FAIL a send whose sender lacks a
from-name** (never render a blank signature); (3) `company_address` added as
workspace-level Required for email goals; (4) `lead_magnet` → Recommended,
`qualification_criteria` added Recommended, `icp` stays Required on Generate
leads. Cross-cutting rules unchanged: `icp` not core; every field stored as
`{ value, citations: chunkId[], source: "distilled" | "typed" | "ai_decides" }`;
gap checker merges workspace+agent layers (agent wins, "✓ Found in your docs"
never re-asked, DEC-025); goal change re-runs gap evaluation (typed/delegated
answers persist; no-longer-required answers kept but ignored).

## Open questions → design/product

| ID    | Question                                                                                                                                                                                                             | Type                                                    | Default taken (if non-blocking)                                                                                                        | Status                                                                                                                                                                                                              |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Q-001 | Auth provider?                                                                                                                                                                                                       | —                                                       | —                                                                                                                                      | **ANSWERED** → Clerk (§G); Phase 1 stays dev-auth per A3                                                                                                                                                            |
| Q-002 | Rename "Qualified" chip to "Interested"?                                                                                                                                                                             | —                                                       | —                                                                                                                                      | **ANSWERED** → keep "Qualified" (§G)                                                                                                                                                                                |
| Q-003 | BusinessContext preview surface — no prototype exists (superseded: prototype exists in Settings → Brand kit)                                                                                                         | NON-BLOCKING                                            | P1 verifies BusinessContext via API/tests only; no invented UI                                                                         | **RESOLVED 2026-07-03** → two-layer context, canonical surface = Settings → Brand kit (DEC-025)                                                                                                                     |
| Q-004 | <1280px responsive treatment                                                                                                                                                                                         | NON-BLOCKING                                            | Fluid 1280–1920, zero horizontal scroll at 1280; below 1280 unsupported this phase                                                     | OPEN (needs a design batch)                                                                                                                                                                                         |
| Q-005 | `OWNER_CHECKLIST.md` is referenced by handoff §G but absent from the repo                                                                                                                                            | NON-BLOCKING                                            | I author it during P1.5, when SendGrid emits the real DNS records — written click-by-click for a non-technical owner                   | **RESOLVED 2026-07-04** — authored in PR #29 (domain auth documented as done; secrets table; §G test-inbox note; P1.7/P1.8 steps forecast)                                                                          |
| Q-006 | SendGrid domain-auth timing: §F says "start now, DNS lags"; owner's kickoff message says records at P1.5. Exact CNAMEs require SendGrid access (`SENDGRID-API-KEY`)                                                  | NON-BLOCKING                                            | Wait for P1.5 per the owner's (later) message; if the key lands in Key Vault earlier, produce the SiteGround records the same day      | **RESOLVED 2026-07-03** — owner reports domain authentication for `clientforce.io` is **verified in SendGrid**; no DNS wait at P1.5                                                                                 |
| Q-007 | Create GitHub issues for P1.1–P1.7, or work from `PHASE1_ISSUES.md` with plans as first PR comments?                                                                                                                 | NON-BLOCKING                                            | Work from the doc; plans go on PRs (owner's kickoff instruction) — no issues unless requested                                          | OPEN                                                                                                                                                                                                                |
| Q-008 | Cold-email sending path for auto-prospecting: SendGrid AUP prohibits cold/purchased lists, so at scale this tier likely needs user-connected mailboxes or cold-friendly infra + engagement warmup (pairs with Q-005) | NON-BLOCKING for Phase 1 (allow-listed test sends only) | Decide alongside the phase that activates user-owned senders (`GMAIL_OAUTH`/`OUTLOOK_OAUTH`/`SMTP` tiers)                              | OPEN (owner-raised 2026-07-03; owner referenced it as "Q-006" — filed as Q-008, that ID was taken)                                                                                                                  |
| Q-009 | Uploaded checkpoints §6 lists **XLSX** among Brand-kit Company-docs upload types (PDF, DOCX, XLSX, TXT, MD · 25 MB) — the P1.2 extractor + upload filter support PDF/DOCX/TXT/MD only                                | NON-BLOCKING                                            | Add XLSX extraction (sheet → text) when C2.6 wires the Brand-kit upload surface; until then the API rejects .xlsx with a clear message | **RESOLVED 2026-07-04** — owner: defer to C2.6, with the accept-list rule logged in checkpoints §6 (accept list must match extractor capabilities at wiring time; never advertise a format that fails after upload) |
| Q-010 | **Brand-kit persistence** — the Settings → Brand kit Agent-summary + Guardrails cards ship as amber previews (C2.6/DEC-045); the backing work is workspace-level P1.3 distillation (14-section summary model + guardrails persistence + citations) | NON-BLOCKING | Previews render the full prototype anatomy with the amber "edits here don't save yet" note; offer/docs/web-sources already persist | OPEN — queued after C2.7 (owner to slot; the amber note resolves when it lands) |
| Q-011 | **Site crawl** — the URL source's "Entire site" scope is designed-but-gated (B2/DEC-047); the extractor fetches single pages only | NON-BLOCKING | "This page" is the live default; "Entire site" shows the coming-soon treatment | OPEN — crawl (sitemap/link-walk + page budget) queued as a follow-up unit |
| Q-020 | **Guided-compose metering (G1/DEC-070, amended G2/DEC-071)** — credits are DISPLAY-ONLY at launch (guided SMS shows "3 credits / send", guided EMAIL "2 credits / send" — owner brief; the T2 catalog's `credits.consumed.v1` exists but no ledger/balance does, so emitting it would be dishonest) and the **sample preview is free** — unmetered, owner-facing compose calls (email previews return subject+body) | NON-BLOCKING | Display the per-channel figure, emit nothing; preview unmetered until a credits ledger exists | OPEN — real metering (ledger + `credits.consumed.v1` emission + preview policy: free-N-per-day vs charge) queued with the billing unit |
| Q-021 | **Per-CONTACT language override (L1/DEC-072)** — a lead whose language differs from the agent's audience (an English lead of a German agent) receives the agent's language today. A per-contact override implies PER-SEND generation — scripted steps are planned once per agent, not per lead — so it belongs with the composed-at-send surfaces (guided mode / the reply-draft wave), not with pinned graphs | NON-BLOCKING | Per-AGENT language only this unit; an agent's contacts are assumed to share its audience language | OPEN — queued; pairs with the reply-draft wave + guided email (G2), where per-lead composition already exists |
| Q-022 | **Prototype creator triggers "Reply received" and "Reply contains keyword" (SC/W2)** — neither is R1-expressible as a creator trigger: a bare "reply received" has no R1 trigger kind (replies enter as `reply_classified` intents), and keyword matching is R1's CONDITION refinement on reply triggers, never a primary trigger (the DEC-034/DEC-074 stance). Building either would mint a parallel vocabulary | NON-BLOCKING | Both OMITTED from the creator's trigger menu (`lib/triggers.ts` documents the omission); the 7 R1 kinds render, email-backed ones honestly gated on a live sender | OPEN — if the owner wants "any reply" as an entry behaviour, it needs an R1 vocabulary decision (new kind vs. all-intents preset), not a UI hack; keyword stays a condition and belongs to the rules UI wave |
| Q-023 | **Persisted AI provenance for sub-campaign branches (SC/W2)** — the canon container card carries a "✦ AI" chip, but the API persists no built-with-AI marker (the W1 endpoint stores graph + rule only). The wizard renders the chip from IN-SESSION creation state (known provenance); the dashboard renders NO chip — inferring one would be silent fakery | NON-BLOCKING | Dashboard cards omit the ✦ AI chip until provenance is persisted (flagged in `SubcampaignCards.tsx`); wizard chips are in-session only and disappear on resume ("Rule pending" fallback covers the trigger chip) | OPEN — decide the persistence rider (e.g. `CampaignRule.seededFrom` or a graph-node meta) with the rules UI wave |

## Live verification log

| Unit | Date       | Run                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ---- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P1.2 | 2026-07-03 | `knowledge-live-proof` [run 28674265474](https://github.com/tronwebdev/clientforce-platform/actions/runs/28674265474) (main, real OpenAI embeddings via Key Vault OIDC)                                                                                                                                                                                                                                                                                                                                                                                        | `https://clientforce.io` ingested PENDING→**READY**, title `"Clientforce AI — Launch Once. Sell 24/7."`, 2 chunks (1,315 tokens embedded). `retrieve("What does Clientforce do?")` → chunk `10905e45` (score 0.471) + chunk `c5befff8` (0.433). **≥2 traceable facts (§G):** (1) chunk `10905e45`: "Deploy AI Agents That Refill Your Pipeline, Personalize Outreach, And Close Deals On Autopilot—Multichannel, 24/7" + "Single Smart Inbox — every email, call, and chat in one thread"; (2) chunk `c5befff8`: testimonial "120+ Personalized Proposals Sent—Zero Manual Work — Alex K, SaaS Founder" + ROI-calculator copy "Every Agent runs a full sales cycle". Both retrieval queries hit the same chunks with sane cosine ordering                                                                                                          |
| P1.3 | 2026-07-03 | `context-live-proof` [run 28679138167](https://github.com/tronwebdev/clientforce-platform/actions/runs/28679138167) (fix branch, real Sonnet + OpenAI via Key Vault OIDC; **run 1 on main was rejected** — classify's 1024-token default truncated the tool call and the lenient schema distilled to zero fields, fixed in this PR)                                                                                                                                                                                                                            | clientforce.io → READY (2 chunks) → workspace layer distilled: **9 evidence-cited fields** (icp, usp, tone, offer, services, proof_points, trial_details, objection_handling, qualification_criteria — every one citing chunk `c5a9a124` and/or `01c5280d`), non-empty rawSummary. Gap report (book_appointments): offer/usp/tone/icp **covered — "found in your docs"**; `booking_link` + `company_address` correctly **GAP** (truly absent from the ingested page); resolved 4/6, launchReady=false. **≥2 traceable facts:** proof_points quotes "120+ personalized proposals sent, zero manual work" + "$6,720,000+ ROI-calculator projection" ← chunk `01c5280d` verbatim; usp/offer quote "Launch Once. Sell 24/7 … Close Deals On Autopilot—Multichannel, 24/7" ← chunk `c5a9a124`. §G gate (≥2 cited fields, enforced by the script) passed |
| P1.4 | 2026-07-04 | `planner-live-proof` [run 28715807651](https://github.com/tronwebdev/clientforce-platform/actions/runs/28715807651) (main, real Opus planning + Sonnet distill + OpenAI embeddings via Key Vault OIDC). A second dispatch ([run 28716040560](https://github.com/tronwebdev/clientforce-platform/actions/runs/28716040560)) FAILED correctly: the clientforce.io fetch flaked → source FAILED → 0 distilled fields → the planner's DEC-015 grounding guard refused to plan (gate working as designed; proof script hardened to surface ingest errors in PR #29) | clientforce.io → 8 cited fields distilled → **CampaignGraph v1 (source=AI)**: 3 email steps + 2 delays + branch on reply (interested→action-book) — dry-run `send → wait → send → branch → action → end`. Tokens `{{firstName}}`/`{{company}}` throughout. **§G/DEC-015 gate: 8 context fields traceable in copy** — e.g. "booked 37 qualified meetings in 7 days" + "$42,000 closed from AI voice handling objections" + "120+ personalized proposals, zero manual work" (proof_points), "built-in prospecting engine … works standalone or with your existing CRM" (objection_handling), "templates, playbooks … launch the same day" (trial_details) — all lifted from chunk-cited BusinessContext values, never priors                                                                                                                         |
| P1.5 | 2026-07-04 | `channels-live-proof` [run 28717709066](https://github.com/tronwebdev/clientforce-platform/actions/runs/28717709066) (main, REAL SendGrid API round-trip in **sandbox mode** — payload validated + accepted, nothing delivered; key via Key Vault OIDC)                                                                                                                                                                                                                                                                                                        | CF_MANAGED send accepted: **providerMessageId `UGMHLC-kTgG2iNNlRCAuHw`** (SendGrid's own X-Message-Id). Rendered body carried the tokens, the signature from the sender's from-name, the **verbatim `company_address` footer** ("Clientforce, Lagos, Nigeria") and the unsubscribe link on `reply.clientforce.io`. **Owner rule 3 live:** follow-up threaded — subject inherited as `Re: A quick idea for Tronweb`, `inReplyToId` set to the first Message row. **Refusal paths live:** `SENDER_NO_FROM_NAME` (rule 1) and `SUPPRESSED` both blocked as designed. §G gate (script-enforced) passed                                                                                                                                                                                                                                                 |
| P1.6 | 2026-07-04 | `workflow-live-proof` [run 28719899162](https://github.com/tronwebdev/clientforce-platform/actions/runs/28719899162) (PR #31 branch, real Temporal dev server + REAL SendGrid sandbox sends via Key Vault OIDC). **Run 1 on main ([28719640378](https://github.com/tronwebdev/clientforce-platform/actions/runs/28719640378)) failed its header gate CORRECTLY** — full durable timeline passed but In-Reply-To carried SendGrid's X-Message-Id instead of the wire RFC Message-ID (P1.5 conflation, DEC-033, fixed in PR #31) | Durable timeline: workflow start → step-1 sent (provider id `XNXPT4x6RKGBe1pK66-9dQ`) → **real timer: step-2 fired 6,293ms later** (scaled 1-day delay; graph data untouched) → step-2 threaded (`Re: A quick idea for Tronweb`, In-Reply-To = step-1's wire Message-ID `<9bfc032e…@send.clientforce.io>`) → reply signal `interested` → branch routed to end-a, **pipeline → booked, enrollment DONE**. Refusal path: a SUPPRESSED contact's run ended **BLOCKED with zero wire sends**, refusal recorded on `Enrollment.meta` (Logs-tab amber-row data). All four §G gates (delay/threading/branch/refusal) script-enforced and passed |
| M1b | 2026-07-11 | `inbound-live-proof` [run 29132157048](https://github.com/tronwebdev/clientforce-platform/actions/runs/29132157048) (PR #70 branch, REAL Sonnet classifier + local Temporal dev server + Redis bus + REAL SendGrid sandbox via Key Vault OIDC) | **All three M1b gates + the P1.7 legacy gates green in 28s.** (1) **Pinned fixture matrix:** all 9 emission labels classified to their pins by the REAL model — incl. the boundary cases: "Can you get back to me in March?…" → `objection_timing` (human, not ooo), "Thanks, noted." → `replied` (none-fits fallback), "Remove me from your list…" → `unsubscribe`. (2) **The objection path on the six-case playbook graph:** price-objection reply → REAL classify `objection_price` → branch routed the VALUE-REFRAME (sandbox-sent, threaded, stage `replied`) → turnaround reply → `interested` → close path — **enrollment DONE, stage `booked`**. (3) **not_interested:** graceful close SENT → enrollment DONE @ stage `lost`, **zero Suppression rows / zero optOut** (script-asserted count-before = count-after; not_interested ≠ unsubscribe). Legacy legs unchanged-green: interested → booked and the unsubscribe stop (optOut + Suppression + UNSUBSCRIBED + workflow CANCELLED) — unsubscribe leg runs LAST since its suppression of the shared allow-listed inbox would block later sends |
| P1.7 | 2026-07-05 | `inbound-live-proof` [run 28732467278](https://github.com/tronwebdev/clientforce-platform/actions/runs/28732467278) (main, first run: local Temporal dev server + Redis bus + REAL SendGrid sandbox + **REAL Sonnet classifier** via Key Vault OIDC) | **The loop, closed:** lead 1 enrolled → step-1 sandbox-sent (wire Message-ID `<c17ff0d1…@send.clientforce.io>`) → reply "how do we book a call this week?" ingested as INBOUND Message → **REAL classifier: `interested`** → `email.replied.v1` persisted with intent → signal delivered → branch routed interested → end-a — **enrollment DONE, pipeline `booked`**, `lead.stage_changed.v1` persisted. Lead 2 replied "remove me from your list" → **`unsubscribe`** → `Contact.optOut` + `Suppression` + enrollment `UNSUBSCRIBED` + **workflow CANCELLED**, zero further sends. Full timeline 7.6s; all four §G gates script-enforced. Post-merge staging deploy green ([run 28732443216](https://github.com/tronwebdev/clientforce-platform/actions/runs/28732443216)) — conditional wiring picked up the Temporal Cloud secrets; deployed worker now runs CampaignWorkflows |

## Fidelity log

| Screen                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | State matrix captured | Deviations (DEC id)                                                                                                                        | Screenshots                                       |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------- |
| B1 W3 (backoffice): adoption dashboard (activation funnel · DAU/WAU · feature adoption) — INTERNAL surface, no design-handoff prototype; built to `DESIGN_TOKENS.md` atoms + `packages/ui` | functional §8 = the adoption e2e (`backoffice-adoption.e2e.spec.ts`: funnel counts + conversion, DAU/WAU, feature matrix, low-data floor) + the telemetry privacy-rail pinned test (`packages/telemetry` — zero PII/body keys); proto-vs-build N/A (internal). Visual build capture DEFERRED — the local API server was reaped repeatedly under container resource pressure this session; page is build-verified + e2e-proven | Internal surface, no prototype to diff (flagged, DEC-081); visual capture deferred (flagged) | docs/fidelity/b1-w3/ (adoption dashboard capture pending) |
| B1 W2 (backoffice): usage · reconciliation · pricing — INTERNAL surface, no design-handoff prototype; built to `DESIGN_TOKENS.md` atoms + `packages/ui` | build-only y — usage rollup · reconciliation (a real zero-variance email match + a −10 voice variance from the seed fixture) · effective-dated credit-price editor + history, 1440×900; proto-vs-build N/A (internal surface) | Internal surface, no prototype to diff (flagged, DEC-080); token/atom fidelity honored | docs/fidelity/b1-w2/ — build-usage · build-reconciliation · build-pricing |
| B1 W1 (backoffice): INTERNAL operator surface (login · tenants · suspend modal · audit) — no design-handoff prototype exists for the backoffice, so built to `DESIGN_TOKENS.md` atoms + `packages/ui` | build-only y — default · overlay (suspend modal) open · empty/audit states, 1440×900; proto-vs-build N/A (no prototype for an internal surface) | Internal surface has no prototype to diff against (flagged, DEC-079); token/atom fidelity honored (warm `#FBF7F0` canvas, `#35E834`, Bricolage/Hanken) | docs/fidelity/b1-w1/ — build-login · build-tenants · build-suspend-modal · build-audit |
| W3-4 (PR #88): agent-view sequence editor vs `Campaign View.dc.html` Steps tab (+ `Create Agent.dc.html` drawer/brief anatomy via the shared G1/G2/G3 component) — **canon pairs**: `proto-steps-sequence` vs `build-steps-editable-default` (cards w/ green ✎ Edit · delay pills · dashed + Add step · schedule chip; flagged additions: ↑↓ reorder, ✦ Regenerate, DEC-076 banner) · `proto-steps-add-picker` vs `build-steps-add-picker` (236px "Choose a step type"; SMS sender-gated, WhatsApp/Voice honest disclosures vs canon's live picks) · `proto-step-editor` vs `build-steps-drawer-scripted` (shared 560px drawer + mode-control strip + DEC-076 notice + red Delete step) · `proto-delay-editing` vs `build-steps-delay-editing` (inline − N + Done, clamp 1–30, write through the gate) · `proto-branches-subcampaigns` vs `build-steps-chain-and-absence` (chain-true strategy group w/ per-chain + Add step; Add-a-sub-campaign canon card as HONEST ABSENCE — R1-gated) — **W2 set**: `build-steps-flip-guided-seeded` (deterministic seed, ✦ AI-picked marks, honestly INCOMPLETE 2-of-6) → `build-steps-flip-guided-composed` (staged one-step compose through the real rails) → `build-steps-sample-refused` + `build-steps-invalid-guided-edit` (REAL NEVER_SAY refusal + min-3 drawer refusal in one frame) · `build-steps-guided-cards` (guided email+sms cards + mismatch affordance) · `build-steps-brief-editor-email`/`-sms` (wizard brief editor verbatim on the dashboard) · `build-steps-sample-composed` · `build-steps-flip-scripted-empty` → `-drafted` (compose-a-draft, ✦-marked) — **W1 rest**: `build-steps-reordered` (entry follows head, ids stable) · `build-settings-regen-deeplink` (W3-12 closed). Full state matrix: default/skeleton/empty carried by the unchanged G3 states; drawer closed AND open; picker closed AND open; delay pill idle AND editing; refusal states real. Capture: real local stack (PG16+pgvector RLS, Redis, api+web prod builds, dev sign-in), deterministic prompt-driven fake model over `ANTHROPIC_BASE_URL` (G1/G2/G3 discipline), planner-SHAPED graph fixtures validated w/ real `validateGraph`, unpkg deps vendored from npm-registry tarballs via route interception, Google Fonts live via proxy; capture scripts dev-local, never committed — full disclosures in `docs/fidelity/w3-4/README.md` | y (see left) | reorder/delete/mode-control/Regenerate = designed additions; single-list composition kept vs canon sub-tabs (R1 owns the Branches view); add-step seeds sendable copy — all DEC-076 | `docs/fidelity/w3-4/` (22 shots) |
| G3 (PR #87): guided-mode visibility vs `Create Agent.dc.html` (2026-07-11 canon, updated in this PR) — **wave 1 (canon anchors)**: `proto-step2-scripted` vs `build-wizard-step2-scripted` (toolbar composition: schedule chip · Scripted \| ✦ Guided control · ✦ Regenerate with AI; scripted cards, no banner) · `proto-step2-guided` vs `build-wizard-step2-guided-mismatch` (the flip through the REAL control — `guardrails.composeMode` DB-asserted mid-capture: guided banner + "planned as scripted" line + button "✦ Regenerate to apply" over the still-scripted v1 cards) AND `build-wizard-step2-guided-planned` (graph v2: banner w/o mismatch line, guided cards w/ per-channel credits + subject hints, control hydrated guided on resume) · **wave 2 (designed — dashboard mirrors the wizard treatment; the canon drawer is the anatomy anchor)**: `build-steps-tab-guided` (mixed cards + View brief ›) · `proto-brief-drawer` vs `build-steps-brief-viewer` (READ-ONLY drawer on a launched agent: display values + honest read-only footer) · `build-steps-brief-viewer-preview` (REAL compose-preview through the REAL checks: Jane/Acme-personalized subject+body, mustSay included; honest count-only sends line) · `build-steps-brief-viewer-draft` (DRAFT variant: REAL ✎ Edit-in-campaign-setup link, "No sends from this step yet.") · **wave 3 (designed)**: `build-inbox-composed-marking` (scripted outbound UNMARKED above the guided outbound's ✦ Composed header tag) + `build-inbox-composed-line` (the mode line under the composed bubble). Capture env: real local stack (Postgres 16 + pgvector, Redis, api :3001, web :3000 production build, dev sign-in, seeded demo workspace); every interaction through the REAL UI/API; **the model is a deterministic prompt-driven fake over `ANTHROPIC_BASE_URL`** (G1/G2 discipline, disclosed) so the real gateway/composer/checks ran for the compose frame. Disclosed: both agents' graphs are planner-SHAPED fixtures validated with the real `validateGraph` (agent B's guided v2 stands in for the regenerate result — no plan worker in the §8 stack); agent A's thread rows + agent-layer BusinessContext seeded by SQL shaped as the boundaries persist them; heartbeat writer (M1a precedent); prototype's unpkg deps fulfilled from npm-tarball copies via route interception (C2.2 vendoring — the proxy blocks unpkg; prototype file untouched); capture scripts dev-local, deleted before commit | y (12 shots: 3 proto + 9 build) | DEC-075 (banner-copy deviation; carried-over G1/G2 drawer deltas noted; seeded provenance disclosed) | `docs/fidelity/g3/` (12 shots + README) |
| W3 (PR #85): wizard steps 3/4/6 vs `Create Agent.dc.html` (2026-07-11 canon) — **step 3**: `proto-step3-default` vs `build-step3-empty` (honest empty preview — designed, no proto anchor for empty) → `build-step3-list-picked` (picked card + REAL 8-member list rows + live count pill + "+ 4 more") · `build-step3-goal-tags` (reactivate agent: FOR-THIS-GOAL tags + soft-green borders, disabled Next) · `proto-list-picker` vs `build-list-picker` (4 seeded lists, live counts) · `proto-manual-drawer` vs `build-manual-drawer` (2 really-queued rows) → `build-step3-manual-added` (adds LAND in the preview, 8→10) · **the REAL import over the step**: `proto-csv-upload/mapping/review/done` vs `build-csv-*` (file parsed live; auto-match honestly skips the unmatched `clinic` column; review = snapshot tiles 10 new · 1 dupe · 0 suppressed + the wizard-mount `＋ New list "lapsed-clients-q3"` default; done = SERVER counts) → `build-step3-csv-picked` (card flips to the created list, name+count re-resolved from GET /lists) · **step 4**: `proto-step4-default` vs `build-step4-default` (anatomy match; honest deltas: keywords start EMPTY, panel "· on" + matches-after-launch body — the proto's found-leads rows are mock) · `proto-step4-keywords-open` vs `build-step4-keywords-open` (suggestions DERIVED from the agent's own context + flagged typed row) · `proto-step4-asset-menu` vs `build-step4-asset-menu` (honest "No saved widgets yet" + ＋ Create-new → nav stub) · `proto-step4-preview-empty` vs `build-step4-ap-off` (dashed 👁 empty; build also collapses config per the owner direction) · `build-step4-not-typical` (AP off default + badge + ♻ note) · `build-step4-automations-prospecting`/`-reactivate` (the two static template pairs) · `build-step4-master-off` (dim-below, designed) · **step 6**: `proto-step6-default` vs `build-step6-ready` + `build-step6-cost-strip` (proto card scale; COMPUTED estimate — 20 contacts × 3 steps × 1 = "Outbound sends (Email) 60 credits", AP/enrichment per-lead rates, "Total to launch 60 credits"; readiness line + 🚀 Deploy in the strip) · `build-step6-unresolved`/`-strip` (amber 6-gaps gate, Deploy disabled rail+strip; doubles as the AP-off estimate — no per-lead lines). Capture env: real local stack (Postgres 16 + pgvector, Redis, api :3001, web :3000 production build, dev sign-in, seeded demo workspace); every interaction through the REAL UI/API incl. the transactional import. **No model ran locally** — two DRAFT agents seeded mid-wizard for B6 resume with planner-SHAPED graph fixtures (M1b/G2 discipline) + TYPED context answers (the real no-AI resolution path, so launch-ready is genuine); the amber variant = the same agent with its agent-layer context cleared by SQL (disclosed); dev-local heartbeat writer (M1a precedent) + one seeded ACTIVE CF_MANAGED sender (step-5 gate; nothing launches); prototype served from a sandbox COPY with CDN deps vendored locally (C2.2 precedent — repo prototypes untouched); capture seed + Playwright scripts dev-local, deleted before commit | y (35 shots: 12 proto + 23 build) | DEC-073 (import-flow location premise correction; C2.5-over-wizard-proto reuse conflicts; honest-state deviations as listed) | `docs/fidelity/w3/` (35 shots + README) |
| L1 (PR #83): Agent output language — **designed surfaces, no prototype anchor exists for language** (DEC-065(6)/DEC-070 waiver pattern; composed from the §0/§4 card/label/menu conventions): **Settings → Language card** `build-settings-language-detected` ("Deutsch · German" select + "✦ Detected from this agent's content — change it any time." + the in-language compliance preview: "Abmelden: reply.clientforce.io/u/…" and "Antworten Sie mit STOP, um sich abzumelden." + the pinned-graph footnote) → `build-settings-language-edited` (flipped to "Français · French" **through the REAL UI select** — provenance "Set by you.", French lines "Se désinscrire:"/"Répondez STOP pour vous désabonner."; DB truth SQL-verified after capture: `guardrails.language=fr · languageSource=owner`) · **footer preview** `build-footer-preview-german` (the Language card clipped — the §8 footer-preview frame, both pre-translated German lines legible) · **step-2 drafted steps in German** `build-wizard-step2-german` (wizard step-2 via B6 draft resume: four German ✦ AI-draft step cards — "wo Termine verloren gehen" opener → "ich schließe die Akte" breakup — + "Branches & rules 7") · **Steps tab** `build-steps-german` (German main sequence, six-intent branch pill, live per-step stats incl. 1 sent · 1 reply from the seeded thread) + `build-steps-german-reply-strategies` (the German REPLY STRATEGIES drafts — Price objection / Not now / Wrong person cards, Email · threaded chips) · **inbox German thread** `build-inbox-german-thread` (Interested chip on the row AND the reading pane; the outbound pane shows the REAL German footer block "--\nClientforce, Lagos, Nigeria\nAbmelden: https://reply.clientforce.io/u/…"; the German reply "Das klingt spannend — wie können wir diese Woche einen Termin vereinbaren?" is the pinned MULTILINGUAL fixture text). Capture env: real local stack (Postgres 16 + Redis + api + web, seeded demo workspace, dev sign-in); the German agent was built by the REAL pipelines — agent-scoped TEXT source → real P1.2 ingest → real P1.3 distill (**the REAL evidence-pack detection wrote `language=de/detected`, script-asserted before capture**) → real P1.4 `planCampaign` rendering the v7 prompt — with the deterministic prompt-driven fake gateway (the integration-fixture discipline; no network AI, disclosed). Also disclosed: the inbox thread rows + events seeded by SQL (M1b precedent — the real classify→signal→branch path is proven by the suites + the live-proof matrix); the worker heartbeat key written during capture (M1a precedent); capture seed + Playwright scripts dev-local, deleted before commit; the wizard frame shot with the agent flipped back to DRAFT (launched agents aren't resumable — B6 rule) | y (7 shots) | DEC-072 (designed-surface waiver; disclosures as listed) | `docs/fidelity/l1/` (7 shots) |
| G2 (PR #82): Guided mode, email — **designed surfaces, same no-prototype-anchor waiver as G1** (DEC-065(6)/DEC-070(6); §0/§3/§4 card/drawer/chip conventions; email keeps the green ChannelChip tint, the composed-at-send tag keeps G1's cyan accent): **mixed-mode step-2 view** `build-wizard-mixed-mode-step2` (scripted step-1 card + guided EMAIL brief card — objective + **Subject hint** line + bullets + "✦ Composed at send" + "**2 credits / send**" — + guided SMS card at 3 credits, one sequence) · **email brief editor** `build-wizard-email-brief-editor` (560px drawer email variant: email chips, email-aware compose note incl. "the unsubscribe footer is always added by the platform", objective, **Subject hint field** w/ subject-rules explainer, bullets, mustSay/neverSay) · **sample preview** `build-preview-composed-subject` (REAL `POST /planner/compose-preview` → subject card + body card + the api's 2-credits caption) → `build-preview-subject-refused` (**the subject-check refusal proof**: a clickbait hint "quick question about the audit!!" typed + saved through the REAL drawer/PUT — owner-typed words deliberately unvalidated at save — then recomposed: the REAL SUBJECT_RULE checker walked the bounded retry to "⚠ Composer refused — nothing would send · SUBJECT_RULE — contains an exclamation mark; contains banned pattern(s): \"quick question\"…"; hint restored after) · **Steps tab** `build-steps-email-brief-card` (email + sms brief cards, per-channel credits, stats line intact) · **Inbox thread** `build-inbox-guided-email-thread` (the composed email in the real thread, the boundary-appended footer — address + exactly ONE `Unsubscribe:` line — visible) · **refusal Logs row** `build-logs-email-refusal-row` (`email.compose_refused.v1` amber row: "…SUBJECT_RULE… The lead is paused; nothing was sent.") · **Settings** `build-settings-compose-messages` ("How this agent writes messages", email+SMS copy, footer-by-platform). Capture env: real local stack (Postgres 16 + Redis + api + worker + web, seeded, dev sign-in); **the model is a deterministic prompt-driven fake served over `ANTHROPIC_BASE_URL`** (disclosed — G1's exact discipline; personalizes only from the prompt's lead block, grounds only on cached-context facts, pastes subject hints — the naive-model failure mode that makes the trap UI-drivable), so the REAL gateway/composer/checks/retry/api paths ran end-to-end. Also disclosed: the ACTIVE agent's graph/messages/events are fixture rows inserted byte-shaped as the planner emits, `sendStep` persists and `recordComposeRefused` records; worker heartbeat written during capture. Real-Sonnet evidence = the `guided-email-live-proof` workflow (provenance table in `docs/fidelity/g2/README.md`) | y (8 shots) | DEC-071 (designed-surface waiver; disclosures as listed) | `docs/fidelity/g2/` (8 shots + README) |
| G1 (PR #81): Guided mode, SMS — **designed surfaces, no prototype anchor exists for guided mode** (DEC-065(6)/DEC-059 waiver; composed from the §0/§3/§4 card/drawer/chip conventions; sms tint per the P2.1 ChannelChip anatomy): **brief card** `build-wizard-brief-card` (step-2 sms card = objective + 4 talking-point bullets + "✦ Composed at send" + "3 credits / send"; email steps keep their scripted cards in the same frame) + `build-steps-brief-card` (the same anatomy on the agent Steps tab, live per-step stats line intact) · **brief editor** `build-brief-editor` (560px drawer guided variant: compose-at-send note w/ credits, objective input, bullet rows w/ ✕ + add-input "4 of 6 (min 3)", mustSay/neverSay chip sections, "Save brief" footer — bullets, never copy) · **sample preview** `build-sample-preview-composed` (REAL `POST /planner/compose-preview` → "Jane — ran the numbers dentists ask us about: our free growth audit finds the booking leaks. Want yours?" + "Sample lead: Jane Doe · Acme Dental … 3 credits per real send (display only for now)") → `build-sample-preview-refused` (the trap brief — "free growth audit" added to Never say through the REAL UI while Must say requires it → amber "⚠ Composer refused — nothing would send · NEVER_SAY_VIOLATION — contains banned phrase(s): …") · **the guided thread for two leads** `build-inbox-guided-thread-1`/`-2` (both threads in one Inbox frame with SMS chips; panes show the two DIFFERENT composed texts for Ada/Bright Ortho vs Ben/Lakeside Dental, each ending "Reply STOP to opt out." — the boundary's line) · **refusal Logs row** `build-logs-refusal-row` (amber row: "Composer refused the SMS for Cara Adeyemi — NEVER_SAY_VIOLATION … The lead is paused; nothing was sent." — a REAL `sms.compose_refused.v1` Event row via the real bus) · **Settings toggle both states** `build-settings-compose-guided`/`-scripted` (Message-composing card above the M1a Strategy section: Scripted vs ✦ AI-guided option cards + the applies-to-future-sends footnote). Capture env: real local stack (Postgres 16 + Redis + api + web, seeded demo workspace, dev sign-in); **the model is a deterministic prompt-driven fake served over `ANTHROPIC_BASE_URL`** (disclosed — same discipline as the integration fixtures; no network AI), so the REAL planner/composer/checks/boundary/api code paths ran end-to-end: graph planned by the real `planCampaign`, both sends through the real activity + composer + sandbox transport, the refusal through the real bounded retry + `recordComposeRefused` + bus-persisted Event row (trap brief = mustSay∩neverSay, the live-proof's trap). Also disclosed: the two INBOUND sms rows seeded by SQL so the Inbox lists the threads (P2.1 precedent); the worker heartbeat key written during capture (M1a precedent); the wizard footnote's graph-version counter reflects capture-round brief saves. The §8 walk caught a real bug (preview card flex-collapsed to 2px in the drawer's overflow column) — fixed in this PR | y (10 shots) | DEC-070 (designed-surface waiver; disclosures as listed) | `docs/fidelity/g1/` (10 shots) |
| F1 (PR #78): outcome badges — step-card title-row chip anatomy from `Campaign View.dc.html`/`Create Agent.dc.html` (the prototypes' step cards carry NO stat chips — §0 convention addition like the existing step-stats span; "low data" copy is designed, flagged): `f1-steps-tab-ok-low-none` (Steps tab @1440×900 — step-1 green "4.8% reply"/ok · step-2 muted "0% reply · low data" · step-3 (7 sent) + step-4 (0 sent) no chip, honest absence; stats span rebound to the rollup) · `f1-wizard-step2-ok-low-none` (same three states on the wizard step-2 seq cards via B6 draft resume; the amber degraded-worker banner is the local stack's honest health row, unrelated to F1) · `f1-endpoint-payload.json` (live payload: signals ok/low/none/none, rates null at none, totals.goalCompletions=1) · `f1-regen-diff.md` (v2→v3 regen: OBSERVED OUTCOMES block cites the endpoint's numbers verbatim, weak step-2 rewritten, ok-signal step-1 byte-identical). Local stack, deterministic outcome-aware fake AI disclosed — `docs/fidelity/f1/` |
| M1b (PR #70): Branch taxonomy + classifier v2 — 4 shots against the DEC-068 vocabulary (designed states; the three no-anchor chip labels + Reply-strategies grouping flagged there): `build-wizard-branches-six` (step-2 Branches & rules: MAIN SEQUENCE · 4 STEPS flow card + six labeled branch rows — Interested / Price objection / Not now / Wrong person / Question / Not interested — each goto pill resolving its strategy step's copy, + "Any other reply" default) · `build-inbox-chips-six` (chip row with live counts: All 8 · Interested 2 · Meeting booked 0 · **Price objection 1 · Not now 1 · Wrong person 1 · Question 1 · Not interested 1** — all six strategy chips in one frame; Marcus's objection→reframe thread open in the reading pane with per-row intent chips on every thread) · `build-inbox-fallback-chip` (a seeded FUTURE intent `callback_request` renders VERBATIM on the thread row AND the reading-pane chip — the unknown-intent fallback rule, no crash) · `build-timeline-objection-path` (lead drawer, pill "Meeting booked": Enrolled → "where bookings leak" sent → Replied — classified "Price objection" → stage contacted→replied → reframe "Re: where bookings leak" sent → Replied — classified "Interested" → Stage changed — Meeting booked — the full acceptance journey in one frame). Capture env: real local stack (Postgres 16 + api + web, seeded demo workspace, dev sign-in, worker heartbeat key written so the B1 banner doesn't overlay); intents/events/threads seeded by SQL script (disclosed — no local Temporal/AI; the REAL classify→signal→branch path is proven by the integration suites + the inbound-live-proof M1b gates); capture seed script dev-local, deleted before commit | y (4 shots) | DEC-068 (designed labels/grouping; seeded provenance disclosed) | `docs/fidelity/m1b/` (4 shots) |
| M1a (PR #63): Playbooks + Strategy settings — **Settings → Strategy section** (designed, no prototype anchor — DEC-065(6) waiver; §0 card/label/chip conventions): `build-settings-strategy-default` (arc card "Diagnose, then prescribe" + derivation note naming the category · empty notes 0/500 · never-say input · pinned-graph footnote) → `build-settings-strategy-filled` (notes 125/500 + chips "rock-bottom prices"/"industry-leading", added through the REAL UI; persistence script-asserted across reload before the edit round) → `build-settings-strategy-edited` (post-reload journey per DEC-057: notes extended to 174/500, first chip removed, "cheap" added; DB truth verified by SQL — `strategy.neverSay = ["industry-leading","cheap"]`) · **same-agent sequence before vs after the playbook prompt**: `build-steps-before-playbook` (graph v1 — the still-registered `planner.campaign@v2` prompt through the deterministic prompt-driven fake, persisted via `validateAll`, replicating the pre-M1a pipeline: "I hope this finds you well… I wanted to reach out…", two asks per email, no breakup) vs `build-steps-after-playbook` (graph v2 — REAL `planCampaign` reading the agent's UI-edited guardrails: opener observation ending in its one question · value/proof "37 booked appointments…" · objection-preempt "one 20-minute call" · breakup "closing the file", decreasing length, per-role subjects) · **never-say auto-repair proof** (console transcript in the PR §8 comment): attempt 1 emits "We offer industry-leading." → deterministic gate error names term+step → repair prompt carries it verbatim → attempt 2 clean → persisted v3 clean, 2 model calls. Capture env: real local stack (Postgres 16 + api + web, seeded demo workspace, dev sign-in), Strategy edits driven through the real UI; the model is the SAME deterministic prompt-driven fake as the integration tests (disclosed — no network AI); fixture agent + context + before-graph seeded by script | y (5 shots + console proof) | DEC-065 (designed section waiver; before-graph provenance disclosed) | `docs/fidelity/m1a/` (5 shots) |
| P2.1 (PR #54, Track B): Twilio SMS — §3/§4/§6 amendment pass (13 shots; ChannelChip anatomy per prototype, sms tint `rgba(54,215,237,.16)`/`#1192A6`): **Settings §6** `build-sms-section-empty` → `build-sms-connect-chooser` (Twilio LIVE + buy/port inert) → `build-sms-add-form` (E.164 + label + MG sid) → **`build-sms-sender-row` (created through the REAL POST /senders — SID field-encrypted)** → `build-sms-sender-drawer` · **the loop, driven through the REAL webhook** (outbound sms Message seeded by SQL, disclosed; both inbound POSTs hit `/webhooks/twilio-inbound` for real): reply → `build-inbox-sms-thread` (SMS chip on row + channel-true pane chips + opt-out line visible on the first outbound; the C2.9 "Purchase made" chip composes for free) · `build-inbox-channel-menu` → **`build-inbox-channel-filter-sms` (the §4 filter is FUNCTIONAL — 1 thread under SMS)** · STOP → `build-suppression-post-stop` (channel sms row in §6 Suppression, script-asserted) + `build-timeline-sms-rows` ("Replied STOP — suppressed for SMS" on the contact drawer; enrollment UNSUBSCRIBED + sms.opted_out.v1 + lead.unsubscribed.v1 DB-verified) · **wizard §3** `build-wizard-sms-step` (chips Email·SMS·Email — sms step card, channel-true icon; graph's step-2 flipped to sms by SQL, disclosed — the fake-AI planner can't emit sms) → `build-wizard-sms-editor` · `build-limits-card-sms-tile` + `build-limits-modal-sms-cap` (Daily SMS cap stepper). Boundary/refusal proofs live in the 9-test integration suite; live-proof run + STOP-on-staging are owner-gated (TWILIO-* + SMS-TEST-NUMBER secrets). **54-review delta:** drawer blocks now gate by sender TYPE (no SPF/DKIM/DMARC card on TWILIO_SMS — an "Opt-out compliance" card with the double-rail rows renders instead; ✆ glyph + "Twilio SMS" chip in the identity header), Suppression header Email→**Address** (sms rows carry phones), and the inbox filter frames are now genuinely distinct: `build-inbox-all-channels` (email + sms threads side by side, seeded email thread disclosed) → menu open → `build-inbox-channel-filter-sms` (menu CLOSED, trigger reads "💬 SMS", 1 thread — script-asserted 2→1) | y (13 shots) | DEC-061/062/063 (designed: no prototype exists for sms variants — ChannelChip anatomy is the binding source per the kickoff) | `docs/fidelity/p2.1/` (13 shots) |
| A3 (PR #53, Track A): Clerk + live-send — §7 login pass (dev-mode states; the SKINNED CLERK components + Google acceptance are owner-gated on the Clerk app + KV secrets, appearance skin code-reviewed vs §7 in `lib/clerk-appearance.ts`): `build-login-default` / `build-login-error` / `build-login-loading` (dev /login — #FBF7F0 canvas, card anatomy, Bricolage heading, gradient primary) · `build-switcher-two-memberships` (owner's two REAL memberships, ✓ on active) · first-run trio `build-first-run-modal` → `-filled` → `-created` (fresh principal lazy-upserted via dev token → typed NO_WORKSPACE → modal → POST /workspaces → shell scoped to "A3 Proof Workspace", switcher shows it live — the full acceptance path minus the Clerk hop) | y (7 shots) | DEC-060/060a (Clerk-mode shots owner-gated; logged, will re-shoot post-secrets) | `docs/fidelity/a3/` (7 shots) |
| C2.9 (PR #52): dynamic goal-completion state — validated against the PLAN_GOAL_STATE label table (design pass WAIVED per DEC-059; no proto frames) — **Contacts mixed-goal (the acceptance proof)**: `build-contacts-mixed-tab` — tab **"Goal met"** (count 3) with per-row SPECIFIC pills side by side: **Booked** (book_appointments) · **Qualified** (generate_leads — the table's pill; distinct from the A10 interested-segment count in the same frame) · **Purchased** (promote_offer, created live via the drawer move) · `build-contacts-move-menu-mixed` ("Mark as goal met") · `build-contacts-drawer-timeline-goal` (drawer pill Purchased + "Moved to Purchase made by you" — the event's verbatim label) · `build-contacts-status-filter-mixed` + `build-contacts-quick-toggle-mixed` ("Goal met" option / "Goal met only") · **single-goal variant**: `build-contacts-single-goal-tab` — every non-promote_offer ACTIVE agent SQL-paused (disclosed, restored in-script) → tab "Purchased" (3) · **campaign view (promote_offer)**: `build-inbox-chip-promote-offer` (chip "Purchase made", script-asserted NO "Meeting booked" in the DOM) · `build-leads-pills-promote-offer` + `build-leads-filter-promote-offer` ("Purchase made" pill · "Purchased" filter option) · `build-lead-drawer-goal` ("Stage changed — Purchase made" + "Mark as purchased") · **booked-goal unchanged**: `build-inbox-booked-unchanged` + `build-leads-booked-unchanged` (Dental agent still "Meeting booked"/original pills) · **wizard explainers**: `build-wizard-explainer-promote` ("completes when: Purchase made") · `build-wizard-explainer-book` ("Meeting booked") · `build-wizard-custom-label-typed` (input + live "Contract signed") · **custom END-TO-END** (real wizard launch, fake-AI pipelines, disclosed): typed "Contract signed" → launch → `build-custom-lead-drawer`/`build-custom-leads-pill` (lead pill "Contract signed", move "Mark as goal met") · `build-custom-inbox-chip` (chip "Contract signed") · `build-custom-contacts-drawer` (row+drawer pill "Goal met" — custom's fixed short pill — with timeline "Moved to Contract signed by you"). DB-verified payloads: `{goalKey: promote_offer, label: "Purchase made"}` and `{goalKey: custom, label: "Contract signed"}` on the two live events | y (19 shots) | DEC-059 (waiver: label table is the fidelity source; no bulk Move menu exists on Contacts — the drawer Move menu is §5's Move surface) | `docs/fidelity/c2.9/` (19 shots) |
| CSV import execution round (PR #51) vs v4 `Contacts.dc.html` import modal — **anchor**: `proto-import-review` (c2.5 frame, review-step composition unchanged) vs `build-import-review-snapshot` (tiles 36 new / 4 dupes / 0 suppressed / 5 mapped, list select "Q4 webinar signups") → **`build-import-review-after-2-polls` (script-asserted BYTE-IDENTICAL modal text after 11s open across two 5s polls — the climbing-counter bug is dead, IMP-2)** · **in-flight (designed, DEC-058)**: `build-import-progress-start` (0 of 40, primary disabled to "Importing…", Back dimmed) → `build-import-progress-midrun` (25 of 40 · 63% gradient bar · close-and-continue caption) · **done modal (real server counts)**: `build-import-done-counts` — 34 Imported / 6 Duplicates skipped / 2 On suppression list / 0 Failed against the known 40-row fixture (34 unique new incl. 2 seeded-suppressed + 4 workspace dupes + 2 within-batch repeats); DB truth verified by SQL: exactly 34 created, 0 duplicate emails · **error summary (designed)**: `build-import-error-summary` — 41-row fixture, chunk 2 network-aborted via Playwright route → "25 imported · 16 failed", per-row reasons (1× "Invalid email address" non-retryable + 15× "Network error"), footer "Retry 15 failed" + Close → `build-import-retry-complete` ("40 imported · 1 failed" — retry re-sent ONLY the 15 failed rows; the invalid row persists honestly; DB: exactly 40, no doubles) · **DEC-058 close-mid-run**: `build-import-bg-close-toast` (modal closed at ~0/30, toast "✓ Imported 30 contacts"; DB: 30) · **rail/count sanity pair**: `build-rail-before` (Q4 webinar signups = 2) → `build-rail-after-scoped` (scoped header "36 contacts in this list", rail 36, LIST column filled; 36 membership events in the DB). Capture: local stack, real API + Postgres; suppression rows for the 2 fixture addresses seeded by SQL (disclosed); network delay/abort via Playwright route interception (disclosed — the only way to hold in-flight/error states still) | y (10 build + 1 proto anchor) | DEC-058 (designed: progress block, done-modal count grid, error summary — no prototype states exist for execution) | `docs/fidelity/imp-csv/` (11 shots) |
| C2.8 (PR #49): contact lists vs v4 `Contacts.dc.html` + `Create Agent.dc.html` — **rail pair**: `proto-rail-default` vs `build-rail-default` (5 live lists w/ real counts 8/5/6/3/3, glyph mapping verbatim) → `build-rail-scoped` (gradient active row · scoped header "Dental — local" + green LIST badge + "8 contacts in this list" · segment counts recomputed over the scope · LIST column filled) · **New-list modal**: `proto-newlist-modal` vs empty/named/created-toast + **designed duplicate error** (409 → "A list named … already exists", coral border) · **bulk menu pair**: `proto-bulk-menu-open` vs `build-bulk-menu-open` (header "Add 3 to list", icon+count rows, green footer) → `build-bulk-after-add` (toast + rail count 3→6 live) · **new-list-from-selection e2e**: menu footer → modal w/ "2 selected contacts will be added" hint → `build-newlist-from-sel-created` (new rail row, count 2, selection cleared) · **contact drawer**: `proto-drawer-list-row`/`proto-drawer-list-menu` vs List row atop DETAILS (92px label, right value, green pill) + menu w/ ✓ on current · **add-drawer LIST select** open (glyph rows + No list) · **CSV step-3**: select open ("No list (all contacts)" default + list rows) → done → `build-csv-after-count` (Q4 webinar signups rail count 2) · **wizard**: `proto-wizard-choose-list-card`/`proto-wizard-list-picker` vs live card → 480px picker (38px icon rows, count sub, ›) → picked card (goal-card selected composition, "8 contacts enroll at launch · snapshot", ✕ clear — flagged) → **`build-wizard-list-picked-resumed`** (full resume, card intact from `draftState.pickedListId` re-resolved server-side) → review Contacts tile ("8 enrolled at launch (incl. “Dental — local”)") → launch → **`build-postlaunch-leads` (exactly the 8 snapshot-enrolled Dental members on the Leads tab — the acceptance proof)** · **Leads-tab mounts**: `build-leads-bulk-menu` (dark bulk bar) + `build-lead-drawer-menu` (drawer List row + ✓ menu) · **archived absence**: Capture VIPs archived via PATCH → absent from rail + bulk menu (`build-archived-absent`), membership rows preserved in the DB. Capture data: seeded 5 base lists + the prototype's 22 sample contacts w/ their proto list values as real memberships (SQL, disclosed); real pipelines + fake AI for the wizard leg; dev-local worker deleted before commits. **Review-round re-shots (DEC-057):** `build-timeline-list-events` (drawer feed renders "Added to Capture VIPs" — script-asserted no raw slug in the DOM) · genuine resume trio `c28-before-leave` → `c28-agents-draft-row` (DRAFT pill + Continue setup) → `c28-resumed-picked` (card re-resolved, new "as of launch day" copy) · `build-postlaunch-leads-provenance` (ORIGINATED FROM mix in one frame: 8× "≣ List · Dental — local" + "✎ Manual") · `build-wizard-list-picker-search` ("den" filtering live); the duplicate resumed frame from the first pass superseded | y (31 build + 7 proto) | DEC-055, DEC-056, DEC-057 (flagged: picked-card composition, Leads New-list modal, surrogate id + workspaceId on membership; accepted: helper copy, missing picker updated-meta, "as of launch day") | `docs/fidelity/c2.8/` (38 shots) |
| Wizard bug round #2 (PR #48): B8–B10 + hardening vs `Create Agent.dc.html` guardrails step — **B8 pair (the owner's requested evidence)**: `b8-distilling` (source row Ready · About card "Reading your documents…" + amber indeterminate bar · gap-checker header "Reading your documents — results update as soon as it finishes." + mini bar · counter 0/8) → `b8-after-covered` (SAME session, script-asserted no reload: About filled with the distilled brief + "Grounded in" chip, 4 covered chips "Found in your docs", counter 4/8) — captured with the distill artificially held 45s so the in-flight window is visible; the in-flight treatment is a designed state (no prototype anchor; B3's sanctioned motion) · **B9**: `proto-guardrails-senders` (chip anchor) + `proto-connect-email` (the prototype's `openAddEmail` centered provider modal) vs `b9-connect-drawer` (build opens the C2.6 ConnectFlowDrawer over step 5 — owner-sanctioned destination, flagged composition per DEC-053) · **B10**: `proto-guardrails-schedule` (closed tz control "America/Chicago (CT)") vs `b10-tz-open` (menu, "(GMT−06:00) Central Time" rows) → `b10-tz-selected` → `b10-tz-resumed` (full resume through `/agents/new?agent=` — control still reads "America/Chicago (CT)"; draft persistence proven) · **hardening**: `hard-storage-banner` (B1 ladder names storage: "…check the storage account's network access") + `hard-storage-upload-failed` (upload → designed 503 in exactly 15s, row FAILED + Retry) — captured against a never-responding local endpoint standing in for the closed network path; sweep evidence is DB-level (synthetic 25-min PENDING row → READY on worker boot). Capture env: local pg/redis/api/web + dev-local fake-AI worker (now also writing the heartbeat key so the honest worker-offline banner doesn't overlay unrelated shots — disclosed), deleted before commits. **Evidence nit (owner review, log-only):** `proto-guardrails-senders.png` and `proto-guardrails-schedule.png` are byte-identical (the capture's second scrollIntoView never moved the page — both anchors were already in one viewport); kept as-is per owner, counted as one frame | y (11 shots, 1 duplicated frame) | DEC-053 (amended on review: 5-provider Settings chooser over the prototype's 3-provider mini-modal — owner-sanctioned, one component everywhere), DEC-054 | `docs/fidelity/wizard-b8-b10/` (2 distinct proto + 8 build) |
| C2.7 (PR #40): custom fields vs v3 `Contacts.dc.html` + `Create Agent.dc.html` — **add drawer**: defs-empty (admin dashed CTA verbatim "＋ New field for this workspace (e.g. Source URL) · Admin") · inline-create open ("New field name" input) · defs filled (teal `#1192A6` labels, Industry/Plan values) · **non-admin pair (no create affordances, def inputs still fillable)** · archived-def absence (Plan archived → input gone, values preserved) · **31-cap designed error** · proto pair (custom block + ad-hoc row) · **CSV map step**: dropdown pairs ×2 (clipped last-row anatomy match + first-row scrolled showing Standard/teal CUSTOM FIELDS sections, ✓, green `＋ Create field "…"` w/ 9px ADMIN pill, Skip) · picked-new teal pair (`#1192A6`/`#9AD6E4`, "＋ Practice type · new field") · review w/ the teal created-fields note row (prototype has NO created-count tile — flagged addition, 4 prototype tiles kept verbatim) · done · **detail drawer**: custom rows view/editing/saved pairs (teal 120px labels, persistent ✎ `#C2B79F`, edit input `#35E834` border right-aligned, 30px green ✓; saved value re-renders) · **token picker**: fallback-card pairs (chip active state `rgba(54,215,237,.24)`/`#36D7ED`, coral Required badge, verbatim never-blank note, "e.g. your practice") + filled + **inserted `{{custom.industry|your practice}}` into the body** (the prototype editor is read-only display — the build adds an explicit gated "Insert token" affordance + Enter, flagged). Capture data: defs created THROUGH the UI during capture (the acceptance flow itself); non-admin = seeded AGENT-role member (local DB); cap shot via 29 SQL filler defs, removed after; **review-round re-shots**: `build-add-drawer-humanized-label` + `build-map-dd-humanized` (stored label now "Practice type" via the shared humanize; generic "Value" placeholder) and `build-cap-error` re-shot with the real 30-field-limit copy (DEC-050) | y (30 shots) | DEC-049, DEC-050 (plan adopted; flagged: created-fields note row, insert affordance, prototype "New field name" placeholder kept over the plan example copy) | `docs/fidelity/c2.7/` (11 proto + 16 build) |
| Wizard bug round (PR #39): live-behavior evidence (no new prototype anchor — the fixes render designed states on the §3 v2 surfaces) — **B3 row lifecycle timestamped**: `b3-row-queued` (20:33:53, worker paused) → `b3-row-ingesting-bar` (20:33:57, amber indeterminate bar + "Ingesting") → `b3-row-ready` (20:34:07) on a REAL 800KB ingest; `b3-row-failed-retry` (FAILED red + green Retry on the row, "couldn't be read" sub) + `b3-retry-clicked` (toast, row back to Queued) · **B2**: `b2-url-scope-open` (This page ✓ / Entire site · Coming soon) + `b2-url-scope-site-toast` (DEC-026 gated) · **B4 reason ladder**, all four rungs as §0 toasts: no-name, no-goal, no-context, `b4-blocked-ingesting` ("Waiting for your knowledge sources to finish ingesting." over a Queued row) · **B5**: `b5-no-name-notice` (amber notice on the add-source panel) + `b5-no-name-toast` + `b5-implicit-draft` ("Draft saved · just now" top-bar chip) · **B7**: `b7-plan-failed` (interstitial holds w/ ⚠ "Sequence generation failed" + reason + gradient Retry + Back to setup — DEC-038 amendment) + `b7-plan-retrying` (Retry re-enqueues, panel returns to progress) · **B1**: `b1-env-banner-worker-offline` (worker killed, 45s heartbeat TTL expired → amber banner naming the prerequisite). Capture env: real pipelines + deterministic fake AI; batched-embed delay stretched so the 5s poll can catch INGESTING; workspace `company_address` answer temporarily flipped typed→distilled for the blocked-state shots (fresh workspaces genuinely block; this one carries the §G typed address) — restored after · **B6 trio (owner GO round)**: `b6-wizard-before-leave` (step 3, manual contact added) → `b6-agents-list-draft-row` (DRAFT pill row, menu open with "Continue setup" first) → `b6-resume-mid-wizard` (resumed via Continue setup: step 3 restored, steps 1–2 checked, contact row intact, Draft-saved chip) | y (20 shots) | DEC-047 (incl. DEC-038 amendment), DEC-048 | `docs/fidelity/wizard-bugfix/` (20 shots, timestamps in the PR #39 evidence comments) |
| C2.6 (PR #38): Settings §6 vs `Settings.dc.html` — frame pair (header · 226px sticky gradient rail w/ Account/Workspace/Communication groups, BETA pill, active gradient pill, Log out) · **Email senders**: designed §0 empty state (no OAuth/SMTP tier live — the prototype's 3 seed rows have no backing) + connect chooser + Gmail coming-soon step pair · **Mailer**: live CF_MANAGED rows pair, **500px sender drawer pair** (identity + live sends-today/daily-limit bar + domain-auth rows from stored `domainAuthStatus`; prototype's health ring/reputation/blacklist/token tiles omitted per DEC-038a) · **live add-sender flow** (identity → DNS → summary → real `POST /senders`, new row appears: `build-mailer-after-add`) · **Suppression**: live table w/ §6 channel+source columns pair, search, add modal → **`build-suppress-after-add` (real row + toast; the row now blocks sends at the boundary)** → remove-after pair, §0 skeleton/error · **Brand kit**: docs card pair w/ REAL workspace-scope upload → Indexed trio, web-pages add, Agent-summary dark card pair + Guardrails pair (designed-inert previews, noted) · 7 inert-section pairs (billing/custom/team/schedules/usage/phone/whatsapp incl. the verbatim WhatsApp amber notice). 19 proto + 29 build shots. Deviations per DEC-045 (§0 bottom-center toast; derived sending vocabulary; email→mailer chooser hand-off wired to the live flow — the prototype's branch dead-ends) | y | DEC-045, DEC-046 | `docs/fidelity/c2.6/` (48 shots, proto-* vs build-*) |
| Wizard v2 round (PR #37): §3 v2 vs updated `Create Agent.dc.html` — 9-goal grid pair · **per-goal gap rows** (offer/event/upsell picked, registry rows seeded as OPEN instantly) · **zero-context set** (amber "No context yet" subline · dashed About empty card · locked build-method placeholder · dimmed no-op Generate) · URL add INGESTING(amber)→READY pair (real P1.2 ingest vs slow local site; covered chips + About card fill on distill) · doc upload added→READY→**removed via ✕** · DEC-026 disabled-with-reason upload card (§0 — no proto anchor; config route stubbed for the shot, live behavior = staging without the storage secret) · **sticky rail footer scrolled pair** · **DEC-039a manual drawer pair** (multi-add: 2 queued rows, MA/LF initials, "2 contacts ready to add") · **Settings tracking persistence pair** (toggle → reload → still off, vs Campaign View anchor). 13 proto + 13 build shots. Capture wiring as C2.3: real ingest/distill/plan pipelines locally with deterministic fake AI (no network AI). Deviations: build gap card shows the FULL DEC-024 registry (core + goal + address = 8 rows, 0/8) vs the prototype's 4 UI-local rows — registry is authoritative (DEC-042(3), flagged in the plan); proto's 2600ms fake ingest timer not ported (real states over the same visuals) | y | DEC-042, DEC-039a | `docs/fidelity/c2.3v2/` (26 shots, proto-* vs build-*) |
| C2.5 (PR #36): Contacts §5 vs `Contacts.dc.html` — full matrix (default 22 live rows · **each segment chip switched** (New/Replied/Qualified/Booked/Unsub — A10 queries over `deriveStatus`, live counts) · Status dd · Agent dd · Filters dd (SOURCE section + quick toggles) · quick-toggle ON (active trigger + badge + gradient switch) · sorted w/ green ↑ · bulk bar · **bulk-unsubscribe before/after pair** (live POST: pills flip to Unsubscribed, Replied 6→4 / Unsub 2→4 recomputed on refetch — the A10 proof) · **450px contact drawer own pair** (LIVE Event timeline, Opens/Replies computed, Score tile omitted per DEC-038a; Title/Company/Phone/Source/Agent details) · Move menu · per-page menu (upward) · pagination · filtered-empty (verbatim copy + Reset filters + Find leads) · TRUE empty (§0 no-anchor variant, Import CSV + Add contact CTAs) · skeleton · error+Retry · add-contact modal (validity hint) · import-CSV modal. 18 proto + 22 build shots, every state distinct. Capture data: local seed, 22 contacts mirroring the prototype's sample rows across all five derived statuses incl. optOut+Suppression+UNSUBSCRIBED enrollment cases. Deviations flagged in the PR #36 plan: lists rail inert (no model), List column "—", avatar initials for emoji sample glyphs, "Mark as replied" dropped (A10 — replied is derived), proto bulk 🗑 Delete → ⊘ Unsubscribe + Export (DEC-034 semantics), email-only omissions (Call/Pause/note/tag) per DEC-038/040 precedent | y | DEC-034, DEC-038a, DEC-041 | `docs/fidelity/c2.5/` (40 shots, proto-* vs build-*) |
| C2.4 (PR #35): Agent view §4 vs `Campaign View.dc.html` — record header + 8-tab bar (active = brand gradient per prototype; checkpoints "ink fill" wording flagged in DEC-040) · Inbox (chips w/ live counts · filtered · channel dd · sort dd · thread menu · mark-done · thread view w/ real bodies · empty · skeleton) · Steps (persisted graph + live per-step counts, no-anchor stats line flagged · empty) · Leads (§4 grid · filter dd · bulk bar · filtered-empty · TRUE empty · skeleton) · **lead drawer own pair** (460px, LIVE Event/Message timeline incl. reply-classified + stage-changed rows; Opens/Replies tiles computed live, AI Score tile omitted per DEC-038a rule) · Settings (senders · schedule · limits · locked Required rows · danger zone · 500px sender drawer w/ AUTH n/3 ring · 460px volume modal) · Logs (typed feed · empty · no-anchor empty copy flagged) · inert Calls treatment · sidebar Tools-row fix pair · view-error state. 13 proto + 28 build shots, every state distinct. Capture data: local seed of enrollments/Messages/Events across all stages+intents (the engagement pipeline itself is P1.6/P1.7 staging-proofed); email-only omissions per DEC-040 | y | DEC-039, DEC-040 | `docs/fidelity/c2.4/` (41 shots, proto-* vs build-*) |
| C2.3 (PR #34): Create Agent wizard §3 vs UPDATED `Create Agent.dc.html` — full matrix (step-1 empty/goal-picked/add-source picker+URL mode/ingesting/READY/**citation chip open w/ verbatim quote**/gap checker w/ covered chips/**covered-evidence open**/gap typing/gaps resolved/**source FAILED**/build methods · **building interstitial** (proto & build both at 38%, same step highlighted) · step-2 sequence/editor/delay modal/**Branches view** · step-3 cards/CSV modal/manual drawer/added rows · step-4 toggles off+on · step-5 top/behavior/compliance/limits modal · step-6 preview · deploying · success overlay) — 20 proto + 31 build shots. Capture wiring: real P1.2/P1.3/P1.4 pipelines ran locally against a local demo site with deterministic fake AI providers (integration-test technique — REAL ingest/chunking/citation snapshots/gap checker/graph persistence, no network AI); **success-overlay shot only**: enrollment POST stubbed 201 (no Temporal locally — staging path live since P1.7 deploy); deploying overlay shot via 2.5s PATCH delay. Deviations: step-5 email-only + A8 locked consent rows + live-values-only building panel (all DEC-038); step-3 auto-prospecting/preview-contacts blocks out of §3 scope (checkpoints bind 3 cards + CSV + manual) | y | DEC-038, DEC-038a | `docs/fidelity/c2.3/` (proto-* vs build-*; step-1 set + editor drawer + ingesting/step-5 evidence re-shot in the owner-review round) |
| C2.1+C2.2 (PR #33): App shell §1 (sidebar 248px + Help flyout) + Agents List §2 vs `Agents List.dc.html` — full matrix ×2 (default · status/channel/columns dropdowns open · column toggled · selection+bulk bar · row menu open · filtered-empty · sorted · TRUE empty + CTA · skeleton · error+retry). Checkpoints §1 width **corrected by the owner 256→248** (prototype literal, issue-19 rule) before capture; build was 264 (T6) → now 248. First capture round caught a composition drift (T6 white TopBar band on the Agents screen — prototype has none) — removed and re-shot. Deviations: ~~profile sign-out button~~ (fixed in review round `7f177a9` — chevron + account flyout); **deferred nit (owner 2026-07-05): Tools-row padding pokes out vs prototype — punch-listed for the next sidebar-touching PR**; Health derived per DEC-037; prototype CDN deps (React/fonts) served locally in the sandbox — prototype files untouched | y | DEC-036, DEC-037 | `docs/fidelity/c2.2/` (22 shots, proto-* vs build-*) |
| C1 design-review round (2026-07-03): scrim **measured identical** to prototype — build (143,145,139) vs proto (145,149,147) over the canvas, both = rgba(12,20,15,.45); the lighter read came from `/design`'s all-light canvas vs the prototype's dark sidebar. Gradient-active state chips in the table demo were **demo-harness only** — switched to ink fill; product active-filter states = ink fill or green tint, never the gradient. Toggle on-state verified = `--cf-gradient-brand` (DESIGN_TOKENS). Re-shot: table default/empty/error, empty-states, 460 drawer, + new Dropdown open/closed pair | y                     | DEC-021 (filtered-empty CTA overrides prototype literal), DEC-022                                                                          | `docs/fidelity/c1/`                               |
| C1 composites on `/design` (drawer 460/480/500 closed+open · modal closed+open · table default/loading/empty/error/selection+bulk-bar · segments · chips · stepper enabled/at-min/disabled · skeleton · empty-state ×2) vs prototype anchors (Contacts / Campaign View / Create Agent / Agents List)                                                                                                                                                                                                                                                                                                         | y                     | Skeleton has **no prototype anchor** (checkpoints §0 convention — flagged, not invented beyond the convention); icons vendored per DEC-020 | `docs/fidelity/c1/` (in-repo, linked from PR #24) |

## Icon map

Prototype glyph → `lucide-react` icon. **Append-only** — once mapped, reuse
verbatim everywhere.

| Glyph | Where seen                                       | lucide icon       |
| ----- | ------------------------------------------------ | ----------------- |
| ✉     | channel chips, inbox tab (Campaign View)         | `mail`            |
| 💬    | SMS channel chip (Campaign View)                 | `message-square`  |
| 🗨     | WhatsApp channel chip (Campaign View)            | `messages-square` |
| ☎     | Voice channel chip / Calls tab                   | `phone`           |
| −     | stepper decrement (volume modal)                 | `minus`           |
| +     | stepper increment (volume modal)                 | `plus`            |
| ✕     | drawer/modal close buttons (all overlays)        | `x`               |
| ⋯     | table row menu (Contacts/Agents List rows)       | `more-horizontal` |
| 🤖/✨ | true-empty state tile (Agents List `allEmpty`)   | `sparkles`        |
| 🔍    | filtered-empty state tile (Contacts/Agents List) | `search`          |
| 🌱🦷🎥🏢🚀🛒🏠💪💆⚖🔧📊 (avatar pool) | Agents List row avatars — deterministic hash(agent.id) over the prototype's emoji pool + its 4 tints; render-time bump prevents adjacent duplicates (owner review PR #33) | (emoji avatars, not lucide) |
| ↑     | step reorder up (Steps tab cards — W3-4 designed addition) | `chevron-up`      |
| ↓     | step reorder down (Steps tab cards — W3-4 designed addition) | `chevron-down`    |
| ⏱     | delay pills (Campaign View sequence)             | `timer`           |
| ⎇     | reply-branch pill (Steps tab)                    | `git-branch`      |
| ›     | Add-a-sub-campaign chevron (Campaign View)       | `chevron-right`   |
